# __PROJECT_NAME__

TypeScript Vue app built with Frappe UI and configured for a Frappe backend.

## Setup

```sh
npm install
npm run dev
```

This project is configured for the `__BASE_ROUTE__` route. Keep this folder
inside `apps/<appname>/` so the Frappe UI Vite plugin can find your app's public
directory during production builds.
