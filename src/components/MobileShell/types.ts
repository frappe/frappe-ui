/**
 * `MobileShell` has no props — it's a pure slot frame (default slot = scrolling
 * content, `#nav` slot = bottom navigation). This interface exists so the docs
 * PropsTable and `defineProps` share one source of truth as props are added.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface MobileShellProps {}
