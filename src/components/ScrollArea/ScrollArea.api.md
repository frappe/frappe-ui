<!-- Auto Generated by scripts/propsgen.ts -->
<script setup>
  import PropsTable from '@/components/Docs/PropsTable.vue'
  import SlotsTable from '@/components/Docs/SlotsTable.vue'
  import EmitsTable from '@/components/Docs/EmitsTable.vue'
  import ExposedTable from '@/components/Docs/ExposedTable.vue'

  const propsData = [
  {
    name: 'orientation',
    description: 'Which scrollbars to render.',
    required: false,
    type: '"vertical" | "horizontal" | "both"',
    default: '"vertical"'
  },
  {
    name: 'scrollHideDelay',
    description: 'Idle delay (ms) before the thumb fades out.',
    required: false,
    type: 'number',
    default: '600'
  },
  {
    name: 'viewportClass',
    description: 'Extra classes for the scrolling viewport (e.g. padding).',
    required: false,
    type: 'string'
  }
]

  const slotsData = [
  {
    name: 'default',
    description: 'The scrolling content.',
    type: 'any'
  }
]

  const exposedData = [
  {
    name: 'viewportElement',
    description: 'The element that actually scrolls. Null until mounted.',
    type: 'HTMLElement | null'
  }
]
</script>

## API Reference

<PropsTable name="ScrollArea" :data="propsData"/>

<SlotsTable :data="slotsData"/>

<ExposedTable :data="exposedData"/>
