<!-- Auto Generated by scripts/propsgen.ts -->
<script setup>
  import PropsTable from '@/components/Docs/PropsTable.vue'
  import SlotsTable from '@/components/Docs/SlotsTable.vue'
  import EmitsTable from '@/components/Docs/EmitsTable.vue'

  const propsData = [
  {
    name: 'defaultCountry',
    description: 'Country pre-selected when the value is empty — an ISO2 code (`"in"`),\na dial code (`"+91"` or `"91"`), or a country name (`"India"`).\nDefaults to the country of the system timezone; when neither resolves,\nno country is selected.',
    required: false,
    type: 'string'
  },
  {
    name: 'size',
    description: 'Visual size of the control.',
    required: false,
    type: 'InputSize',
    default: '"sm"'
  },
  {
    name: 'variant',
    description: 'Style variant of the control.',
    required: false,
    type: 'PhoneInputVariant',
    default: '"subtle"'
  },
  {
    name: 'placeholder',
    description: 'Placeholder text for the number input.',
    required: false,
    type: 'string'
  },
  {
    name: 'disabled',
    description: 'Disables the country picker and the number input.',
    required: false,
    type: 'boolean'
  },
  {
    name: 'label',
    description: 'Label rendered above (or beside, for binary controls) the input.',
    required: false,
    type: 'string'
  },
  {
    name: 'description',
    description: 'Helper text rendered below the input.\nHidden when `error` is set.',
    required: false,
    type: 'string'
  },
  {
    name: 'error',
    description: 'Error message rendered below the input. When set, the control receives\n`aria-invalid="true"` and `data-state="invalid"`. May be either a string\nor an `Error` object whose `messages?: string[]` is rendered as stacked\nlines (with `Error.message` as the fallback).',
    required: false,
    type: 'string | FrappeUIError'
  },
  {
    name: 'required',
    description: 'Marks the field as required. Renders an asterisk next to the label and\nforwards `required` / `aria-required` to the underlying control.',
    required: false,
    type: 'boolean'
  },
  {
    name: 'id',
    description: 'HTML id of the underlying control. Auto-generated via `useId()` if omitted.',
    required: false,
    type: 'string'
  },
  {
    name: 'modelValue',
    description: '',
    required: false,
    type: 'string',
    default: '""'
  }
]

  const slotsData = [
  {
    name: 'label',
    description: 'Overrides the rendered label content. Receives `{ required }`.',
    type: '{ required: boolean; }'
  },
  {
    name: 'description',
    description: 'Overrides the rendered description content.',
    type: 'any'
  },
  {
    name: 'suffix',
    description: 'Content rendered at the end of the control shell.',
    type: 'any'
  }
]

  const emitsData = [
  {
    name: 'update:modelValue',
    description: 'Fired when the model value changes.',
    type: '[value: string]'
  }
]
</script>
## API Reference

<PropsTable name="PhoneInput" :data="propsData"/> 

<SlotsTable :data="slotsData"/> 

<EmitsTable :data="emitsData"/> 

