<!-- Auto Generated by scripts/propsgen.ts -->
<script setup>
  import PropsTable from '@/components/Docs/PropsTable.vue'
  import SlotsTable from '@/components/Docs/SlotsTable.vue'
  import EmitsTable from '@/components/Docs/EmitsTable.vue'
  import ExposedTable from '@/components/Docs/ExposedTable.vue'

  const propsData = [
  {
    name: 'theme',
    description: 'Visual color theme of the tag',
    required: false,
    type: '"gray" | "blue" | "green" | "amber" | "red" | "violet"',
    default: '"gray"'
  },
  {
    name: 'size',
    description: 'Controls the size of the tag',
    required: false,
    type: '"sm" | "md" | "lg"',
    default: '"md"'
  },
  {
    name: 'variant',
    description: 'Visual style of the tag',
    required: false,
    type: '"solid" | "subtle" | "outline" | "ghost"',
    default: '"subtle"'
  },
  {
    name: 'label',
    description: 'Content displayed inside the tag',
    required: false,
    type: 'string | number'
  },
  {
    name: 'dismissible',
    description: 'Shows the × after the label. Clicking it, or pressing Delete or Backspace while the tag has focus, emits `dismiss`',
    required: false,
    type: 'boolean',
    default: 'false'
  },
  {
    name: 'disabled',
    description: 'Dims the tag and ignores clicks, hover and `dismiss`',
    required: false,
    type: 'boolean',
    default: 'false'
  }
]

  const slotsData = [
  {
    name: 'prefix',
    description: 'Content shown before the tag label',
    type: 'any'
  },
  {
    name: 'default',
    description: 'Main tag content (overrides `label` prop)',
    type: 'any'
  },
  {
    name: 'suffix',
    description: 'Content shown after the tag label, before the ×',
    type: 'any'
  }
]

  const emitsData = [
  {
    name: 'dismiss',
    description: 'Fired when the user dismisses the tag — a click on the ×, or Delete or Backspace while it has focus. The parent owns removing it.',
    type: '[]'
  }
]

  const exposedData = [
  {
    name: 'focus',
    description: 'Moves focus to the tag. Does nothing on a non-interactive tag.',
    type: '(options?: FocusOptions) => void'
  }
]
</script>

## API Reference

<PropsTable name="Tag" :data="propsData"/>

<SlotsTable :data="slotsData"/>

<EmitsTable :data="emitsData"/>

<ExposedTable :data="exposedData"/>
