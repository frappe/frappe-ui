# LoadingIndicator

A [Spinner](./spinner) wrapper that scales by percentage instead of a fixed
size step. Prefer `Spinner` directly unless you need an arbitrary in-between
size.

## Scale

<ComponentPreview name="LoadingIndicator-Scale" />

<!-- @include: ./LoadingIndicator.api.md -->
