<!-- Auto Generated by scripts/propsgen.ts -->
<script setup>
  import PropsTable from '@/components/Docs/PropsTable.vue'
  import SlotsTable from '@/components/Docs/SlotsTable.vue'
  import EmitsTable from '@/components/Docs/EmitsTable.vue'

  const railSlots = [
  {
    name: 'default',
    description: '',
    type: '{}'
  }
]

  const railItemProps = [
  {
    name: 'label',
    description: 'Tooltip text and the base of the item\'s accessible label.',
    required: true,
    type: 'string'
  },
  {
    name: 'icon',
    description: 'Icon CSS class, e.g. `lucide-search`. Rendered centered in the cell.\nIgnored when the default slot is used (for an image, avatar, or initials).',
    required: false,
    type: 'string'
  },
  {
    name: 'to',
    description: 'Navigation target. When set, the item renders as a router link; otherwise\nit renders as a button. A `click` event fires in both cases.',
    required: false,
    type: 'string | kt | Tt'
  },
  {
    name: 'active',
    description: 'Marks the item as the current destination (indicator bar / active fill).',
    required: false,
    type: 'boolean'
  },
  {
    name: 'badge',
    description: 'Unread count. Drives the badge and folds into the accessible label.',
    required: false,
    type: 'number',
    default: '0'
  },
  {
    name: 'badgeStyle',
    description: 'How the badge renders: a count pill or a small dot.',
    required: false,
    type: '"count" | "dot"',
    default: '"count"'
  },
  {
    name: 'variant',
    description: 'Visual treatment.\n- `tile` (default): a filled cell with a left indicator bar when active —\n  for image/avatar items like communities or workspaces.\n- `ghost`: transparent until hovered, a raised highlight when active — for\n  icon shortcuts like Search or Notifications.',
    required: false,
    type: '"tile" | "ghost"',
    default: '"tile"'
  }
]

  const railItemSlots = [
  {
    name: 'default',
    description: '',
    type: '{}'
  }
]

  const railItemEmits = [
  {
    name: 'click',
    description: '',
    type: '[event: MouseEvent]'
  }
]
</script>
## API Reference

### Rail

<SlotsTable :data="railSlots"/> 

### RailItem

<PropsTable folder="Rail" name="RailItem" :data="railItemProps"/> 

<SlotsTable :data="railItemSlots"/> 

<EmitsTable :data="railItemEmits"/> 

