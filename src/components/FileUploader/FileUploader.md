# FileUploader

Allows users to upload files with ease. Supports drag-and-drop and provides
clear feedback on upload status. Uploads are private by default
(`is_private=1`). Pass `uploadArgs.private = false` only for intentionally
public files.

<ComponentPreview name="FileUploader-Examples" />
