<!-- Auto Generated by scripts/propsgen.ts -->
<script setup>
  import PropsTable from '@/components/Docs/PropsTable.vue'
  import SlotsTable from '@/components/Docs/SlotsTable.vue'
  import EmitsTable from '@/components/Docs/EmitsTable.vue'

  const propsData = [
  {
    name: 'bg',
    description: 'Background chip style.',
    required: false,
    type: 'boolean'
  },
  {
    name: 'combo',
    description: 'Shortcut combo string, e.g. `"Mod+Shift+K"`. `Mod` resolves to Cmd on macOS, Ctrl elsewhere.',
    required: false,
    type: 'string'
  },
  {
    name: 'showPlus',
    description: 'Whether to visually show `+` separators between keys (non-`bg` mode only).',
    required: false,
    type: 'boolean',
    default: 'true'
  },
  {
    name: 'altCombos',
    description: 'Alternative equivalent combos, rendered after a `/` separator (display only).',
    required: false,
    type: 'string[]',
    default: '[]'
  },
  {
    name: 'useIcons',
    description: 'Render icons for the arrow, Enter, Backspace and Delete keys.',
    required: false,
    type: 'boolean',
    default: 'true'
  }
]

  const slotsData = [
  {
    name: 'default',
    description: 'Fallback content shown when no `combo` is given.',
    type: 'any'
  }
]
</script>

## API Reference

<PropsTable name="KeyboardShortcut" :data="propsData"/>

<SlotsTable :data="slotsData"/>
