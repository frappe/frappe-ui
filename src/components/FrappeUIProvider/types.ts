/**
 * `FrappeUIProvider` has no props — it's a pass-through wrapper that also
 * mounts the imperative `dialog.*` / `toast.*` portals. This interface exists
 * so the docs PropsTable and `defineProps` share one source of truth if props
 * are ever added.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface FrappeUIProviderProps {}
