# Textarea

A multi-line input for entering longer text. Supports clear editing and flexible sizing for different content needs.

<ComponentPreview name="Textarea-Default" layout="stacked" />

## Variants

<ComponentPreview name="Textarea-Variants" />

## Sizes

<ComponentPreview name="Textarea-Sizes" />

## Labeling

`label`, `description`, `error`, and `required` are wired into the
underlying textarea via the shared labeling contract. Setting `error`
suppresses the `description` and applies `aria-invalid="true"`.

<ComponentPreview name="Textarea-Labeling" />

## States

<ComponentPreview name="Textarea-States" />

<!-- @include: ../../../meta/Textarea.md -->
