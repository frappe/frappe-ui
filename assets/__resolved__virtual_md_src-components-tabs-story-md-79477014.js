const html = '<h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-hidden="true">#</a></h2>\n<h3 id="tabs" tabindex="-1">Tabs <a class="header-anchor" href="#tabs" aria-hidden="true">#</a></h3>\n<p>It is an array of objects which contains the following attributes:</p>\n<ol>\n<li><code>label</code> is the name of the tab, it is required.</li>\n<li><code>icon</code> is the icon to be shown in the tab, it accept component and it is\noptional.</li>\n<li>You can add more attributes which can be used for custom rendering in the tab\nheader or content.</li>\n</ol>\n<h2 id="v-model" tabindex="-1">v-model <a class="header-anchor" href="#v-model" aria-hidden="true">#</a></h2>\n<p>It is used to set the active tab or change the active tab. It is required.</p>\n';
const frontmatter = {};
const relativePath = "src/components/Tabs.story.md";
export {
  frontmatter,
  html,
  relativePath
};
