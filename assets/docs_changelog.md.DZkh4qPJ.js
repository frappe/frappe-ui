import{g as o,ae as t,r as a,B as d}from"./chunks/framework.CZ-ntRvv.js";const u=JSON.parse('{"title":"Frappe UI v1 Changelog","description":"","frontmatter":{"outline":[2,4]},"headers":[],"relativePath":"docs/changelog.md","filePath":"docs/changelog.md","lastUpdated":1790275646000}'),s={name:"docs/changelog.md"};function r(n,e,i,c,l,p){return t(),a("div",null,[...e[0]||(e[0]=[d(`<h1 id="frappe-ui-v1-changelog" tabindex="-1">Frappe UI v1 Changelog <a class="header-anchor" href="#frappe-ui-v1-changelog" aria-label="Permalink to “Frappe UI v1 Changelog”">​</a></h1><p>This page lists the v1 changes that affect apps. The <strong>Unreleased</strong> section lists every change since <strong>v0.1.278</strong>. It records breaking changes, deprecations, behavior changes you can see, and migration steps. It does not record internal refactors or new tests.</p><p>A deprecated API keeps working through v1.x and logs a one-time warning in development, unless the entry says otherwise. Deprecated APIs are removed after v1. This rule does not cover members deprecated during the betas. Under <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a> (no deprecated member ships in <code>1.0.0</code>), each of them was removed before <code>1.0.0</code>, or moved to <code>frappe-ui/experimental</code>, which has no stability promise. <code>ThemeSwitcher</code> and the v0 <code>TextEditor</code> took the second path. Each entry says which, and the <a href="#deprecation-log">Deprecation log</a> lists them.</p><h2 id="how-to-read-an-entry" tabindex="-1">How to read an entry <a class="header-anchor" href="#how-to-read-an-entry" aria-label="Permalink to “How to read an entry”">​</a></h2><p>Each entry says what changed, what to do, and where the <a href="/docs/migration">migration guide</a> covers it. The tag in the heading says how the change reaches your app:</p><table tabindex="0"><thead><tr><th>Tag</th><th>What it means for your app</th></tr></thead><tbody><tr><td><strong>breaking, loud</strong></td><td>The build, the type-check or the import fails, so the tools show you every place to change.</td></tr><tr><td><strong>breaking, silent</strong></td><td>Old code still compiles and runs, but it behaves differently. Nothing fails. Search your code for the old names. The migration guide usually has a before/after for these.</td></tr><tr><td><strong>breaking; loud in TS, silent in JS</strong></td><td>TypeScript reports the change. JavaScript call sites, and values bound at runtime, do not.</td></tr><tr><td><strong>breaking</strong></td><td>The entry lists which parts are loud and which are silent.</td></tr><tr><td><strong>fix</strong>, <strong>additive</strong></td><td>Nothing to change, unless you depended on the old behavior.</td></tr></tbody></table><p>Entries are grouped by area. Inside each area, they keep the order they had in the single list this page used before.</p><h2 id="unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#unreleased" aria-label="Permalink to “Unreleased”">​</a></h2><ul><li><a href="#packaging-and-setup">Packaging and setup</a></li><li><a href="#tailwind-preset-and-design-tokens">Tailwind preset and design tokens</a></li><li><a href="#data-fetching-http-and-the-frappeui-plugin">Data fetching, HTTP and the <code>FrappeUI</code> plugin</a></li><li><a href="#uploads-and-fileuploader">Uploads and FileUploader</a></li><li><a href="#inputs-and-forms">Inputs and forms</a></li><li><a href="#date-and-time-pickers">Date and time pickers</a></li><li><a href="#select-combobox-multiselect-and-dropdown">Select, Combobox, MultiSelect and Dropdown</a></li><li><a href="#dialog-popover-tooltip-hovercard-and-toast">Dialog, Popover, Tooltip, HoverCard and Toast</a></li><li><a href="#sidebar-tabs-and-navigation">Sidebar, tabs and navigation</a></li><li><a href="#app-shell-page-header-and-color-scheme">App shell, page header and color scheme</a></li><li><a href="#keyboard-shortcuts">Keyboard shortcuts</a></li><li><a href="#other-components">Other components</a></li><li><a href="#lists">Lists</a></li><li><a href="#editor">Editor</a></li><li><a href="#code-editor">Code editor</a></li><li><a href="#charts">Charts</a></li><li><a href="#calendar">Calendar</a></li></ul><h3 id="packaging-and-setup" tabindex="-1">Packaging and setup <a class="header-anchor" href="#packaging-and-setup" aria-label="Permalink to “Packaging and setup”">​</a></h3><h4 id="npm-create-frappe-ui-sets-up-a-new-app" tabindex="-1"><code>npm create frappe-ui</code> sets up a new app (additive) <a class="header-anchor" href="#npm-create-frappe-ui-sets-up-a-new-app" aria-label="Permalink to “npm create frappe-ui sets up a new app (additive)”">​</a></h4><p><code>npm create frappe-ui@latest</code> creates a frappe-ui project with Vue, TypeScript, Vite and Tailwind CSS. Run it from <code>apps/&lt;app&gt;</code> for the frontend of a Frappe app, or anywhere else for a plain Vite app. In a Frappe app it also adds:</p><ul><li>the page that serves the frontend</li><li>the route rule in <code>hooks.py</code></li><li>the scripts in the app&#39;s <code>package.json</code> that let bench build it</li><li>the build output to <code>.gitignore</code></li></ul><p>It lists these changes and asks before it writes them.</p><h4 id="package-contract-peers-dependencies-and-the-tarball-breaking" tabindex="-1">Package contract: peers, dependencies and the tarball (breaking) <a class="header-anchor" href="#package-contract-peers-dependencies-and-the-tarball-breaking" aria-label="Permalink to “Package contract: peers, dependencies and the tarball (breaking)”">​</a></h4><p>The package now declares what it needs, ships less, and resolves its own imports for your type-checker.</p><ul><li><strong><code>tailwindcss</code> is a peer dependency, pinned to <code>&gt;=3.4.2 &lt;4</code>.</strong> 3.4 is the first version that derives the sizing families from <code>theme(&#39;spacing&#39;)</code>, and the preset depends on that. Tailwind v4 does not read the JavaScript config the preset is written in. The install now fails, instead of half-working at build time. The floor is 3.4.2, not 3.4.0: the preset uses <code>import.meta.url</code>, and the config loader in 3.4.0 and 3.4.1 cannot transform it, so the config fails to load with a <code>SyntaxError</code>.</li><li><strong><code>vite</code> and <code>vitepress</code> are optional peers</strong>, together with <code>shiki</code>, <code>@shikijs/transformers</code> and <code>@vue/compiler-dom</code>, which <code>frappe-ui/vitepress</code> imports. An app that never imports <code>frappe-ui/vite</code> or <code>frappe-ui/vitepress</code> installs none of them.</li><li><strong><code>@floating-ui/vue</code> is now a direct dependency</strong>, not a transitive install. So are the five <code>@codemirror/*</code> packages and <code>@lezer/highlight</code> that the CodeEditor imports. They resolved before only because another package happened to install them.</li><li><strong>Removed from <code>dependencies</code>:</strong> <code>@tailwindcss/line-clamp</code> (in Tailwind core since 3.3), <code>prosemirror-tables</code>, <code>ora</code>, <code>slugify</code>, and the <code>@tiptap/extension-{bubble-menu,color,highlight,image,mention,node-range}</code> packages. The shipped code imports none of them. <code>prettier</code> moved to <code>devDependencies</code>.</li><li><strong>The <code>frappe-ui/src/utils/tailwind.config</code> shim is deleted.</strong> The <code>exports</code> map already refused that path, so an app still on it fails with <code>Package subpath &#39;…&#39; is not defined</code>. Now the file is gone too. Nothing under <code>frappe-ui/src/...</code> resolves, because the map has no wildcard.</li><li><strong><code>frappe-ui/tailwind</code>, <code>frappe-ui/icons</code> and <code>frappe-ui/vite/lucideIconsPlugin</code> now carry a <code>types</code> condition.</strong> The preset&#39;s type declaration says which Tailwind sections it replaces.</li><li><strong><code>frappe-ui/style.css</code> also resolves under the <code>style</code> condition</strong> (additive). Tailwind v4 looks up package CSS with that condition. The export listed only <code>import</code>, so <code>@import &quot;frappe-ui/style.css&quot;</code> failed with <code>not exported under the condition &quot;style&quot;</code>. Both conditions point at the same file, and the file is unchanged.</li><li><strong>The tarball ships no tests.</strong> <code>*.test.*</code>, <code>*.spec.ts</code>, <code>*.cy.ts</code>, <code>*.story.vue</code>, <code>*.playground.vue</code>, <code>stories/</code> and <code>src/mocks</code> are excluded. The tarball no longer ships the 520 test, story, playground, mock and helper files it used to carry.</li><li><strong>Fix: the internal <code>#</code> imports resolve for your app.</strong> The package ships TypeScript source, so your compiler resolves these imports through the <code>imports</code> map. A type-check of an app that imports <code>frappe-ui/editor</code> used to report 136 unresolved modules. It now reports none.</li></ul><p><strong>What to do:</strong> import the preset from <code>frappe-ui/tailwind</code>. Run <code>npx -p frappe-ui packaging-v1 .</code> from your app&#39;s root to fix the preset path. Point it at the root, not <code>./src</code>: the preset is imported in <code>tailwind.config.js</code>, which sits outside <code>src</code>. The <a href="/docs/migration#packaging-and-tokens">packaging migration guide</a> lists the manual steps.</p><h4 id="frappe-ui-vite-lucideicons-is-off-by-default-breaking-loud-at-build-time" tabindex="-1"><code>frappe-ui/vite</code>: <code>lucideIcons</code> is off by default (breaking, loud at build time) <a class="header-anchor" href="#frappe-ui-vite-lucideicons-is-off-by-default-breaking-loud-at-build-time" aria-label="Permalink to “frappe-ui/vite: lucideIcons is off by default (breaking, loud at build time)”">​</a></h4><p><code>frappeui()</code> no longer installs <code>unplugin-icons</code>, <code>unplugin-auto-import</code> and <code>unplugin-vue-components</code>. frappe-ui draws its own icons as CSS mask classes (<code>lucide-check</code>), so the library needs none of them. Before this change, every app ran three extra plugins over every file it built.</p><p>If your own code writes <code>&lt;LucideCheck /&gt;</code> or imports <code>~icons/lucide/*</code>, you need the option. Without it, Vite fails the import with <code>Failed to resolve import &quot;~icons/lucide/check&quot;</code>, and a <code>&lt;LucideCheck /&gt;</code> tag renders as an unknown element with a Vue warning.</p><p><strong>What to do:</strong> pass <code>lucideIcons: true</code>.</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_indoxt">frappeui</span><span class="s_13ahmt">({</span><span class="s_r4oegk"> lucideIcons</span><span class="s_1jjt6x">:</span><span class="s_40mev6"> true</span><span class="s_13ahmt"> })</span></span></code></pre></div><p><code>npx -p frappe-ui packaging-v1 .</code> adds the option when it finds either form in the folder you point it at. When it cannot tell, it reports the plugin call.</p><h4 id="root-exports-—-frappeuierror-removed-inputlabelingprops-and-routedestination-added-breaking-loud" tabindex="-1">Root exports — <code>FrappeUIError</code> removed, <code>InputLabelingProps</code> and <code>RouteDestination</code> added (breaking, loud) <a class="header-anchor" href="#root-exports-—-frappeuierror-removed-inputlabelingprops-and-routedestination-added-breaking-loud" aria-label="Permalink to “Root exports — FrappeUIError removed, InputLabelingProps and RouteDestination added (breaking, loud)”">​</a></h4><ul><li><strong><code>FrappeUIError</code> is removed</strong> from <code>frappe-ui</code> and from <code>frappe-ui/experimental</code>. An input&#39;s <code>error</code> prop is typed where it is declared. To forward it, use <code>InputLabelingProps[&#39;error&#39;]</code>, which the root now exports. The prop accepts everything it accepted before.</li><li><strong><code>RouteDestination</code> and <code>RouteLocationObject</code> are exported.</strong> Every prop that takes a router destination is typed with them, instead of vue-router&#39;s <code>RouteLocationRaw</code>. The accepted values are the same. The generated API docs can now print the name.</li><li><code>dayjs</code> and <code>dayjsLocal</code> stay exported and public. <code>dayjsSystem</code> stays internal.</li></ul><h4 id="frappe-ui-resources-and-frappe-ui-editor-—-named-exports-breaking-loud" tabindex="-1"><code>frappe-ui/resources</code> and <code>frappe-ui/editor</code> — named exports (breaking, loud) <a class="header-anchor" href="#frappe-ui-resources-and-frappe-ui-editor-—-named-exports-breaking-loud" aria-label="Permalink to “frappe-ui/resources and frappe-ui/editor — named exports (breaking, loud)”">​</a></h4><p>Both entry files used <code>export *</code> from implementation files, so they published whatever those files exported next. Each now lists its exports by name, and a test fails when the lists drift apart. Every name that apps import is still there.</p><h4 id="frappe-ui-vite-—-types-and-docs" tabindex="-1"><code>frappe-ui/vite</code> — types and docs (additive) <a class="header-anchor" href="#frappe-ui-vite-—-types-and-docs" aria-label="Permalink to “frappe-ui/vite — types and docs (additive)”">​</a></h4><p><code>frappe-ui/vite</code> now ships hand-written types (<code>vite/index.d.ts</code>, wired through the <code>types</code> export condition). <code>frappeui(...)</code> and its options (<code>frontendRoute</code>, <code>lucideIcons</code>, <code>barrelImports</code>, <code>frappeProxy</code>, <code>jinjaBootData</code>, <code>buildConfig</code>, <code>frappeTypes</code>) are typed, so you no longer need a <code>// @ts-expect-error</code>. A new <a href="/docs/other/vite">docs page</a> covers every sub-plugin, including <code>barrelImports</code>, which had no docs before.</p><h4 id="list-style-css-and-editor-style-css-exports-—-removed" tabindex="-1">list-style.css and editor-style.css exports — removed (breaking, loud) <a class="header-anchor" href="#list-style-css-and-editor-style-css-exports-—-removed" aria-label="Permalink to “list-style.css and editor-style.css exports — removed (breaking, loud)”">​</a></h4><p>The manual <code>frappe-ui/list-style.css</code> and <code>frappe-ui/editor-style.css</code> exports are gone. The build fails with <code>Missing &quot;./list-style.css&quot; specifier</code>.</p><p>They existed only because bundlers tree-shook the side-effect <code>import &#39;./style.css&#39;</code> inside the <code>frappe-ui/list</code> and <code>frappe-ui/editor</code> entry files. This was never specific to Rolldown: plain Rollup and Vite production builds dropped the CSS too. Those entry files are now listed in <code>sideEffects</code>, so each family&#39;s CSS ships as soon as you import anything from its subpath.</p><p><strong>What to do:</strong> delete the manual <code>@import</code> lines.</p><h4 id="node-js-requirement" tabindex="-1">Node.js requirement (breaking) <a class="header-anchor" href="#node-js-requirement" aria-label="Permalink to “Node.js requirement (breaking)”">​</a></h4><p>The minimum Node version is now <code>&gt;=20.19.0</code>. It was Node 18 on 0.1.x. It is declared in <code>package.json</code> <code>engines</code>, so installers and CI show the requirement, instead of unclear engine errors from dependencies.</p><h4 id="experimental-barrel-—-tidy-and-frappeuierror" tabindex="-1"><code>experimental</code> entry — tidied, and <code>FrappeUIError</code> (breaking) <a class="header-anchor" href="#experimental-barrel-—-tidy-and-frappeuierror" aria-label="Permalink to “experimental entry — tidied, and FrappeUIError (breaking)”">​</a></h4><ul><li><strong>Breaking:</strong> <code>LabelingWrapper</code> is no longer exported from <code>frappe-ui/experimental</code>. It stays exported from its own entry file (<code>src/components/InputLabeling</code>), where <code>Combobox</code>, <code>Select</code>, <code>MultiSelect</code> and <code>MultiEmailInput</code> import it internally. Only the <code>experimental</code> re-export had zero outside importers, so only that goes. It is the only member removed in this tidy-up.</li><li><code>FrappeUIError</code> was exported from <code>frappe-ui/experimental</code> as a type during the betas. It is removed again before <code>1.0.0</code>; see <a href="#root-exports-—-frappeuierror-removed-inputlabelingprops-and-routedestination-added-breaking-loud">the root exports entry</a>. Type a forwarded <code>error</code> prop with <code>InputLabelingProps[&#39;error&#39;]</code>.</li></ul><h4 id="tsconfig-base-json-—-cleaned-up-breaking-for-extenders" tabindex="-1"><code>tsconfig.base.json</code> — cleaned up (breaking for extenders) <a class="header-anchor" href="#tsconfig-base-json-—-cleaned-up-breaking-for-extenders" aria-label="Permalink to “tsconfig.base.json — cleaned up (breaking for extenders)”">​</a></h4><p><strong>Breaking:</strong> <code>tsconfig.base.json</code> no longer sets <code>types</code> (<code>vitest/globals</code>, <code>unplugin-icons/types/vue</code>, <code>node</code>). If your app extends this file and relies on any of these globals, <code>tsc</code> fails with a missing-global error (for example <code>Cannot find name &#39;vi&#39;</code>) the first time the code uses a global that came from <code>vitest/globals</code> or <code>unplugin-icons/types/vue</code>.</p><p><code>noEmit</code> stays, because <code>allowImportingTsExtensions</code> is only legal with it. The <code>declaration</code> / <code>emitDeclarationOnly</code> pair is gone: it contradicted <code>noEmit</code>, and frappe-ui&#39;s own build did not use it.</p><p><strong>What to do:</strong> add <code>types</code> to your own <code>tsconfig.json</code>.</p><h4 id="hljs-theme-css-export-removed" tabindex="-1"><code>./hljs-theme.css</code> export removed (breaking) <a class="header-anchor" href="#hljs-theme-css-export-removed" aria-label="Permalink to “./hljs-theme.css export removed (breaking)”">​</a></h4><p><strong>Breaking:</strong> <code>frappe-ui/hljs-theme.css</code> is no longer exported. It had zero importers. The file behind it (<code>experimental/TextEditor/hljs-github.css</code>) ships until the deprecated <code>TextEditor</code> is removed.</p><h4 id="frappe-and-drive-subpaths-—-removed-breaking" tabindex="-1"><code>frappe</code> and <code>drive</code> subpaths — removed (breaking) <a class="header-anchor" href="#frappe-and-drive-subpaths-—-removed-breaking" aria-label="Permalink to “frappe and drive subpaths — removed (breaking)”">​</a></h4><p>frappe-ui is a plain UI library, so framework-specific code moves out. This was decided in #867, and the code moved in frappe/frappe#41671.</p><ul><li><strong>Breaking:</strong> <code>frappe-ui/frappe</code> is removed and the <code>frappe/</code> directory is deleted. <code>useTelemetry</code>, <code>telemetryPlugin</code>, <code>useOnboarding</code>, <code>GettingStartedBanner</code>, <code>IntermediateStepModal</code>, <code>HelpModal</code>, <code>showHelpModal</code>, <code>minimize</code>, <code>TrialBanner</code>, <code>SignupBanner</code>, <code>DataImport</code>, <code>Link</code>, <code>Filter</code> and <code>LinkProps</code> now live in <code>@framework/ui</code>. The <code>Link</code> and <code>Filter</code> there do more than the old ones. <code>Link</code> adds the <code>redirectable</code>/<code>editable</code> props and the <code>redirect</code>/<code>edit</code> emits. <code>Filter</code> adds <code>useFilters</code>, <code>parseFilters</code>/<code>serializeFilters</code> and an operator registry.</li><li><strong>Breaking:</strong> <code>OnboardingSteps</code>, <code>HelpCenter</code> and <code>showHelpCenter</code> are removed with no standalone replacement. They had zero call sites across all consumer apps. They still power <code>HelpModal</code> inside <code>@framework/ui</code>.</li><li><strong>Breaking:</strong> <code>frappe-ui/drive</code> and <code>frappe-ui/drive/*</code> are removed with no replacement. No app imported them. The drive app owns the live copy of all six components.</li><li>The <code>content</code> export from <code>frappe-ui/tailwind</code> and the docs no longer list a <code>frappe/**</code> glob.</li></ul><p><strong>What to do:</strong> import these from <code>@framework/ui</code>. If your <code>tailwind.config.js</code> lists <code>node_modules/frappe-ui/frappe/**</code> by hand, delete that line.</p><h3 id="tailwind-preset-and-design-tokens" tabindex="-1">Tailwind preset and design tokens <a class="header-anchor" href="#tailwind-preset-and-design-tokens" aria-label="Permalink to “Tailwind preset and design tokens”">​</a></h3><h4 id="typography-—-text-line-height-is-1-35-breaking-silent" tabindex="-1">Typography — <code>text-*</code> line height is 1.35 (breaking, silent) <a class="header-anchor" href="#typography-—-text-line-height-is-1-35-breaking-silent" aria-label="Permalink to “Typography — text-* line height is 1.35 (breaking, silent)”">​</a></h4><p>The tight text styles, <code>text-2xs</code> to <code>text-4xl</code> and their <code>-medium</code>, <code>-semibold</code> and <code>-bold</code> variants, move from line height 1.15 to 1.35. At 14px, one line of <code>text-base</code> is now 18.9px, not 16.1px. Text that wraps now has room between its lines. The <code>text-p-*</code> styles, and <code>text-5xl</code> and up, do not change.</p><p><code>leading-tighter</code> is a new class that sets 1.15. Use it to keep the old box: <code>text-base leading-tighter</code> is the old 1.15. <code>leading-tight</code> keeps Tailwind&#39;s 1.25.</p><ul><li><strong>frappe-ui components keep their height.</strong> Their single-line label text carries <code>leading-tighter</code>: Button, Badge, Breadcrumbs, the input, the Select, Combobox and MultiSelect triggers, menu items and group labels, picker cells, <code>FormLabel</code>, <code>InputLabel</code>, <code>ItemListRow</code>, <code>MobileNavItem</code>, the <code>SettingsRow</code> title, Sidebar, Tree items, tabs and the list header. A bare <code>Switch</code> no longer takes its height from the parent&#39;s line height.</li><li><strong>Who is affected:</strong> app code with single-line text in a tight style. A fixed-height box can clip its text or push it off-centre. Repeated rows grow by 2.4 to 5px each: menus, kanban cards, timelines, search results, tree rows. Text does not re-wrap, because line height does not move line breaks.</li></ul><p><strong>What to do:</strong> add <code>leading-tighter</code> to single-line UI text in a fixed-height box. Use <code>text-p-*</code> for long prose. See the <a href="/docs/migration#line-height">migration guide</a>.</p><h4 id="tailwind-preset-—-the-design-tokens-are-exported-as-data" tabindex="-1">Tailwind preset — the design tokens are exported as data <a class="header-anchor" href="#tailwind-preset-—-the-design-tokens-are-exported-as-data" aria-label="Permalink to “Tailwind preset — the design tokens are exported as data”">​</a></h4><p><code>frappe-ui/tailwind/tokens</code> exports the tokens by name: <code>colors</code>, <code>cssVariables</code>, <code>focusRing</code>, <code>fontFamily</code>, <code>fontSize</code>, <code>fontWeight</code>, <code>radius</code>, <code>screens</code>, <code>semanticColors</code>, <code>shadows</code>, <code>spacing</code> and <code>tracking</code>. One type ships with them: <code>TextStyle</code>, the shape of one <code>fontSize</code> entry.</p><p>Every value works without Tailwind: a resolved <code>oklch(...)</code> colour, a plain px string, a plain number. No value carries a Tailwind placeholder, so no <code>&lt;alpha-value&gt;</code> and no <code>color-mix(...)</code> reaches code that wants a value rather than a class. Tailwind-only shaping stays in <code>colorPalette.js</code> and <code>plugin.js</code>.</p><p>The tokens get their own subpath because <code>frappe-ui/tailwind</code> imports <code>tailwindcss/plugin</code> at the top level, and plain Node does not resolve it. So the preset entry only works inside a bundler. The token module imports nothing but the four data modules next to it, and it loads anywhere. Per ADR-0010, build-time entries are additive-only until <code>2.0.0</code>: a new one can be added, but none can be renamed or removed. So a second entry is allowed, and neither one may be renamed.</p><ul><li><code>semanticColors</code> is <code>{ light, dark }</code> with resolved values, keyed by category (<code>surface</code>, <code>surface-alpha</code>, <code>ink</code>, <code>outline</code>, <code>outline-alpha</code>).</li><li><code>fontSize</code> entries are objects <code>{ fontSize, lineHeight, letterSpacing, fontWeight }</code>, not Tailwind&#39;s <code>[size, meta]</code> tuple. Both families ship: <code>base</code> for text, <code>p-base</code> for paragraph.</li><li><code>shadows</code> is a flat map of complete <code>box-shadow</code> strings, keyed like the <code>shadow-*</code> utilities: <code>none</code>, <code>sm</code> through <code>2xl</code>, and <code>DEFAULT</code>. Only the light elevation ramp ships, because both themes render that one.</li><li><code>focusRing</code> is <code>{ light, dark }</code> and holds <code>outline</code> shorthands, not box-shadows. frappe-ui draws focus with <code>outline</code> (ADR-0005).</li><li><code>cssVariables</code> is keyed by theme: <code>light</code> goes on <code>:root</code>, <code>dark</code> on <code>[data-theme=&quot;dark&quot;]</code>. <code>dark</code> gives new values to the semantic and focus properties, and adds the dark ramps under <code>--dark-*</code> names. The light ramps, elevation and radius are in <code>light</code> only, because they do not change with the theme.</li><li>Keys are typed literally, so a wrong key does not compile and your editor completes the real names. Values stay <code>string</code>, or <code>number</code> for a font weight, so a token sync changes values and never a type. A font weight is a number in both places: <code>fontSize.base.fontWeight === fontWeight.regular</code>.</li></ul><p>This is additive. It replaces the removed <code>tailwind/tokens.js</code>, but the names and shapes differ from that module. The <a href="/docs/migration#hljs-theme-css-and-tailwind-tokens-js-removed">migration guide</a> gives the before and after, and <a href="/docs/getting-started/tailwind#design-tokens">Tailwind Setup</a> documents each export.</p><h4 id="tailwind-tokens-—-one-committed-source-no-vendored-figma-export" tabindex="-1">Tailwind tokens — one committed source, no vendored Figma export <a class="header-anchor" href="#tailwind-tokens-—-one-committed-source-no-vendored-figma-export" aria-label="Permalink to “Tailwind tokens — one committed source, no vendored Figma export”">​</a></h4><p>The token files moved. <code>tailwind/generated/*.json</code> is now <code>tailwind/tokens/*.js</code> (<code>colors</code>, <code>radius</code>, <code>typography</code>, <code>effects</code>), and those four files are the one source for the tokens. The importer <code>tailwind/figma-tokens-to-theme.js</code> is now <code>tailwind/tokens/build.js</code>. It still runs through <code>yarn sync-tokens</code>.</p><p>One token value changed, and only its type. Each <code>fontSize</code> entry now carries the number <code>420</code> for <code>fontWeight</code>, where it carried the string <code>&quot;420&quot;</code> before. <code>fontWeight.regular</code> was already a number, so <code>fontSize.base.fontWeight === fontWeight.regular</code> is now true. The resolved preset theme carries the number too: <code>theme.fontSize.base[1].fontWeight</code> is <code>420</code>, not <code>&#39;420&#39;</code>. Tailwind writes <code>font-weight: 420</code> from either, so the compiled CSS is byte-identical. Every other value is unchanged.</p><p>No package export pointed at any of these paths, so nothing you can import moves. A fork, or a script that reads the files from <code>node_modules</code> by path, needs the new path and the new extension.</p><ul><li><strong><code>tailwind/colors.json</code> is deleted.</strong> It was byte-identical to the generated copy. A hand-edit to it took effect in the build, and the next sync silently undid it.</li><li><strong><code>espresso-v2-design-tokens/</code> is deleted.</strong> This raw Figma export never shipped (it was not in <code>files</code>). It changed six times in four months, and four of its ten files were never read. It also does not describe what frappe-ui uses: <code>build.js</code> overrides the radius <code>9</code> value, overrules a corrupt font-weight column, drops five sizes, and converts every colour to oklch. The export is an input, so it now goes in a gitignored <code>.figma-export/</code> folder that the person running the sync fills.</li><li><strong><code>tailwind/tokens/provenance.json</code> is new.</strong> It holds the Figma file id and a sha256 per input file. This records which export produced the committed values.</li><li><strong>The four token files are JS modules, not JSON.</strong> They are <code>tailwind/tokens/*.js</code>. Each is an <code>export default</code> of the same data under a generated-file header. Reading JSON from an ES module needs an import attribute (<code>with { type: &#39;json&#39; }</code>), and the oldest config loaders in the supported peer range cannot parse one. Your <code>tailwind.config.js</code> reaches this data through the preset, so the attribute is gone. Values are unchanged.</li></ul><p>A script that reads <code>tailwind/generated/*.json</code> or <code>tailwind/colors.json</code> out of <code>node_modules</code> by path has no file to read. Raven, wiki and the LMS tests read by path today.</p><p><strong>What to do:</strong> import the values instead: <code>import { semanticColors } from &#39;frappe-ui/tailwind/tokens&#39;</code>.</p><p>To re-sync the tokens: export into <code>.figma-export/</code>, run <code>yarn sync-tokens</code>, review the diff on <code>tailwind/tokens/*.js</code>, and commit.</p><h4 id="tailwind-preset-—-hover-applies-only-where-hovering-is-possible" tabindex="-1">Tailwind preset — <code>hover:</code> applies only where hovering is possible <a class="header-anchor" href="#tailwind-preset-—-hover-applies-only-where-hovering-is-possible" aria-label="Permalink to “Tailwind preset — hover: applies only where hovering is possible”">​</a></h4><p>The preset sets <code>future.hoverOnlyWhenSupported</code>, so every <code>hover:</code> utility compiles under <code>@media (hover: hover) and (pointer: fine)</code>. A phone applied <code>:hover</code> on tap and kept it until the next tap landed somewhere else, so every ghost button a thumb touched stayed filled.</p><ul><li><strong>Behavior change:</strong> touch screens no longer show hover styles at all. This applies to your app&#39;s own <code>hover:</code> classes too, not only the library&#39;s.</li><li>Anything shown <em>only</em> on hover, such as <code>opacity-0 group-hover:opacity-100</code> or a <code>hidden</code> that hover undoes, is now unreachable on touch unless a tap also reaches it.</li></ul><p><strong>What to do:</strong> give such controls a touch path. Add <code>[@media(hover:none)]:opacity-100</code> so a device that cannot hover shows them all the time, as the library&#39;s own editor controls now do. See <a href="/docs/getting-started/tailwind#hover-styles">Tailwind setup</a>.</p><h4 id="tailwind-preset-one-sizing-scale-one-focus-ring-form-breaking" tabindex="-1">Tailwind preset: one sizing scale, one focus ring form (breaking) <a class="header-anchor" href="#tailwind-preset-one-sizing-scale-one-focus-ring-form-breaking" aria-label="Permalink to “Tailwind preset: one sizing scale, one focus ring form (breaking)”">​</a></h4><ul><li><strong>Spacing is declared once and every sizing family reads it.</strong> The scale is the integers 1 to 128 and the half steps 0.5 to 19.5, all <code>n * 0.25rem</code>. Tailwind 3.4 reads <code>theme(&#39;spacing&#39;)</code> for <code>width</code>, <code>height</code>, <code>size</code>, <code>minWidth</code>, <code>maxWidth</code> and <code>minHeight</code>. So <code>p-4.5</code>, <code>w-112</code>, <code>h-13</code>, <code>max-h-52</code> and <code>gap-7.5</code> now exist because the scale has them, not because someone added that one value. The plugin&#39;s hand-written <code>spacing</code>, <code>width</code>, <code>height</code>, <code>minWidth</code> and <code>maxHeight</code> blocks are deleted. Two classes change: <ul><li><code>w-wizard</code> (650px, named after an app screen) is removed. Use <code>w-[650px]</code>.</li><li><code>min-w-50</code> follows the scale, so it is 12.5rem instead of 18rem. Use <code>min-w-[18rem]</code> to keep the old size.</li></ul></li><li><strong>The <code>--focus-&lt;name&gt;</code> box-shadow variables are removed.</strong> Keep <code>--focus-outline-&lt;name&gt;</code> and apply it as an outline: <code>outline: var(--focus-outline-default)</code>. The names are <code>default</code>, <code>red</code>, <code>green</code>, <code>amber</code>, <code>blue</code> and <code>violet</code>. There is one form: 2px in light mode and 3px in dark. A ring no longer changes the element&#39;s size.</li><li><strong><code>rounded-9</code> is 100px</strong>, not 999px. It is the pill radius for large surfaces. <code>rounded-full</code> (9999px) is still the circle.</li><li><strong>Documented, not changed:</strong> the preset replaces Tailwind&#39;s <code>colors</code>, <code>fontSize</code>, <code>screens</code>, <code>borderRadius</code> and <code>boxShadow</code> sections. Stock classes from those five sections (<code>text-base</code> at Tailwind&#39;s own size, <code>2xl:</code>, <code>shadow-inner</code>) are not generated. Alpha token names are unchanged: <code>alpha</code> stays a suffix on the group key (<code>bg-gray-alpha-100</code>, <code>bg-surface-alpha-gray-2</code>).</li><li><code>frappe-ui/tailwind</code> exports <code>content</code>, the source globs that produce classes inside the package. <code>experimental/FloatingWindow</code> and <code>vitepress</code> are now in that list. Without the <code>vitepress</code> glob, a docs site on <code>frappe-ui/vitepress</code> never gets the theme&#39;s own layout classes.</li></ul><h4 id="radius-aliases-and-text-black-styles-removed-breaking-silent" tabindex="-1">Radius aliases and <code>text-*-black</code> styles removed (breaking, silent) <a class="header-anchor" href="#radius-aliases-and-text-black-styles-removed-breaking-silent" aria-label="Permalink to “Radius aliases and text-*-black styles removed (breaking, silent)”">​</a></h4><p>Per ADR-0006 and ADR-0008 (#998, decided in #993):</p><ul><li><p>The named radius aliases (<code>rounded</code>, <code>rounded-sm</code>, <code>rounded-md</code>, <code>rounded-lg</code>, <code>rounded-xl</code>, <code>rounded-2xl</code>, and their directional forms) are removed. The numbered tokens are the only radius names. The px values are identical:</p><table tabindex="0"><thead><tr><th>Removed</th><th>Use</th></tr></thead><tbody><tr><td><code>rounded</code></td><td><code>rounded-4</code></td></tr><tr><td><code>rounded-sm</code></td><td><code>rounded-1</code></td></tr><tr><td><code>rounded-md</code></td><td><code>rounded-5</code></td></tr><tr><td><code>rounded-lg</code></td><td><code>rounded-6</code></td></tr><tr><td><code>rounded-xl</code></td><td><code>rounded-7</code></td></tr><tr><td><code>rounded-2xl</code></td><td><code>rounded-8</code></td></tr></tbody></table><p><code>rounded-none</code> and <code>rounded-full</code> stay. <strong>Silent break:</strong> the preset replaces Tailwind&#39;s scale, so an alias you did not migrate produces no CSS. You get square corners and no build error.</p></li><li><p>The <code>text-&lt;size&gt;-black</code> and <code>text-p-&lt;size&gt;-black</code> style classes are removed. They had zero usage, and the Figma black weights were corrupt export data. This is also a silent break.</p></li></ul><p><strong>What to do:</strong> run the <code>tokens-v2</code> codemod. It now does these renames. It is safe to run more than once, and it runs in every mode. It rewrites a bare <code>rounded</code> only inside quoted strings and <code>@apply</code> rules, so search for leftovers. For <code>font-extrabold</code> / <code>font-black</code> next to a text size, the codemod reports the site instead of merging it onto the removed class.</p><h4 id="tailwind-preset-—-content-export-added" tabindex="-1">Tailwind preset — <code>content</code> export added (additive) <a class="header-anchor" href="#tailwind-preset-—-content-export-added" aria-label="Permalink to “Tailwind preset — content export added (additive)”">​</a></h4><p><code>frappe-ui/tailwind</code> exports <code>content</code>, the list of globs for the frappe-ui source folders that produce Tailwind classes. Tailwind v3 does not merge a preset&#39;s <code>content</code>, so apps had to maintain these paths by hand. They had already drifted: some apps on <code>frappe-ui@1.0.0-beta</code> glob <code>src/components/**</code> only, which silently drops every class that the editor and list components produce.</p><p><strong>What to do:</strong> spread <code>content</code> into the <code>content</code> array of your app&#39;s <code>tailwind.config.js</code>, instead of maintaining the paths by hand. See the new <a href="/docs/getting-started/tailwind">Tailwind Setup</a> docs page.</p><h4 id="tailwind-preset-—-tokens-js-export-removed-breaking" tabindex="-1">Tailwind preset — <code>tokens.js</code> export removed (breaking) <a class="header-anchor" href="#tailwind-preset-—-tokens-js-export-removed-breaking" aria-label="Permalink to “Tailwind preset — tokens.js export removed (breaking)”">​</a></h4><p>The <code>./tailwind/tokens.js</code> export is removed outright, with no deprecation period. It re-exported <code>colorPalette.js</code> through <code>export *</code>. The library does not allow <code>export *</code> from an implementation module.</p><p>This ships before the <code>1.0.0</code> tag, while the library can still change freely. The rule that requires a deprecation period starts at the tag.</p><p><strong>Correction.</strong> This entry used to say the export had zero importers anywhere. That was wrong. <code>frappe/studio</code> imports it at <code>frontend/src/utils/espressoTokens.ts</code>. A code search across <code>org:frappe</code> finds Studio as the only consumer, and Studio pins <code>frappe-ui@1.0.0-beta.25</code>, so the break has not reached it. The advice to use the preset directly was also wrong: the preset is a Tailwind <code>Config</code> and carries no readable values. The subpath is back as <code>frappe-ui/tailwind/tokens</code>, without the <code>.js</code> and in a new shape.</p><p><strong>What to do:</strong> import from <code>frappe-ui/tailwind/tokens</code>. See <a href="#tailwind-preset-—-the-design-tokens-are-exported-as-data">the design tokens entry</a> and the <a href="/docs/migration#hljs-theme-css-and-tailwind-tokens-js-removed">migration guide</a>.</p><h4 id="tailwind-preset-—-unused-token-vocabulary-and-utilities-removed-breaking" tabindex="-1">Tailwind preset — unused token vocabulary and utilities removed (breaking) <a class="header-anchor" href="#tailwind-preset-—-unused-token-vocabulary-and-utilities-removed-breaking" aria-label="Permalink to “Tailwind preset — unused token vocabulary and utilities removed (breaking)”">​</a></h4><p>This is the design-token audit before the additive-only token freeze (#940). Every family in <code>tailwind/generated/*.json</code>, and every utility and <code>--*</code> variable that <code>plugin.js</code> produces, was checked against frappe-ui&#39;s own source, docs and stories. It was also checked against a new survey of all consumer apps (crm, helpdesk, gameplan, insights, builder, suite, central, frappe_calendar, frappe-ui-starter, and frappe&#39;s <code>ui/</code> package).</p><p>These are in use and are not touched: the primitive and semantic color ramps (all twelve hues, in <code>surface-*</code>/<code>ink-*</code>/<code>outline-*</code>), every typography weight (including <code>bold</code>/<code>black</code>), and every size through <code>text-12xl</code>.</p><p>These had zero call sites anywhere, and are removed:</p><ul><li><strong><code>text-tiny</code></strong> and its uppercase text-transform. Even the docs&#39; own type-scale page did not show it.</li><li><strong><code>text-13xl</code> through <code>text-16xl</code></strong> (and their <code>-medium</code>/<code>-semibold</code>/ <code>-bold</code>/<code>-black</code> variants). The docs&#39; own &quot;display sizes&quot; showcase stops at <code>text-12xl</code>. Even the type-scale demo did not use these four sizes.</li><li><strong><code>shadow-status</code></strong> and its <code>--elevation-status</code> variable. It was named once in the text of the elevation docs page, but never rendered there or anywhere else.</li><li><strong><code>surface-alert-button-*</code> / <code>ink-alert-button-*</code></strong> (<code>default</code>, <code>info</code>, <code>success</code>, <code>warning</code>, <code>error</code>). <code>Alert</code>&#39;s buttons get their color from the shared <code>variant</code> + <code>theme</code> props. This Figma spec was never wired to code.</li><li><strong><code>surface-alpha-gray-2-overlay</code></strong>. It resolved to the black/white overlay ramp rather than the gray-alpha ramp its name implies. That broke the <code>{family}-{step}</code> pattern every other <code>surface-alpha</code> entry follows.</li></ul><p>All five are silent breaks. A missing Tailwind class or <code>--*</code> variable stops applying, with no build or type error. See the <a href="/docs/migration#unused-tokens-and-utilities-removed">migration guide</a>.</p><p>The token generator (<code>tailwind/figma-tokens-to-theme.js</code>, since renamed; see <a href="#tailwind-tokens-—-one-committed-source-no-vendored-figma-export">the tokens source entry</a>) now filters these out at the source. They stay gone on the next <code>yarn sync-tokens</code> run. <code>ALPHA_FAMILIES</code> also dropped a dead <code>&#39;red-alpha&#39;</code> entry that never matched anything in the Figma export. No generated token changed.</p><p><strong>Also removed:</strong> <code>tailwind/colors.js</code>, a 642-line legacy color module that <code>colors.json</code> + <code>colorPalette.js</code> replaced. It had zero importers, and no <code>frappe-ui</code> package export reached it (there is no <code>./tailwind/*</code> wildcard). So deleting it changes nothing for apps.</p><h3 id="data-fetching-http-and-the-frappeui-plugin" tabindex="-1">Data fetching, HTTP and the <code>FrappeUI</code> plugin <a class="header-anchor" href="#data-fetching-http-and-the-frappeui-plugin" aria-label="Permalink to “Data fetching, HTTP and the FrappeUI plugin”">​</a></h3><h4 id="data-fetching-v2-—-transform-ran-twice-on-cached-data-fix" tabindex="-1">Data fetching (v2) — <code>transform</code> ran twice on cached data (fix) <a class="header-anchor" href="#data-fetching-v2-—-transform-ran-twice-on-cached-data-fix" aria-label="Permalink to “Data fetching (v2) — transform ran twice on cached data (fix)”">​</a></h4><p>With <code>cacheKey</code> and <code>transform</code> both set, <code>useList</code> and <code>useCall</code> saved the rows after <code>transform</code> had changed them. When they showed the saved rows again, they ran <code>transform</code> on them a second time. A <code>transform</code> that gives a different result the second time broke: one that parses a JSON field threw <code>&quot;[object Object]&quot; is not valid JSON</code> the next time the list opened. The same was true for <code>useDoc</code> methods with <code>cacheKey</code>.</p><ul><li>The cache now holds the response as the server sent it. <code>transform</code> runs on it once, each time it is read, the same as on a fresh response. Values that do not survive JSON, such as <code>Date</code> objects, now come back correctly.</li><li>The cache key format changed, so entries saved by older versions are ignored. The first load after the update fetches from the server, the same as with no cache. The old entries stay in IndexedDB until the app clears it.</li><li><code>useList</code> runs <code>transform</code> on every loaded row at once, not one page at a time. It runs again after each new page and after each <code>updateRow</code> or <code>removeRow</code>. A <code>transform</code> that sorts, groups or removes duplicates now gives the same result on a fresh list and on a cached one. <code>transform</code> gets a copy of the rows, so it may change them in place.</li><li><code>updateRow</code> and <code>removeRow</code> change the rows as the server sent them, and save the changed rows to the cache at once. <code>updateRow</code> takes values in the shape the server sends. With a <code>transform</code> that parses a JSON field, an updated row now shows the parsed value, not the raw string.</li><li><code>initialData</code> is now in the shape the server sends, and goes through <code>transform</code> the same as a response. <code>updateRow</code> and <code>removeRow</code> change its rows before the first response arrives. Before, they did nothing on these rows. <code>useList</code> works on its own copy, so the array you pass never changes. <code>initialData</code> rows are not saved to the cache.</li></ul><p>No API change: the types stay the same, and the behaviour changes are the ones listed above.</p><h4 id="data-fetching-v2-—-each-user-has-their-own-cache-fix" tabindex="-1">Data fetching (v2) — each user has their own cache (fix) <a class="header-anchor" href="#data-fetching-v2-—-each-user-has-their-own-cache-fix" aria-label="Permalink to “Data fetching (v2) — each user has their own cache (fix)”">​</a></h4><p><code>useCall</code> and <code>useList</code> save a <code>cacheKey</code> response in IndexedDB, and <code>useDoc</code> saves every document there. The keys did not include the user, so a second user on the same browser could see the first user&#39;s saved data.</p><ul><li>The keys now include the signed-in user, read from the <code>user_id</code> cookie that Frappe sets at login. A user reads only their own saved data. The cookie is read each time data is saved, so a user who signs in without a page reload has their data saved under their own key. Still, reload the page when the user changes: data loaded for the previous user stays in memory and can be saved under the new user&#39;s key.</li><li>Guests and requests with no session share one cache that is not tied to a user. A signed-in user never reads from it.</li><li>Documents saved by older versions are ignored, like lists and calls. The first load after the update fetches from the server.</li></ul><p>See <a href="/docs/data-fetching/use-call#cache-namespace">One cache per user</a>.</p><p>No API change.</p><h4 id="data-fetching-v2-—-stale-responses-no-longer-write-the-shared-stores-fix" tabindex="-1">Data fetching (v2) — stale responses no longer write the shared stores (fix) <a class="header-anchor" href="#data-fetching-v2-—-stale-responses-no-longer-write-the-shared-stores-fix" aria-label="Permalink to “Data fetching (v2) — stale responses no longer write the shared stores (fix)”">​</a></h4><p>Two writes to one document at the same time could leave <code>docStore</code> and <code>listStore</code> (and every view bound to them) on the response that arrived last, instead of the newest one. Meanwhile <code>data</code> already held the fresh response (#1017).</p><p>The check now lives in the stores:</p><ul><li>Every request takes an increasing sequence number when it is sent. The response&#39;s store writes carry that number. The stores reject a write for a document that a request sent later has already written. It is a sequence, not a timestamp, because clocks move and two responses can share a millisecond.</li><li>One ordering covers every writer: the <code>docs</code> side channel, the <code>useDoctype</code> / <code>useList</code> / <code>useDoc</code> hooks, and any mix of instances or paths that write the same document.</li><li>A sequence is recorded only when a write that changes data lands. A newer request that failed wrote nothing on the server, so it does not make the older success stale.</li><li>A read (GET) is accepted on its sequence but records nothing. The server may answer a later reload before an earlier save commits, and the save must still land.</li><li>A delete seals the document when it settles, because a delete is final. No write or reload still in flight, whatever order it was sent in, can re-create a deleted document.</li><li>One known limitation: the check orders by send time. A read sent after a save, handled by the server before the save committed, and answered after it, is accepted and puts the pre-save value back. Fixing that needs ordering on the server, which this design does not do.</li></ul><p><code>useAction</code> still skips the <code>onSuccess</code>/<code>onError</code> hooks of a submit when a newer submit with the same key, on the same instance, has already overtaken it. The store check protects the stores. This skip only avoids re-running hook side effects with a stale response. <code>useDoc</code>&#39;s write members and <code>useNewDoc</code> skip their hooks the same way. None of these skips decides a store write: only the store check does. It compares per document and knows whether the newer request landed. So an overtaken insert of a different document still lands, and an older success is kept when the newer submit failed.</p><p>No API change. Behavior changes, if you relied on the old behavior:</p><ul><li>A stale <code>setValue</code>/<code>delete</code> on the same document no longer triggers <code>useList</code>&#39;s automatic refetch. The newest submit&#39;s refetch already ran.</li><li>Submits with different keys, and submits with no key (inserts), are independent. All of their hooks still fire, as before.</li></ul><h4 id="data-fetching-v2-—-usedoc-writes-and-usenewdoc-get-one-request-per-submit-fix" tabindex="-1">Data fetching (v2) — <code>useDoc</code> writes and <code>useNewDoc</code> get one request per submit (fix) <a class="header-anchor" href="#data-fetching-v2-—-usedoc-writes-and-usenewdoc-get-one-request-per-submit-fix" aria-label="Permalink to “Data fetching (v2) — useDoc writes and useNewDoc get one request per submit (fix)”">​</a></h4><p><code>useDoc</code>&#39;s <code>setValue</code>, <code>delete</code> and every <code>methods:</code> entry, and <code>useNewDoc</code>, still shared one request after <a href="#data-fetching-v2-—-one-request-per-submit">the <code>useDoctype</code>/<code>useList</code> fix</a>. Two submits at once cancelled each other, and every submit resolved from the same <code>data</code>, so a caller could get another caller&#39;s answer, or <code>null</code>. Each submit now sends its own request and resolves with its own response (#991).</p><ul><li>No API change. These members keep the full <code>useCall</code> surface: same members, same types. (At the time, <code>submit()</code> resolved <code>null</code> on a failed request. It rejects now; see <a href="#data-fetching-v2-—-writes-reject-reads-resolve-breaking-silent">writes reject, reads resolve</a>.)</li><li><code>data</code> and <code>error</code> belong to the submit that started last, the same as <code>useDoctype</code> and <code>useList</code>. A stale submit answers its own caller and writes nothing shared. <code>loading</code> stays <code>true</code> until every submit settles.</li><li>Behavior change, if you relied on it: a second submit no longer cancels the first. Both requests reach the server.</li></ul><h4 id="data-fetching-v2-—-writes-reject-reads-resolve-breaking-silent" tabindex="-1">Data fetching (v2) — writes reject, reads resolve (breaking, silent) <a class="header-anchor" href="#data-fetching-v2-—-writes-reject-reads-resolve-breaking-silent" aria-label="Permalink to “Data fetching (v2) — writes reject, reads resolve (breaking, silent)”">​</a></h4><p>One rule now covers every composable: a write rejects when it fails, and a read resolves. A failed write must not let its caller continue down the success path.</p><ul><li><strong>Now rejects:</strong> <code>useCall</code>&#39;s <code>submit()</code>, and <code>useDoc</code>&#39;s <code>setValue</code>, <code>delete</code> and every <code>methods:</code> member. They used to resolve with <code>null</code>.</li><li><strong>Still resolves:</strong> <code>execute()</code>, <code>fetch()</code> and <code>reload()</code> in every composable. Read <code>error</code> after you await them.</li><li><code>useDoctype</code> and <code>useList</code> write methods already rejected. Nothing changes for them.</li><li><code>useCall({ refetch: true }).submit()</code> follows the rule too. It used to return <code>undefined</code> at once and leave the request to the params watcher, so a failed write could neither reject nor resolve with its response. It now settles on a request that carried its params. That is the request the params change started, or a request it sends itself when the change started none: the same object twice, a <code>GET</code> whose params build the same URL, or a <code>submit()</code> with no argument (which used to send nothing at all). Two submits in the same tick still share one request, and a params change replaces the request in flight. The <a href="/docs/data-fetching/use-call#refetch-and-submit"><code>refetch</code> and <code>submit</code></a> section explains what that means.</li></ul><p>A related fix: a successful response now clears <code>error</code>. Before, two overlapping submits ended with the newer one rejecting, on the cancelled request it replaced, even though its own request succeeded.</p><p><code>error</code>, <code>onError</code> and the stores otherwise behave as before. This break is silent: nothing fails to build, the success path simply stops running, and a <code>submit()</code> you do not await becomes an unhandled rejection. <code>null</code> is a valid response now, so <code>if (!result)</code> after a <code>submit()</code> is no longer a failure check.</p><p><strong>What to do:</strong> await write calls inside <code>try</code>/<code>catch</code>, or add <code>.catch()</code>. Do not treat a <code>null</code> result as a failure. The <a href="/docs/migration#data-fetching-writes-reject">migration guide</a> has the before/after and what to search for.</p><h4 id="data-fetching-v2-—-usedoc-method-names-cannot-shadow-built-in-members-breaking-loud" tabindex="-1">Data fetching (v2) — <code>useDoc</code> method names cannot shadow built-in members (breaking, loud) <a class="header-anchor" href="#data-fetching-v2-—-usedoc-method-names-cannot-shadow-built-in-members-breaking-loud" aria-label="Permalink to “Data fetching (v2) — useDoc method names cannot shadow built-in members (breaking, loud)”">​</a></h4><p>A <code>methods:</code> key with the same name as a member of the object <code>useDoc</code> returns (<code>doc</code>, <code>error</code>, <code>loading</code>, <code>reload</code>, <code>setValue</code>, <code>delete</code>, and the rest) used to replace that member silently, which broke the document. <code>useDoc</code> now throws at setup, and names the key and the collision.</p><p><strong>What to do:</strong> rename the key and keep the server method name: <code>{ reloadItems: { name: &#39;reload_items&#39; } }</code>.</p><h4 id="data-fetching-v2-—-usenewdoc-and-usedoc-methods-take-fewer-options-breaking" tabindex="-1">Data fetching (v2) — <code>useNewDoc</code> and <code>useDoc</code> methods take fewer options (breaking) <a class="header-anchor" href="#data-fetching-v2-—-usenewdoc-and-usedoc-methods-take-fewer-options-breaking" aria-label="Permalink to “Data fetching (v2) — useNewDoc and useDoc methods take fewer options (breaking)”">​</a></h4><p>An insert and a document method run when <code>submit()</code> is called, so the options that say otherwise are gone.</p><ul><li><code>useNewDoc</code> options no longer accept <code>refetch</code>, <code>cacheKey</code> or <code>staleOnError</code>. It already fixed <code>url</code>, <code>method</code>, <code>params</code> and <code>immediate</code>. <code>refetch: true</code> used to re-send the insert on every edit to <code>doc</code>.</li><li><code>useDoc</code>&#39;s <code>methods:</code> options no longer accept <code>immediate</code> or <code>refetch</code>.</li><li>Both force <code>immediate: false, refetch: false</code> at runtime. A JavaScript app that still passes them is safe: the values are ignored.</li></ul><p><strong>What to do:</strong> delete these options.</p><h4 id="data-fetching-—-frapperequesterror-is-renamed-frapperesourceerror-breaking-loud" tabindex="-1">Data fetching — <code>FrappeRequestError</code> is renamed <code>FrappeResourceError</code> (breaking, loud) <a class="header-anchor" href="#data-fetching-—-frapperequesterror-is-renamed-frapperesourceerror-breaking-loud" aria-label="Permalink to “Data fetching — FrappeRequestError is renamed FrappeResourceError (breaking, loud)”">​</a></h4><p>The error type that the resource layer raises (<code>call</code>, <code>frappeRequest</code>, <code>createResource</code> and the other v1 resources) is now <code>FrappeResourceError</code>. Both errors in the library are server responses, so &quot;request&quot; versus &quot;response&quot; said nothing. Each is now named for the layer that raises it. Only the v1 betas ever exported the old name: v0.1.278 did not export it at all.</p><ul><li><code>FrappeResponseError</code>, which the v2 composables raise, does not change.</li><li>The fields do not change, and neither does how you narrow either one. <code>FrappeResponseError</code> is a class. <code>FrappeResourceError</code> is a type over a plain <code>Error</code>, so read a field such as <code>exc_type</code>.</li><li>There is no alias (ADR-0008), so importing the old name fails the build.</li></ul><p><strong>What to do:</strong> rename the import. The <a href="/docs/migration#errors-renamed">migration guide</a> has the before/after.</p><h4 id="data-fetching-v2-—-error-classes-and-read-aliases-documented" tabindex="-1">Data fetching (v2) — error classes and read aliases documented <a class="header-anchor" href="#data-fetching-v2-—-error-classes-and-read-aliases-documented" aria-label="Permalink to “Data fetching (v2) — error classes and read aliases documented”">​</a></h4><p>No code change. The docs now state two facts:</p><ul><li><code>FrappeResponseError</code> (the v2 composables) and <code>FrappeResourceError</code> (<code>call</code>, <code>frappeRequest</code>, v1 resources) stay separate, with different fields. <code>UploadError</code> is the third error. <a href="/docs/data-fetching/use-call#which-error-class">A table</a> says which API raises which. It also says which of the three you can narrow with <code>instanceof</code>: <code>FrappeResourceError</code> is a TypeScript type over a plain <code>Error</code>, not a class, so only the other two have a value to test.</li><li><code>execute</code>/<code>fetch</code>/<code>reload</code>, and <code>loading</code>/<code>isFetching</code>, stay as aliases of one another. This is the one place the library publishes two names for one thing. The docs use <code>reload()</code> and <code>loading</code>.</li></ul><h4 id="createlistresource-—-haspreviouspage-stale-after-reload-fix" tabindex="-1"><code>createListResource</code> — <code>hasPreviousPage</code> stale after <code>reload()</code> (fix) <a class="header-anchor" href="#createlistresource-—-haspreviouspage-stale-after-reload-fix" aria-label="Permalink to “createListResource — hasPreviousPage stale after reload() (fix)”">​</a></h4><p><code>reload()</code> sets <code>start</code> to <code>0</code> for a moment, to re-fetch the loaded pages as one request, and then restores it. <code>hasPreviousPage</code> was computed while <code>start</code> was still <code>0</code> and was not computed again after the restore. So it stayed <code>false</code> even when <code>start</code> was back above <code>0</code>. <code>reload()</code> now recomputes <code>hasPreviousPage</code> after restoring <code>start</code>.</p><h4 id="v1-resources-—-at-bar-exception-documented-listresource-gets-test-coverage" tabindex="-1">v1 resources — stay JavaScript, and <code>createListResource</code> gets tests <a class="header-anchor" href="#v1-resources-—-at-bar-exception-documented-listresource-gets-test-coverage" aria-label="Permalink to “v1 resources — stay JavaScript, and createListResource gets tests”">​</a></h4><p>The v1 resources (<code>createResource</code>, <code>createListResource</code>, <code>createDocumentResource</code>, <code>getCachedResource</code>, <code>getCachedListResource</code>, <code>getCachedDocumentResource</code>, <code>resourcesPlugin</code>, <code>saveLocal</code>, <code>getLocal</code>, <code>deleteLocal</code>, <code>onDocUpdate</code>) ship without deprecation, frozen at <code>1.0.0</code> (#886).</p><p>Every other export had to meet the v1 quality bar. <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0013-v1-resources-implementation-freeze.md" target="_blank" rel="noreferrer">ADR-0013</a> records the one exception: the implementation stays hand-written JavaScript, not TypeScript, permanently. With 344 call sites in production, a rewrite is riskier than the freeze. <code>createListResource</code>, the second-most-used export with 57 call sites, gets tests for the first time (<code>listResource.test.ts</code>): pagination, <code>insert</code>/<code>setValue</code> refreshing the list, caching, and how <code>reload()</code> restores the pagination state.</p><h4 id="http-transport-—-four-paths-collapse-to-one-breaking" tabindex="-1">HTTP transport — four paths collapse to one (breaking) <a class="header-anchor" href="#http-transport-—-four-paths-collapse-to-one-breaking" aria-label="Permalink to “HTTP transport — four paths collapse to one (breaking)”">​</a></h4><p><code>frappeRequest</code> is now the only transport. Removed from the root export:</p><ul><li><strong><code>request</code></strong>: the bare <code>fetch</code> wrapper under <code>frappeRequest</code>, now internal. Use <code>frappeRequest</code>.</li><li><strong><code>createCall</code></strong>: no app used it.</li><li><strong><code>initSocket</code></strong>: no app used it; every app defines its own. <code>socket.io-client</code> stays a dependency, because <code>resources/realtime.ts</code> exports functions typed against its <code>Socket</code>.</li></ul><p>All three fail the build at the import.</p><p><strong><code>call</code> now honours <code>setConfig</code> (silent).</strong> <code>call</code> keeps its <code>(method, args, options)</code> signature, the value it resolves to, and the <code>{ response, status, error }</code> shape it passes to <code>onError</code>. But it now hands the request to <code>frappeRequest</code> instead of building its own <code>fetch</code>. It never imported <code>getConfig</code>, so <code>requestBaseUrl</code> and <code>requestHeaders</code> were ignored on every <code>call()</code>, while <code>frappeRequest</code> respected them. In apps that set either one, there are two effects: <code>call</code> now goes to the configured base URL with <code>credentials: &#39;include&#39;</code>, and <code>_server_messages</code> from a <code>call</code> now reach <code>serverMessagesHandler</code>.</p><p><strong><code>FrappeResourceError</code> is now exported.</strong> <code>frappeRequest</code> threw it, but nothing exported it, so you could not type a <code>catch</code>.</p><h4 id="frapperequest-—-onerror-fired-twice-per-failure-fix" tabindex="-1"><code>frappeRequest</code> — <code>onError</code> fired twice per failure (fix) <a class="header-anchor" href="#frapperequest-—-onerror-fired-twice-per-failure-fix" aria-label="Permalink to “frappeRequest — onError fired twice per failure (fix)”">​</a></h4><p><code>request()</code> attached <code>transformError</code> with a <code>.catch</code> at the end, which also caught what <code>transformResponse</code> threw. So every failed HTTP response ran <code>onError</code> twice. It now runs once. The fix is that <code>frappeRequest</code> marks an error it has already reported, not that the rejection handler sees less. That matters: a response that is <em>ok</em> but whose body does not parse is a failure only that handler sees, and it still reaches <code>onError</code>. Frappe answers an expired session with 200 and its login page, so <code>response.json()</code> throws.</p><p><strong>A method name starting with <code>http</code> skipped the <code>/api/method/</code> prefix (fix).</strong> The check for an absolute URL was <code>url.startsWith(&#39;http&#39;)</code>. That matches the four letters, not a scheme, so <code>http_utils.api.run</code> was fetched as a relative path. It now matches <code>https?://</code>.</p><p><code>frappeRequest</code> also has an explicit return type now and passes its type argument through, so <code>frappeRequest&lt;Foo&gt;()</code> resolves to <code>Foo</code> rather than <code>unknown</code>.</p><p><strong><code>login</code> returned only <code>message</code> when <code>requestBaseUrl</code> was set (fix).</strong><code>login</code> is the one endpoint that resolves to the whole body, so a caller can read <code>full_name</code> and <code>home_page</code>. The check compared the whole URL with <code>/api/method/login</code>. <code>requestBaseUrl</code> makes that URL absolute, so it stopped matching, and <code>login</code> quietly resolved to <code>data.message</code>. It now matches on the path.</p><h4 id="frappeui-plugin-—-one-option-left-breaking" tabindex="-1"><code>FrappeUI</code> plugin — one option left (breaking) <a class="header-anchor" href="#frappeui-plugin-—-one-option-left-breaking" aria-label="Permalink to “FrappeUI plugin — one option left (breaking)”">​</a></h4><p><code>app.use(FrappeUI)</code> accepts <code>resources</code> and nothing else, and it no longer installs it by default.</p><ul><li><strong><code>socketio</code> removed.</strong> It defaulted to <code>true</code>, so apps that also built their own socket opened two live socket.io connections per page load.</li><li><strong><code>call</code> removed.</strong> It installed a <code>$call</code> global that nothing used.</li><li><strong><code>config</code> removed (silent).</strong> <code>setConfig</code> is the entry point. One app passed it.</li><li><strong><code>resources</code> no longer defaults to <code>true</code>.</strong> The v1 resources Options API mixin (<code>this.$resources</code>, <code>$getResource</code>, <code>$getDoc</code>, <code>$getListResource</code>, <code>$refetchResource</code>) installs only with <code>app.use(FrappeUI, { resources: true })</code>. Composition API resources are not affected. <code>resourcesPlugin</code> stays exported for direct installation.</li></ul><p>A removed <em>option</em> is ignored rather than rejected, so the plugin logs a warning in development that names any option it does not accept and what to use instead.</p><p><strong>Removed features fail loudly, in production too.</strong> A dropped option that disappears is an annoyance. A dropped feature that disappears causes a crash somewhere else that is hard to trace. So:</p><ul><li>A component that declares a <code>resources</code> option without <code>app.use(FrappeUI, { resources: true })</code> throws when it is created. The error names the component and the fix.</li><li>Reading <code>this.$resources</code> with the option off throws the same advice. Both checks exist because Vue passes what a lifecycle hook throws through its own error handling, which only logs in production. A read throws straight into app code in every build.</li><li>Reading <code>this.$socket</code> or <code>this.$call</code>, the two globals the plugin no longer installs, throws a message that names the replacement, instead of returning <code>undefined</code>. If you assign your own (<code>app.config.globalProperties.$socket = io(…)</code>), it replaces the check. This works before or after <code>app.use(FrappeUI)</code>.</li></ul><p><code>realtime: true</code> on a v1 resource still falls back quietly to a non-realtime resource when no socket is set. That was always its behaviour, and it does not change.</p><p><strong>What to do:</strong> if you use <code>this.$resources</code>, write <code>app.use(FrappeUI, { resources: true })</code>. Move <code>config</code> to <code>setConfig</code>.</p><h4 id="frappeui-plugin-—-resources-is-a-boolean-breaking" tabindex="-1"><code>FrappeUI</code> plugin — <code>resources</code> is a boolean (breaking) <a class="header-anchor" href="#frappeui-plugin-—-resources-is-a-boolean-breaking" aria-label="Permalink to “FrappeUI plugin — resources is a boolean (breaking)”">​</a></h4><p>The option was typed as an object of resource definitions, but the plugin never read what was in it, only whether it was set. It is <code>boolean</code> now.</p><p>Passing an object is a type error, and it still installs the mixin at runtime, so an app that misses the change keeps working.</p><p><strong>What to do:</strong> run <code>npx -p frappe-ui data-v1 ./src</code>. It rewrites the call. It reports any site it will not touch (a spread, a computed key, a variable) instead of guessing.</p><h4 id="data-fetching-v2-—-one-request-per-submit" tabindex="-1">Data fetching (v2) — one request per submit (breaking) <a class="header-anchor" href="#data-fetching-v2-—-one-request-per-submit" aria-label="Permalink to “Data fetching (v2) — one request per submit (breaking)”">​</a></h4><p><code>useDoctype</code>&#39;s <code>insert</code>, <code>delete</code>, <code>setValue</code>, <code>runDocMethod</code> and <code>runMethod</code>, and <code>useList</code>&#39;s <code>insert</code>, <code>setValue</code> and <code>delete</code>, each shared one request. Two submits at once cancelled each other, and every submit resolved from the same <code>data</code>, so a caller could get another caller&#39;s answer, or <code>null</code>. Each submit now sends its own request and resolves with its own response.</p><ul><li><strong>Breaking:</strong> these eight members no longer carry the <code>useCall</code> surface. Removed: <code>params</code>, <code>promise</code>, <code>url</code>, <code>reset</code>, <code>abort</code>, <code>execute</code>, <code>fetch</code>, <code>reload</code>, <code>isFetching</code>, <code>isFinished</code>, <code>canAbort</code>, <code>aborted</code>. Each of them described one shared request, which no longer exists.</li><li>What is left, on all eight: <code>submit()</code>, <code>data</code>, <code>error</code>, <code>loading</code> and <code>isLoading()</code>. <code>loading</code> is true while any submit is in flight.</li><li>New: <code>delete.isLoading(name)</code> and <code>setValue.isLoading(name)</code> on both <code>useDoctype</code> and <code>useList</code>. This replaces the <code>delete.loading &amp;&amp; delete.params.name === row.name</code> pattern, which showed the same spinner on every row once two deletes overlapped.</li><li>New: <code>insert.isLoading()</code> on both, with no argument. A new document has no name to key on, so it answers for the whole method, with the same value as <code>insert.loading</code>. It exists so all eight methods read the same way.</li><li><code>runDocMethod.isLoading(name, method)</code> and <code>runMethod.isLoading(method)</code> keep their signatures, and now answer correctly with several submits in flight. Before, they compared the shared URL, so only the newest submit read as loading.</li><li><code>data</code> and <code>error</code> belong to the submit that started last, not the one that answered last. A slow submit that comes back after a newer one is dropped: it writes no <code>data</code>, writes no <code>error</code> and clears nothing. It still answers its own caller with its own outcome: it resolves with its response, or rejects with its error.</li><li>The winning submit writes <code>data</code> and <code>error</code> together. Success sets <code>data</code> and clears <code>error</code>. Failure sets <code>error</code> and leaves <code>data</code> alone.</li><li><strong><code>data</code> is no longer reset on failure.</strong> The old shared <code>useCall</code> set <code>data</code> back to <code>null</code> whenever a response came back not-ok. It now keeps the last successful response. Read <code>error</code>, not <code>data</code>, to tell a failed submit from a successful one.</li><li><code>error</code> is no longer cleared when a submit starts. Clearing it there erased the error of another submit that was still in flight. An error stays until the newest submit settles.</li><li><strong><code>submit()</code> now rejects on any failure.</strong> It resolves with the response or rejects with the error: one channel, not two. A failed <code>validate</code> already rejected. A failed request used to resolve with <code>null</code>. Both reject now, and a server that answers with <code>null</code> resolves with <code>null</code>.</li><li><code>useList</code>&#39;s <code>insert</code> and <code>delete</code> now send to <code>baseUrl</code>, which they silently dropped before. <code>setValue</code> already used it, so all three write methods now agree. <code>useDoctype</code> was never affected: every one of its methods already passed <code>baseUrl</code>.</li></ul><p><strong>What to do:</strong> replace reads of the removed members. Use <code>delete.isLoading(name)</code> for per-row spinners. Check <code>error</code>, not <code>data</code>, after a submit. See the <a href="/docs/migration#data-fetching-usedoctype-uselist">migration guide</a>.</p><h4 id="data-fetching-v2-—-usefrappefetch-off-the-root-export-breaking" tabindex="-1">Data fetching (v2) — <code>useFrappeFetch</code> off the root export (breaking) <a class="header-anchor" href="#data-fetching-v2-—-usefrappefetch-off-the-root-export-breaking" aria-label="Permalink to “Data fetching (v2) — useFrappeFetch off the root export (breaking)”">​</a></h4><p><strong><code>useFrappeFetch</code> removed.</strong> It is the raw <code>createFetch</code> instance that <code>useCall</code>, <code>useDoc</code> and <code>useList</code> are built on. It handles headers, response parsing and error shaping, and nothing above that: no URL building, no params, no caching, no typed return. No app imports it. The build fails at the import.</p><p><strong>What to do:</strong> use <code>useCall</code> for a whitelisted method, <code>useDoc</code> for one document, and <code>useList</code> for a query.</p><p><strong><code>FrappeResponseError</code> is now exported.</strong> The composables raise it on a Frappe error response and put it on <code>.error</code>, and <code>submit()</code> rejects with it. But nothing exported the class, so you could not narrow the error. This is the same gap that exporting <code>FrappeResourceError</code> closed for <code>frappeRequest</code>.</p><h4 id="data-fetching-v2-—-docs-and-the-sidebar-splits-from-resources" tabindex="-1">Data fetching (v2) — docs, and the sidebar splits from Resources <a class="header-anchor" href="#data-fetching-v2-—-docs-and-the-sidebar-splits-from-resources" aria-label="Permalink to “Data fetching (v2) — docs, and the sidebar splits from Resources”">​</a></h4><p><code>useCall</code>, <code>useDoc</code>, <code>useList</code>, <code>useDoctype</code> and <code>useNewDoc</code> each get a docs page for the first time, under a new <strong>Data Fetching</strong> sidebar section: <code>useCall</code> for a whitelisted method, <code>useDoc</code> for one document, <code>useList</code> for a query, <code>useDoctype</code> for write-only access to a DocType, and <code>useNewDoc</code> for a draft-and-insert form.</p><p>The old <strong>Data Fetching</strong> section is renamed <strong>Resources</strong> and keeps its three pages (Resource, List Resource, Document Resource) unchanged. Both sections link to each other. Resources stays fully supported through <code>1.x</code>. The new composables are the recommended layer for new code.</p><h4 id="data-fetching-v2-—-usenewdoc-lost-reactivity-after-submit-fix" tabindex="-1">Data fetching (v2) — <code>useNewDoc</code> lost reactivity after submit (fix) <a class="header-anchor" href="#data-fetching-v2-—-usenewdoc-lost-reactivity-after-submit-fix" aria-label="Permalink to “Data fetching (v2) — useNewDoc lost reactivity after submit (fix)”">​</a></h4><p><code>useNewDoc</code> built its return value with <code>reactive({ ...out, submit, doc })</code>. Spreading a <code>reactive()</code> proxy reads every ref and computed once and freezes the result. So <code>data</code>, <code>error</code> and <code>loading</code> stopped updating as soon as the object was built: a template bound to <code>newDoc.loading</code> never saw it change. The return value is now built by changing the underlying object in place, so its properties stay live.</p><h4 id="data-fetching-v2-—-initialdata-did-nothing-on-usecall-and-uselist-fix" tabindex="-1">Data fetching (v2) — <code>initialData</code> did nothing on <code>useCall</code> and <code>useList</code> (fix) <a class="header-anchor" href="#data-fetching-v2-—-initialdata-did-nothing-on-usecall-and-uselist-fix" aria-label="Permalink to “Data fetching (v2) — initialData did nothing on useCall and useList (fix)”">​</a></h4><p>Both documented an <code>initialData</code> option, to show a placeholder before the first response. Neither worked.</p><ul><li><code>useCall</code> passed it straight to the underlying fetch, which expects the wrapped <code>{ data: ... }</code> shape the API actually returns. The unwrapped value was invisible, so <code>call.data</code> stayed <code>null</code> until the first response.</li><li><code>useList</code> reads <code>data</code> from a separate list built in <code>afterFetch</code>, which <code>initialData</code> never touched. So <code>list.data</code> stayed <code>null</code> the same way.</li></ul><p>Both now show the seeded value immediately, as documented.</p><h3 id="uploads-and-fileuploader" tabindex="-1">Uploads and FileUploader <a class="header-anchor" href="#uploads-and-fileuploader" aria-label="Permalink to “Uploads and FileUploader”">​</a></h3><h4 id="fileuploader-—-uploads-default-to-private-security-fix-breaking" tabindex="-1">FileUploader — uploads default to private (security fix, breaking) <a class="header-anchor" href="#fileuploader-—-uploads-default-to-private-security-fix-breaking" aria-label="Permalink to “FileUploader — uploads default to private (security fix, breaking)”">​</a></h4><ul><li><strong>Silent break:</strong> <code>isPrivateUpload()</code> is the one function that <code>useFileUpload</code> and <code>FileUploadHandler</code> use to decide privacy. It now makes an upload with no stated <code>private</code> / <code>is_private</code> <strong>private</strong>, not public. Before, <code>FileUploader</code> set this default itself, while <code>useFileUpload().upload(file, {})</code> and <code>new FileUploadHandler().upload(file, {})</code> made the same upload public. That mismatch was <a href="https://github.com/frappe/frappe-ui/issues/922" target="_blank" rel="noreferrer">#922</a>. <code>FileUploader</code> itself already uploaded private (since <code>v1.0.0-beta.21</code>) and is not affected. A caller of <code>useFileUpload</code> or <code>FileUploadHandler</code> that sets no privacy option now gets a private file, where it got a public one before.</li><li>Fixed: the standalone <code>upload(file, options)</code> export (exported from <code>frappe-ui</code> next to <code>useFileUpload</code>) crashed at runtime. It required internal <code>state</code>/<code>reset</code> arguments that the public signature gave no way to pass. It is now a real standalone function, and <code>useFileUpload()</code> wraps it with reactive state.</li></ul><p><strong>What to do:</strong> pass <code>private: false</code> where an upload must be public. See the <a href="/docs/migration#fileuploader">migration guide</a>. (The <code>is_private</code> option was removed later; see <a href="#uploads-—-uploaderror-and-the-is-private-option-removed-breaking">the next entry</a>.)</p><h4 id="uploads-—-uploaderror-and-the-is-private-option-removed-breaking" tabindex="-1">Uploads — <code>UploadError</code>, and the <code>is_private</code> option removed (breaking) <a class="header-anchor" href="#uploads-—-uploaderror-and-the-is-private-option-removed-breaking" aria-label="Permalink to “Uploads — UploadError, and the is_private option removed (breaking)”">​</a></h4><p><code>upload</code>, <code>useFileUpload</code> and <code>FileUploadHandler</code> reject with an exported <code>UploadError</code> instead of a plain <code>Error</code>. <code>useFileUpload</code>&#39;s <code>state.error</code> is typed with it.</p><ul><li><code>error.kind</code> is <code>&#39;file-size&#39; | &#39;network&#39; | &#39;server&#39; | &#39;abort&#39;</code>. Branch on it instead of matching the message text.</li><li>A server failure also carries <code>status</code>, the parsed <code>messages</code> and the raw <code>response</code>. <code>FileUploadHandler</code> now rejects on abort, instead of leaving the promise open. <code>upload</code> and <code>useFileUpload</code> already rejected.</li><li><strong>Breaking, silent in JS:</strong> an aborted <code>upload()</code> / <code>useFileUpload()</code> used to reject with <code>new DOMException(&#39;Upload cancelled&#39;, &#39;AbortError&#39;)</code>. It now rejects with an <code>UploadError</code> with <code>kind: &#39;abort&#39;</code>. <code>error.name</code> is <code>&#39;UploadError&#39;</code> and <code>error instanceof DOMException</code> is false. So a <code>catch</code> that tells a cancel from a failure by either check stops matching. <code>options.signal</code> is public. See the <a href="/docs/migration#upload-abort-error">migration guide</a>.</li><li>Existing <code>catch</code> blocks keep working: <code>UploadError</code> is an <code>Error</code>, and its <code>message</code> is unchanged.</li><li><strong>Breaking, silent in JS:</strong> the <code>is_private</code> upload option is removed. <code>private</code> is the only spelling. An <code>is_private</code> that is still passed is ignored, so <code>{ is_private: 0 }</code> now uploads private. The <code>is_private</code> field on the returned file record is unchanged.</li><li><code>isPrivateUpload</code> and the <code>UploadPrivacy</code> type are no longer exported.</li></ul><p><strong>What to do:</strong> check <code>error.kind === &#39;abort&#39;</code> to detect a cancel. Replace <code>is_private</code> with <code>private</code>.</p><h4 id="fileuploader-—-flat-props-replace-the-uploadargs-blob-breaking-p3" tabindex="-1">FileUploader — flat props replace the <code>uploadArgs</code> blob (breaking, silent) <a class="header-anchor" href="#fileuploader-—-flat-props-replace-the-uploadargs-blob-breaking-p3" aria-label="Permalink to “FileUploader — flat props replace the uploadArgs blob (breaking, silent)”">​</a></h4><p><strong>Silent break:</strong> <code>uploadArgs</code> is removed. The fields of it that apps actually use are now flat props: <code>private</code>, <code>folder</code>, <code>doctype</code>, <code>docname</code>, <code>fieldname</code>, <code>uploadEndpoint</code>, <code>optimize</code>. Old code keeps compiling, because <code>uploadArgs</code> becomes an unused attribute on the root element. So an app that relied on it (<code>folder</code>, <code>doctype</code>, custom <code>private</code>, …) silently stops applying those options.</p><p>The advanced options with no flat prop (<code>file_url</code>, <code>method</code>, <code>type</code>, <code>params</code>, <code>maxWidth</code>/<code>maxHeight</code>, upload cancellation) had zero measured use on the component. For those, use <code>useFileUpload()</code> directly.</p><p><strong>What to do:</strong> move each <code>uploadArgs</code> field to its flat prop. See the <a href="/docs/migration#fileuploader">migration guide</a>.</p><h4 id="fileuploader-—-success-failure-emits-declared-stable" tabindex="-1">FileUploader — <code>success</code> / <code>failure</code> emits declared stable <a class="header-anchor" href="#fileuploader-—-success-failure-emits-declared-stable" aria-label="Permalink to “FileUploader — success / failure emits declared stable”">​</a></h4><ul><li>Both emits lost their <code>@deprecated</code> tag (ADR-0008 does not allow <code>@deprecated</code> members in <code>1.0.0</code>) and gained real types: <code>success: [data: UploadedFile]</code> and <code>failure: [error: unknown]</code>. They were untyped <code>any</code> before. Apps depend on both, and they keep their names, because the names already describe the result rather than the user action.</li><li>Fixed: <code>failure</code> did not fire when <code>validateFile</code> rejected a file, only on an actual upload error. <code>error</code> was set on the slot props either way, but a <code>@failure</code> listener never heard about a rejected file. It now emits <code>failure</code> with the validation error (a string or an <code>Error</code>) in both cases, as the type already promised.</li></ul><h4 id="fileuploader-—-slot-prop-error-is-now-always-a-string" tabindex="-1">FileUploader — slot prop <code>error</code> is now always a string (breaking, silent) <a class="header-anchor" href="#fileuploader-—-slot-prop-error-is-now-always-a-string" aria-label="Permalink to “FileUploader — slot prop error is now always a string (breaking, silent)”">​</a></h4><p><strong>Silent break:</strong> <code>FileUploaderSlotProps.error</code> changed from <code>unknown</code> to <code>string | null</code>. Upload failures were already turned into a message string. Validation failures (<code>validateFile</code> returning an <code>Error</code>) were not, so a custom slot could get either a string or an <code>Error</code> object. Both paths now turn the error into a message string before it reaches the slot. A slot that did <span><code>{{ error.message }}</code></span> for the validation <code>Error</code> case (uncommon, but possible) now renders nothing, because <code>error</code> is always a string.</p><p><strong>What to do:</strong> render <span><code>{{ error }}</code></span> instead. See the <a href="/docs/migration#fileuploader">migration guide</a>.</p><h4 id="fileuploader-—-inputref-removed-nothing-in-its-place" tabindex="-1">FileUploader — <code>inputRef</code> removed, nothing in its place <a class="header-anchor" href="#fileuploader-—-inputref-removed-nothing-in-its-place" aria-label="Permalink to “FileUploader — inputRef removed, nothing in its place”">​</a></h4><p><strong>Breaking, loud:</strong> per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0012-template-ref-surface.md" target="_blank" rel="noreferrer">ADR-0012</a>, <code>FileUploader</code> exposes nothing on its template ref. <code>inputRef</code> was a function that looked like a ref (<code>uploader.value.inputRef().focus()</code>). The <code>openFileSelector</code> slot prop already covers what it was used for. There were zero known call sites.</p><p><strong>What to do:</strong> use the <code>openFileSelector</code> slot prop.</p><h4 id="fileuploader-—-structural-bar-typescript-types-ts-tests-docs" tabindex="-1">FileUploader — TypeScript types, tests, docs and styling hooks <a class="header-anchor" href="#fileuploader-—-structural-bar-typescript-types-ts-tests-docs" aria-label="Permalink to “FileUploader — TypeScript types, tests, docs and styling hooks”">​</a></h4><ul><li><code>FileUploader</code> is now fully typed (<code>FileUploaderProps</code>, <code>FileUploaderEmits</code>, <code>FileUploaderSlotProps</code> in <code>types.ts</code>).</li><li>It has <code>*.cy.ts</code> component tests that cover the five required behaviors: render, <code>v-model</code>, disabled and loading, keyboard and focus, and slots.</li><li>It has a <code>data-slot=&quot;root&quot;</code> / <code>data-state=&quot;idle&quot; | &quot;uploading&quot; | &quot;success&quot; | &quot;error&quot;</code> pair for CSS hooks, so you can style it without class props.</li><li>The default trigger now shows validation and upload errors through <code>&lt;ErrorMessage&gt;</code> (<code>role=&quot;alert&quot;</code>). Before, errors were invisible unless the caller used a custom slot.</li></ul><h4 id="filetobase64-and-the-filesize-helpers-—-unexported-from-root" tabindex="-1"><code>fileToBase64</code> and the <code>fileSize</code> helpers — unexported from root (breaking, loud) <a class="header-anchor" href="#filetobase64-and-the-filesize-helpers-—-unexported-from-root" aria-label="Permalink to “fileToBase64 and the fileSize helpers — unexported from root (breaking, loud)”">​</a></h4><p><strong>Breaking, loud:</strong> <code>fileToBase64</code>, <code>formatBytes</code>, <code>getMaxFileSize</code>, and <code>fileSizeLimitMessage</code> are no longer exported from <code>frappe-ui</code>. They had zero outside call sites at the v1 review. All four stay as internal helpers shared by <code>useFileUpload</code>, <code>FileUploadHandler</code>, and the editor&#39;s media upload engine.</p><h3 id="inputs-and-forms" tabindex="-1">Inputs and forms <a class="header-anchor" href="#inputs-and-forms" aria-label="Permalink to “Inputs and forms”">​</a></h3><h4 id="inputs-—-the-size-scale-is-xs-sm-md-lg-breaking-loud-in-ts-silent-in-js" tabindex="-1">Inputs — the size scale is <code>xs / sm / md / lg</code> (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#inputs-—-the-size-scale-is-xs-sm-md-lg-breaking-loud-in-ts-silent-in-js" aria-label="Permalink to “Inputs — the size scale is xs / sm / md / lg (breaking; loud in TS, silent in JS)”">​</a></h4><p>The input family gains <code>xs</code> and loses <code>xl</code>. The four sizes are fixed single-line heights: <strong>24 / 28 / 32 / 40px</strong>. <code>sm</code>, <code>md</code> and <code>lg</code> render as before. <code>xs</code> matches <code>Button</code>&#39;s own <code>xs</code>, so a 24px input and a 24px button line up.</p><p><code>xl</code> was never a bigger box. It drew <code>lg</code>&#39;s 40px height with an 18px font, so it was a font override with a size name.</p><p>Every component on the shared scale changes together: <code>TextInput</code>, <code>Textarea</code>, <code>Password</code>, <code>Rating</code>, <code>Select</code>, <code>Combobox</code>, <code>MultiSelect</code>, <code>ItemListRow</code>, <code>FormControl</code>, the <code>DatePicker</code> family, <code>TimePicker</code>, <code>Duration</code>, and the experimental <code>CodeEditor</code> and <code>MultiEmailInput</code>. <code>Progress</code>, <code>Slider</code>, <code>Switch</code>, <code>Checkbox</code>, <code>Radio</code>, <code>Avatar</code>, <code>Badge</code>, <code>Button</code> and <code>Dialog</code> keep their own scales, so <code>&lt;Dialog size=&quot;xl&quot;&gt;</code> and <code>&lt;Avatar size=&quot;xl&quot;&gt;</code> still work.</p><ul><li><code>FormControl.size</code> widens from <code>sm | md</code> to the whole scale. <code>type=&quot;checkbox&quot;</code> renders on the smaller toggle scale, so <code>lg</code> there becomes <code>md</code> rather than falling off the end.</li><li><strong>A leftover <code>size=&quot;xl&quot;</code> no longer loses its styling.</strong> Every input size lookup goes through <code>resolvePropValue</code>. An unsupported value falls back to the component&#39;s own default and warns once in development. Before, it shipped an element with no height, font, radius or padding class, and no warning. <code>Badge</code> got the same treatment for <code>theme</code>.</li><li><code>InputSize</code>, <code>InputVariant</code>, <code>ToggleSize</code> and <code>RangeSize</code> are now exported from the root, so a typed wrapper can name the scale that the API tables already print.</li></ul><p>TypeScript flags <code>xl</code> at the call site. JavaScript call sites and bound values (<code>:size=&quot;config.size&quot;</code>) only show up through the development warning.</p><p><strong>What to do:</strong> replace <code>size=&quot;xl&quot;</code> with <code>size=&quot;lg&quot;</code>. After upgrading, check the console for the warning.</p><h4 id="form-typography-—-13px-labels-descriptions-and-textarea-text-breaking-silent" tabindex="-1">Form typography — 13px labels, descriptions and Textarea text (breaking, silent) <a class="header-anchor" href="#form-typography-—-13px-labels-descriptions-and-textarea-text-breaking-silent" aria-label="Permalink to “Form typography — 13px labels, descriptions and Textarea text (breaking, silent)”">​</a></h4><p>Labels, descriptions and <code>Textarea</code> text are a fixed 13px. Labels and descriptions default to <code>ink-gray-6</code>. This is a rendering change: there is nothing to change in your source, except <code>FormLabel.size</code> below.</p><table tabindex="0"><thead><tr><th>Member</th><th>Before</th><th>After</th></tr></thead><tbody><tr><td>Label (<code>InputLabel</code>)</td><td>14px, <code>ink-gray-5</code></td><td>13px, <code>ink-gray-6</code></td></tr><tr><td>Label (<code>FormLabel</code>)</td><td>12px <code>sm</code> / 14px <code>md</code>, <code>ink-gray-5</code></td><td>13px, <code>ink-gray-6</code></td></tr><tr><td>Description</td><td>13px, <code>ink-gray-5</code></td><td>13px, <code>ink-gray-6</code></td></tr><tr><td>Description, disabled</td><td><code>ink-gray-3</code></td><td><code>ink-gray-4</code></td></tr><tr><td><code>Textarea</code> text, every size</td><td>14 / 16 / 18 / 20px</td><td>13px</td></tr></tbody></table><p>The two label components used to disagree. <code>InputLabel</code>, which <code>FormControl</code> renders through, was a flat 14px. <code>FormLabel</code> was 12px or 14px depending on <code>size</code>. Both are 13px now, so a <code>FormLabel</code> and a <code>TextInput</code> label match.</p><p><code>ink-gray-5</code> measured 4.18:1 against the page in both themes, which is below WCAG AA for body text. <code>ink-gray-6</code> measures 7.80:1 in light and 6.29:1 in dark. The disabled description moved off <code>ink-gray-3</code> (1.69:1) to match the disabled label.</p><ul><li><strong><code>FormLabel.size</code> is removed</strong>, not kept as a prop that does nothing: with the size fixed, the prop had nothing to choose. <code>size</code> falls through as a plain HTML attribute, so nothing throws, and the label renders at 13px. TypeScript call sites get a build error. <code>size=&quot;md&quot;</code> was 14px and the <code>sm</code> default was 12px. Both become 13px.</li><li><code>Textarea.size</code> still changes padding, corner radius and minimum height. It no longer changes the text size. The single-line input heights do not set a <code>Textarea</code> height, so a <code>lg</code> <code>Textarea</code> is a roomier box with the same 13px text. The colour of the <code>Textarea</code> <em>value</em> is unchanged.</li></ul><p><strong>What to do:</strong> delete <code>size</code> from <code>FormLabel</code>.</p><h4 id="errormessage-—-several-messages" tabindex="-1">ErrorMessage — several messages (additive) <a class="header-anchor" href="#errormessage-—-several-messages" aria-label="Permalink to “ErrorMessage — several messages (additive)”">​</a></h4><p><code>message</code> accepts <code>string[]</code>. An <code>Error</code> that carries a <code>messages</code> array (what Frappe&#39;s whitelisted methods return) renders one line per message, instead of <code>[object Object]</code>. A single string and a plain <code>Error</code> render as before. The prop type is exported as <code>ErrorMessageValue</code>.</p><p>Every input takes the same value on its <code>error</code> prop, so <code>&lt;TextInput :error=&quot;[&#39;Email is required&#39;, &#39;Password too short&#39;]&quot; /&gt;</code> renders both lines and sets <code>aria-invalid</code>. An empty array means no error. One function decides this for the whole library, so the error text and the input state always agree.</p><h4 id="inputs-—-focus-on-every-control-open-close-on-the-pickers-additive" tabindex="-1">Inputs — <code>focus()</code> on every control, <code>open()</code>/<code>close()</code> on the pickers (additive) <a class="header-anchor" href="#inputs-—-focus-on-every-control-open-close-on-the-pickers-additive" aria-label="Permalink to “Inputs — focus() on every control, open()/close() on the pickers (additive)”">​</a></h4><p>Every input exposes <code>focus()</code> on a template ref, typed as the new exported <code>InputExposed</code>. A generic form that holds a ref to a control it did not choose now has one action it can always call. <code>FormControl</code> forwards <code>focus()</code> to the control its <code>type</code> resolved to.</p><p>Where focus lands is part of the contract. It is the element that <code>Tab</code> reaches: <code>Slider</code> focuses the thumb. <code>RadioGroup</code> focuses the selected option (the first enabled one when nothing is selected). <code>Rating</code> focuses the selected star (the first star when the value is empty), except in half-star mode, where the whole control is one slider.</p><p>The three date pickers and <code>TimePicker</code> add <code>open()</code> and <code>close()</code> next to <code>focus()</code>, typed as the new exported <code>PickerExposed</code>. They render their own trigger, so a parent&#39;s script has no other handle on the panel. <code>open()</code> does nothing while the picker is disabled. <code>clear()</code> stays on <code>Select</code>, <code>Combobox</code> and <code>MultiSelect</code> and is not added anywhere else (ADR-0012).</p><p><code>Duration</code> now forwards the <code>#label</code> and <code>#description</code> slots to its <code>TextInput</code>. It used to drop them.</p><h4 id="inputs-—-attributes-route-to-the-control-class-and-style-to-the-wrapper-breaking-silent" tabindex="-1">Inputs — attributes route to the control, <code>class</code> and <code>style</code> to the wrapper (breaking, silent) <a class="header-anchor" href="#inputs-—-attributes-route-to-the-control-class-and-style-to-the-wrapper-breaking-silent" aria-label="Permalink to “Inputs — attributes route to the control, class and style to the wrapper (breaking, silent)”">​</a></h4><p>An input has a layout wrapper and an interactive element. An attribute you pass that the component does not declare has to land on one of them. The rule is now the same everywhere: <code>class</code> and <code>style</code> go to the wrapper. <code>name</code>, <code>aria-*</code>, <code>data-*</code> and listeners go <strong>once</strong> to the interactive element.</p><p><code>Checkbox</code> applied the whole set twice, so <code>aria-label</code> also named a <code>&lt;div&gt;</code>, and a caller&#39;s <code>@click</code> ran twice. <code>Switch</code>, <code>RadioGroup</code> and <code>Rating</code> sent everything to the wrapper, so <code>aria-label</code> never reached the control at all.</p><p>This is silent. Check your code if you relied on the old placement:</p><div class="language-vue"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">&lt;!-- the listener used to fire on the padded row too; now only on the input --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">Checkbox</span><span class="s_1i4ay4"> padded</span><span class="s_1i4ay4"> label</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">Agree</span><span class="s_w1p9wo">&quot;</span><span class="s_1jjt6x"> @</span><span class="s_1i4ay4">click</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_22m8k2">onClick</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2"> /&gt;</span></span></code></pre></div><p><code>TextInput</code>, <code>Textarea</code>, <code>Password</code>, <code>Select</code>, <code>Combobox</code>, <code>MultiSelect</code>, <code>Slider</code>, <code>FormControl</code>, <code>Duration</code> and the date pickers already followed the rule and are unchanged.</p><h4 id="rating-—-size-defaults-to-sm-breaking-silent" tabindex="-1"><code>Rating</code> — <code>size</code> defaults to <code>sm</code> (breaking, silent) <a class="header-anchor" href="#rating-—-size-defaults-to-sm-breaking-silent" aria-label="Permalink to “Rating — size defaults to sm (breaking, silent)”">​</a></h4><p>Every other input defaults to <code>sm</code>, but <code>Rating</code> defaulted to <code>md</code>. A <code>&lt;Rating&gt;</code> with no <code>size</code> now renders smaller. An unrecognized size resolves to <code>sm</code> too, so the default and the fallback agree.</p><p><strong>What to do:</strong> pass <code>size=&quot;md&quot;</code> to keep the old size.</p><h4 id="input-types-—-selectionoption-selectiongroup-dayjs-daterangevalue-additive-with-one-removal" tabindex="-1">Input types — <code>SelectionOption</code>, <code>SelectionGroup</code>, <code>Dayjs</code>, <code>DateRangeValue</code> (additive, with one removal) <a class="header-anchor" href="#input-types-—-selectionoption-selectiongroup-dayjs-daterangevalue-additive-with-one-removal" aria-label="Permalink to “Input types — SelectionOption, SelectionGroup, Dayjs, DateRangeValue (additive, with one removal)”">​</a></h4><ul><li><code>SelectionOption</code> and <code>SelectionGroup</code> are exported from the root, so a wrapper around any of the three selection components can name its option shape once. The component-specific types stay.</li><li><code>Dayjs</code> is exported. The date pickers pass one to <code>formatter</code>, <code>disabledDate</code> and the setter slot props, so you need to be able to name the type.</li><li><code>DateRangeValue</code> types both sides of <code>DateRangePicker</code>&#39;s <code>v-model</code>. The prop was <code>string[]</code>, which let a one-element array in and made a round-trip through the model fail to type-check.</li><li><strong>Removal:</strong> <code>DatePicker</code>&#39;s entry file lists its public types, instead of re-exporting the whole module. <code>DatePickerViewMode</code> and <code>DatePickerDateObj</code> were calendar internals that the wildcard published. They leave the root.</li><li><code>SelectEmits</code>, <code>ComboboxEmits</code> and <code>MultiSelectEmits</code> no longer declare the model events a second time (<code>defineModel</code> already declares them). See <a href="#selection-emit-interfaces-drop-the-model-events-breaking-in-ts-only">Selection emit interfaces</a>. <code>RadioGroupEmits</code> says <code>RadioValue | undefined</code>, which is what an unbound group starts at.</li></ul><h4 id="inputs-—-control-versus-trigger-and-picker-aria-additive" tabindex="-1">Inputs — <code>control</code> versus <code>trigger</code>, and picker ARIA (additive) <a class="header-anchor" href="#inputs-—-control-versus-trigger-and-picker-aria-additive" aria-label="Permalink to “Inputs — control versus trigger, and picker ARIA (additive)”">​</a></h4><p><code>data-slot=&quot;trigger&quot;</code> belongs to the selection family only: <code>Select</code>, <code>Combobox</code> and <code>MultiSelect</code> render a box that shows the selection and opens the popover. Every other input, the date and time pickers included, marks its main interactive element <code>data-slot=&quot;control&quot;</code>. You type into a picker&#39;s <code>&lt;input&gt;</code>, so it is a control that also opens a panel.</p><ul><li><code>FormLabel</code> carries <code>data-slot=&quot;label&quot;</code>, the hook <code>InputLabel</code> already rendered. One selector now reaches every label in the library.</li><li>The picker chevron carries <code>data-slot=&quot;chevron&quot;</code>.</li><li>The picker <code>&lt;input&gt;</code> carries <code>role=&quot;combobox&quot;</code>, <code>aria-haspopup</code> (<code>dialog</code> on the date pickers, <code>listbox</code> on <code>TimePicker</code>) and <code>aria-expanded</code>. While the panel is open, it also carries <code>aria-controls</code> pointing at the panel element. <code>TimePicker</code> puts <code>aria-activedescendant</code> on the input, where the ARIA combobox pattern expects it, instead of on the listbox.</li></ul><h4 id="formcontrol-—-variant-is-not-forwarded-to-a-checkbox-breaking-silent" tabindex="-1"><code>FormControl</code> — <code>variant</code> is not forwarded to a checkbox (breaking, silent) <a class="header-anchor" href="#formcontrol-—-variant-is-not-forwarded-to-a-checkbox-breaking-silent" aria-label="Permalink to “FormControl — variant is not forwarded to a checkbox (breaking, silent)”">​</a></h4><p>A checkbox draws no container surface, so it has no <code>variant</code>. <code>FormControl</code> forwarded one anyway, and it landed on the <code>&lt;input&gt;</code> as a stray <code>variant=&quot;subtle&quot;</code> attribute. <code>type=&quot;checkbox&quot;</code> no longer receives it.</p><p>The <code>type</code> routes are unchanged: <code>date</code> renders <code>DatePicker</code>, <code>time</code> renders <code>TimePicker</code>, and a native date field is <code>&lt;TextInput type=&quot;date&quot; /&gt;</code>.</p><h4 id="toggles-and-ranged-inputs-—-deprecated-members-removed" tabindex="-1">Toggles and ranged inputs — deprecated members removed (breaking, silent) <a class="header-anchor" href="#toggles-and-ranged-inputs-—-deprecated-members-removed" aria-label="Permalink to “Toggles and ranged inputs — deprecated members removed (breaking, silent)”">​</a></h4><p>Per ADR-0008, this family&#39;s deprecated aliases are <strong>removed</strong>, not kept. All five had zero call sites across the consumer apps. These are silent breaks: old code compiles and runs, but the old name is ignored. See the migration guide&#39;s <a href="/docs/migration#inputs">Inputs table</a>.</p><table tabindex="0"><thead><tr><th>Removed</th><th>Use</th></tr></thead><tbody><tr><td><code>Rating.rating_from</code></td><td><code>max</code></td></tr><tr><td><code>Rating.readonly</code></td><td><code>disabled</code></td></tr><tr><td><code>Switch.change</code> emit</td><td><code>v-model</code> / <code>@update:modelValue</code></td></tr><tr><td><code>Switch.labelClasses</code></td><td><code>data-*</code> styling hooks</td></tr><tr><td><code>Checkbox.padding</code></td><td><code>padded</code></td></tr></tbody></table><p><code>Slider</code> now exports its types (<code>SliderProps</code>, <code>SliderEmits</code>, <code>SliderValue</code>), and <code>Rating</code> exports <code>RatingEmits</code>.</p><h4 id="input-family-—-shared-labeling-contract" tabindex="-1">Input family — shared labeling contract <a class="header-anchor" href="#input-family-—-shared-labeling-contract" aria-label="Permalink to “Input family — shared labeling contract”">​</a></h4><p><code>TextInput</code>, <code>Textarea</code>, <code>Password</code>, <code>Checkbox</code>, <code>Switch</code>, <code>Rating</code>, and <code>Slider</code> accept <code>label</code>, <code>description</code>, <code>error</code> and <code>required</code>. The id is generated for you. <code>&lt;label for&gt;</code>, <code>aria-describedby</code>, <code>aria-errormessage</code>, <code>aria-invalid</code> and <code>aria-required</code> are wired automatically. <code>error</code> accepts a <code>string</code> or an <code>Error</code> (with <code>Error.messages</code> rendered as stacked plain text). Existing call sites do not change.</p><h4 id="input-family-—-data-styling-hooks" tabindex="-1">Input family — <code>data-*</code> styling hooks <a class="header-anchor" href="#input-family-—-data-styling-hooks" aria-label="Permalink to “Input family — data-* styling hooks”">​</a></h4><p>Every input renders the same <code>data-*</code> attributes, so your CSS can target inputs without props that inject classes:</p><ul><li><code>data-slot</code> (<code>&quot;label&quot;</code>, <code>&quot;control&quot;</code>, <code>&quot;description&quot;</code>, <code>&quot;error&quot;</code>)</li><li><code>data-size</code>, <code>data-variant</code> (where the input has them)</li><li><code>data-state</code> (<code>&quot;valid&quot; | &quot;invalid&quot; | &quot;checked&quot; | &quot;unchecked&quot; | …</code>)</li><li><code>data-disabled</code>, <code>data-required</code></li></ul><h4 id="password-—-v-model-fix" tabindex="-1">Password — <code>v-model</code> fix <a class="header-anchor" href="#password-—-v-model-fix" aria-label="Permalink to “Password — v-model fix”">​</a></h4><p><code>Password</code> now uses <code>defineModel&lt;string&gt;()</code>. This fixes a bug where <code>&lt;Password v-model&gt;</code> did not update when the user typed. Explicit <code>size</code>, <code>variant</code>, <code>disabled</code>, <code>placeholder</code>, <code>id</code> and <code>required</code> props replace <code>$attrs</code> routing.</p><h4 id="password-—-value-prop-removed-breaking" tabindex="-1">Password — <code>value</code> prop removed (breaking) <a class="header-anchor" href="#password-—-value-prop-removed-breaking" aria-label="Permalink to “Password — value prop removed (breaking)”">​</a></h4><p>Per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>, no deprecated member ships in <code>1.0.0</code>. Since <code>value</code> was deprecated earlier in this cycle, it warned and seeded <code>v-model</code>. A survey of every downstream app found zero call sites still passing it.</p><p><strong>What to do:</strong> use <code>v-model</code> / <code>modelValue</code>.</p><h4 id="textinput-textarea-password-duration-—-focus-and-inputelement-on-the-ref" tabindex="-1">TextInput / Textarea / Password / Duration — <code>focus()</code> and <code>inputElement</code> on the ref (breaking) <a class="header-anchor" href="#textinput-textarea-password-duration-—-focus-and-inputelement-on-the-ref" aria-label="Permalink to “TextInput / Textarea / Password / Duration — focus() and inputElement on the ref (breaking)”">​</a></h4><p>This implements <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0012-template-ref-surface.md" target="_blank" rel="noreferrer">ADR-0012</a>.</p><ul><li><strong>Breaking:</strong> <code>TextInput.el</code> and <code>Textarea.el</code> are renamed to <code>inputElement</code>. It is a computed, typed <code>HTMLInputElement | null</code> / <code>HTMLTextAreaElement | null</code>, never a raw ref.</li><li>All three, and <code>Duration</code>, now expose <code>focus(options?: FocusOptions)</code>. <code>Password</code> exposed nothing before.</li><li><code>TextInput</code>, <code>Textarea</code> and <code>Password</code> share one exported type, <code>TextInputExposed</code>, from <code>TextInput</code>&#39;s <code>types.ts</code>.</li><li><code>DurationExposed.focus</code> gained the same <code>options?</code> parameter. Its set of members is unchanged.</li></ul><p><strong>What to do:</strong> rename <code>.el</code> to <code>.inputElement</code>.</p><h4 id="rating-—-max-replaces-rating-from" tabindex="-1">Rating — <code>max</code> replaces <code>rating_from</code> <a class="header-anchor" href="#rating-—-max-replaces-rating-from" aria-label="Permalink to “Rating — max replaces rating_from”">​</a></h4><p>The default is <code>5</code>. The old name was kept as a deprecated alias during the betas and is now removed; see <a href="#toggles-and-ranged-inputs-—-deprecated-members-removed">Toggles and ranged inputs</a>. <code>Rating</code> no longer imports <code>FeatherIcon</code>. The default star comes from <code>lucide-star</code> through the shared Tailwind plugin. Filled stars now render visibly for values above zero.</p><h4 id="slider-—-additive-props-and-a11y-fix" tabindex="-1">Slider — additive props and a11y fix <a class="header-anchor" href="#slider-—-additive-props-and-a11y-fix" aria-label="Permalink to “Slider — additive props and a11y fix”">​</a></h4><ul><li><code>disabled</code> prop added.</li><li><code>size: &#39;sm&#39; | &#39;md&#39;</code> added. <code>&#39;md&#39;</code> scales the track and thumb in proportion.</li><li>New <code>value-commit</code> emit. It fires when dragging ends. Use it for side effects you do not want on every step.</li><li>Removed the hardcoded <code>aria-label=&quot;Volume&quot;</code>. The label now comes from the shared labeling contract, so pass <code>label</code> explicitly. This counts as a bug fix: assistive tech announced every Slider that was not a volume control as &quot;Volume&quot;.</li><li>The track is visible in collapsed wrappers, and the slider is full-width by default.</li><li>An uncontrolled <code>Slider</code> starts at <code>min</code>, instead of rendering with no thumb.</li></ul><h4 id="switch-—-lucide-icons-deprecations" tabindex="-1">Switch — Lucide icons; deprecations <a class="header-anchor" href="#switch-—-lucide-icons-deprecations" aria-label="Permalink to “Switch — Lucide icons; deprecations”">​</a></h4><p><code>Switch</code> no longer imports <code>FeatherIcon</code>. <code>icon</code> is now <code>string | Component</code>, and <code>lucide-*</code> strings go through the shared Tailwind plugin. <code>labelClasses</code> and the <code>change</code> emit were deprecated during the betas and are now removed; see <a href="#toggles-and-ranged-inputs-—-deprecated-members-removed">Toggles and ranged inputs</a>. The row&#39;s hover and active background is removed.</p><h4 id="checkbox-—-padding-deprecated" tabindex="-1">Checkbox — <code>padding</code> deprecated <a class="header-anchor" href="#checkbox-—-padding-deprecated" aria-label="Permalink to “Checkbox — padding deprecated”">​</a></h4><p><code>padding</code> was deprecated in favor of <code>padded</code>. It is now removed; see <a href="#toggles-and-ranged-inputs-—-deprecated-members-removed">Toggles and ranged inputs</a>.</p><h4 id="textarea-—-ghost-variant-required-prop" tabindex="-1">Textarea — <code>ghost</code> variant; <code>required</code> prop <a class="header-anchor" href="#textarea-—-ghost-variant-required-prop" aria-label="Permalink to “Textarea — ghost variant; required prop”">​</a></h4><p><code>Textarea</code> now accepts the <code>&#39;ghost&#39;</code> variant (like <code>TextInput</code> and <code>Password</code>) and the shared <code>required</code> prop.</p><h4 id="textinput-textarea-—-ghost-variant-paints-transparent-fix" tabindex="-1">TextInput / Textarea — <code>ghost</code> variant paints transparent (fix) <a class="header-anchor" href="#textinput-textarea-—-ghost-variant-paints-transparent-fix" aria-label="Permalink to “TextInput / Textarea — ghost variant paints transparent (fix)”">​</a></h4><p><code>ghost</code> set no <code>bg-*</code> class, so the <code>@tailwindcss/forms</code> preflight painted the input <code>#fff</code>, which showed as a white pill in dark mode. <code>ghost</code> now sets <code>bg-transparent</code>, like Combobox&#39;s own ghost search input. Closes #851.</p><h4 id="input-—-removed-breaking" tabindex="-1">Input — removed (breaking) <a class="header-anchor" href="#input-—-removed-breaking" aria-label="Permalink to “Input — removed (breaking)”">​</a></h4><p><strong>Breaking:</strong> <code>Input</code> and its <code>Input.cy.ts</code> tests are deleted. Per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>, no deprecated member ships in <code>1.0.0</code>. A survey of downstream apps found no live call sites that render <code>&lt;Input&gt;</code>. Five registrations were global component registrations that never rendered it.</p><p><strong>What to do:</strong> use <code>TextInput</code> for text-like modes, or <code>Textarea</code> / <code>Select</code> / <code>Checkbox</code> for the other <code>type</code> modes that <code>Input</code> accepted.</p><h4 id="formlabel-—-moved-to-a-component-directory-non-breaking" tabindex="-1">FormLabel — moved to a component directory (non-breaking) <a class="header-anchor" href="#formlabel-—-moved-to-a-component-directory-non-breaking" aria-label="Permalink to “FormLabel — moved to a component directory (non-breaking)”">​</a></h4><p><code>FormLabel</code> now lives at <code>src/components/FormLabel/FormLabel.vue</code> instead of a bare <code>src/components/FormLabel.vue</code>, like the rest of the input family. It gains <code>types.ts</code>, tests, stories and a docs page. The import path (<code>import { FormLabel } from &#39;frappe-ui&#39;</code>) is unchanged.</p><h4 id="inputlabel-—-slot-polish" tabindex="-1">InputLabel — slot polish <a class="header-anchor" href="#inputlabel-—-slot-polish" aria-label="Permalink to “InputLabel — slot polish”">​</a></h4><p>The default required indicator is not rendered when you use <code>#label</code> (the slot receives <code>{ required }</code>). The labeling wrapper is not rendered at all when there is nothing to label.</p><h4 id="rating-—-stars-line-up-with-text-beside-a-labeled-rating-fix" tabindex="-1"><code>Rating</code> — stars line up with text beside a labeled rating (fix) <a class="header-anchor" href="#rating-—-stars-line-up-with-text-beside-a-labeled-rating-fix" aria-label="Permalink to “Rating — stars line up with text beside a labeled rating (fix)”">​</a></h4><p>With a <code>label</code>, the stars sat inside a line of text, which added a few pixels under them. Text placed next to the rating sat lower than the stars. The stars are now their own row, so they line up with the text.</p><p><strong>What to do:</strong> nothing. If you added a class to work around the gap, you can remove it.</p><h3 id="date-and-time-pickers" tabindex="-1">Date and time pickers <a class="header-anchor" href="#date-and-time-pickers" aria-label="Permalink to “Date and time pickers”">​</a></h3><h4 id="pickers-—-open-is-honored-at-mount-breaking-silent" tabindex="-1">Pickers — <code>open</code> is honored at mount (breaking, silent) <a class="header-anchor" href="#pickers-—-open-is-honored-at-mount-breaking-silent" aria-label="Permalink to “Pickers — open is honored at mount (breaking, silent)”">​</a></h4><p><code>DatePicker</code>, <code>DateRangePicker</code>, <code>DateTimePicker</code> and <code>TimePicker</code> set their own open state to <code>false</code> at the start, and only watched <code>open</code> for later changes. So a parent that mounted one with <code>open</code> already <code>true</code> got a closed panel.</p><p>The initial value is now read on the first render, and the panel opens fully set up: the calendar shows the bound date, <code>TimePicker</code> scrolls to the bound time, and the trigger&#39;s <code>aria-controls</code> points at the panel. Mounting open emits no <code>update:open</code> and moves no focus: the panel is on the page from the first render, so focus stays wherever you put it. Opening the panel later still moves focus into it when the trigger is a custom <code>#trigger</code>.</p><ul><li><strong>Behavior change:</strong> if you passed <code>open</code> as a constant <code>true</code> and relied on it being ignored, the panel now opens.</li><li><code>open: true</code> together with <code>disabled: true</code> shows the panel. This matches the controlled path, which already skipped the disabled check. The imperative <code>open()</code> still does nothing while disabled.</li></ul><p><strong>What to do:</strong> if you passed a constant <code>open</code>, drop the prop or bind it to your own state.</p><h4 id="datepicker-internals-are-no-longer-exported-breaking-loud" tabindex="-1">DatePicker internals are no longer exported (breaking, loud) <a class="header-anchor" href="#datepicker-internals-are-no-longer-exported-breaking-loud" aria-label="Permalink to “DatePicker internals are no longer exported (breaking, loud)”">​</a></h4><p><code>src/components/DatePicker/index.ts</code> re-exported its whole <code>utils</code> module, so <code>months</code>, <code>monthStart</code>, <code>generateWeeks</code> and <code>getDateValue</code> reached the package root. The library does not allow <code>export *</code> from an implementation module: any helper a later commit added to <code>utils.ts</code> would have joined the public API without review, and been frozen.</p><p><strong>Breaking, loud:</strong> <code>import { months, getDateValue } from &#39;frappe-ui&#39;</code> fails to resolve. Nothing replaces them. <code>months</code> was a hardcoded English-only <code>&#39;Jan&#39;..&#39;Dec&#39;</code> array with no way to translate it, so freezing it would have been worse.</p><p><strong>What to do:</strong> copy what you need into your app, or use <code>dayjs</code> directly.</p><p>The <code>DatePicker</code>, <code>DateTimePicker</code> and <code>DateRangePicker</code> components and their types are unchanged.</p><h4 id="datepicker-family-—-trigger-slot-props-renamed-to-open-toggle-breaking-silent" tabindex="-1">DatePicker family — trigger slot props renamed to <code>open</code> / <code>toggle</code> (breaking, silent) <a class="header-anchor" href="#datepicker-family-—-trigger-slot-props-renamed-to-open-toggle-breaking-silent" aria-label="Permalink to “DatePicker family — trigger slot props renamed to open / toggle (breaking, silent)”">​</a></h4><p><code>#trigger</code>, <code>#prefix</code> and <code>#suffix</code> on <code>DatePicker</code>, <code>DateRangePicker</code>, <code>DateTimePicker</code> and <code>TimePicker</code> now receive <code>{ open, toggle }</code> instead of <code>{ isOpen, togglePopover }</code> (#1054).</p><p><code>Popover</code>, <code>HoverCard</code>, <code>Dropdown</code>, <code>Select</code>, <code>Combobox</code> and <code>MultiSelect</code> all name the boolean <code>open</code>, and <code>Popover</code>&#39;s trigger slot already names the flip <code>toggle</code>. <code>isOpen</code> is on <code>CONTEXT.md</code>&#39;s list of names to avoid in a public API, and <code>togglePopover</code> named the mechanism rather than the behavior. These components also already gave <code>#actions</code> a <code>close()</code>, so one component used two sets of names for one concept. <code>displayLabel</code> and <code>inputValue</code> are unchanged.</p><p><strong>Silent break:</strong> a destructured <code>isOpen</code> becomes <code>undefined</code>, so a class bound to it stops applying, with no error. <code>togglePopover()</code> throws only when you call it.</p><p><strong>What to do:</strong> search for both names and rename them. See the <a href="/docs/migration#trigger-slot-props">migration guide</a>.</p><h4 id="timepicker-—-four-emits-removed-breaking-loud-in-ts-silent-in-js" tabindex="-1"><code>TimePicker</code> — four emits removed (breaking, loud in TS, silent in JS) <a class="header-anchor" href="#timepicker-—-four-emits-removed-breaking-loud-in-ts-silent-in-js" aria-label="Permalink to “TimePicker — four emits removed (breaking, loud in TS, silent in JS)”">​</a></h4><p><code>update:open</code> carries both the open and the close, with the state in the payload, so <code>open</code> and <code>close</code> are gone. <code>input-invalid</code> and <code>invalid-change</code> are gone too: typed text that does not parse goes back to the last valid value, which the user sees.</p><div class="language-vue"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">&lt;!-- Before --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">TimePicker</span><span class="s_1jjt6x"> @</span><span class="s_1i4ay4">open</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_22m8k2">onOpen</span><span class="s_w1p9wo">&quot;</span><span class="s_1jjt6x"> @</span><span class="s_1i4ay4">close</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_22m8k2">onClose</span><span class="s_w1p9wo">&quot;</span><span class="s_1jjt6x"> @</span><span class="s_1i4ay4">invalid-change</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_22m8k2">setInvalid</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2"> /&gt;</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">&lt;!-- After --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">TimePicker</span><span class="s_1jjt6x"> @</span><span class="s_1i4ay4">update</span><span class="s_1jjt6x">:</span><span class="s_1i4ay4">open</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_13ahmt">(</span><span class="s_fsg3al">open</span><span class="s_13ahmt">)</span><span class="s_50ecpt"> =&gt;</span><span class="s_13ahmt"> (</span><span class="s_22m8k2">open</span><span class="s_50ecpt"> ?</span><span class="s_indoxt"> onOpen</span><span class="s_13ahmt">()</span><span class="s_50ecpt"> :</span><span class="s_indoxt"> onClose</span><span class="s_13ahmt">())</span><span class="s_w1p9wo">&quot;</span><span class="s_6am9cx"> /&gt;</span></span></code></pre></div><p><code>TimePickerEmits</code> is exported. <code>Variant</code> is an alias of the shared <code>InputVariant</code>, not a second scale.</p><h4 id="datepicker-family-—-v1-spec" tabindex="-1">DatePicker family — v1 spec <a class="header-anchor" href="#datepicker-family-—-v1-spec" aria-label="Permalink to “DatePicker family — v1 spec”">​</a></h4><p><code>DatePicker</code>, <code>DateRangePicker</code> and <code>DateTimePicker</code> use the same popover-trigger names as <code>Combobox</code> / <code>Dropdown</code> / <code>Select</code>.</p><ul><li><code>side</code> (default <code>&#39;bottom&#39;</code>) + <code>align</code> (default <code>&#39;start&#39;</code>) + <code>offset</code> (default <code>4</code>) replace <code>placement</code> (removed).</li><li><code>keepOpen</code> (default <code>false</code>) replaces <code>autoClose</code> (removed; it is the inverse).</li><li><code>typeable</code> (default <code>true</code>) replaces picker-level <code>readonly</code> and <code>allowCustom</code> (both removed). <code>:typeable=&quot;false&quot;</code> blocks typing and keeps the popover interactive.</li><li>Constraints: <code>min?: string</code> and <code>max?: string</code> (<code>YYYY-MM-DD</code>, plus <code>YYYY-MM-DD HH:mm:ss</code> on <code>DateTimePicker</code>), and <code>isDateUnavailable?: (date: Dayjs) =&gt; boolean</code> to disable any date. Min/max and the function work together. On <code>DateTimePicker</code>, <code>minDateTime</code>/<code>maxDateTime</code> are removed in favor of <code>min</code>/<code>max</code>.</li><li><code>v-model:open</code> works on all three pickers, through <code>open</code> + <code>update:open</code>.</li><li><code>openOnFocus</code> (default <code>false</code>) and <code>openOnClick</code> (default <code>true</code>) let you turn off either way of opening. <code>Combobox</code> got the same defaults.</li><li><code>#trigger</code> is the slot for a custom trigger. <code>#target</code> is removed.</li><li><code>DateRangePicker.clearable</code> now defaults to <code>true</code>. The footer hides when there is nothing to clear. (The footer was removed later; see <a href="#datepicker-family-—-footer-removed-new-actions-sidebar-slot">footer removed</a>.) The same change added a live hover preview while picking the end date, and a stable trigger width derived from <code>format</code>.</li><li>Public type exports added: <code>DateTimePickerProps</code>, <code>DateRangePickerEmits</code>, <code>DateTimePickerEmits</code>, <code>DateRangeValue</code>.</li></ul><h4 id="datepicker-family-—-daterangepicker-emit-shape-breaking" tabindex="-1">DatePicker family — <code>DateRangePicker</code> emit shape (breaking) <a class="header-anchor" href="#datepicker-family-—-daterangepicker-emit-shape-breaking" aria-label="Permalink to “DatePicker family — DateRangePicker emit shape (breaking)”">​</a></h4><p><code>DateRangePicker</code> emits <code>update:modelValue</code> / <code>change</code> as a <code>[from, to]</code> tuple (<code>DateRangeValue = [string, string] | []</code>) instead of a comma-joined string. The <code>modelValue</code> prop already accepted <code>string[]</code>. Only the emit changes.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">// before</span></span>
<span class="line"><span class="s_50ecpt">function</span><span class="s_indoxt"> onChange</span><span class="s_13ahmt">(</span><span class="s_fsg3al">v</span><span class="s_2ekfrt">:</span><span class="s_i592pt"> string</span><span class="s_13ahmt">)</span><span class="s_13ahmt"> { </span><span class="s_252irl">const</span><span class="s_1jjt6x"> [</span><span class="s_295sjd">from</span><span class="s_1jjt6x">,</span><span class="s_295sjd"> to</span><span class="s_1jjt6x">]</span><span class="s_2ekfrt"> =</span><span class="s_22m8k2"> v</span><span class="s_1jjt6x">.</span><span class="s_indoxt">split</span><span class="s_13ahmt">(</span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">,</span><span class="s_w1p9wo">&#39;</span><span class="s_13ahmt">) }</span></span>
<span class="line"><span class="s_1th9sy">// after</span></span>
<span class="line"><span class="s_50ecpt">function</span><span class="s_indoxt"> onChange</span><span class="s_13ahmt">(</span><span class="s_fsg3al">v</span><span class="s_2ekfrt">:</span><span class="s_euu481"> DateRangeValue</span><span class="s_13ahmt">)</span><span class="s_13ahmt"> { </span><span class="s_252irl">const</span><span class="s_1jjt6x"> [</span><span class="s_295sjd">from</span><span class="s_1jjt6x">,</span><span class="s_295sjd"> to</span><span class="s_1jjt6x">]</span><span class="s_2ekfrt"> =</span><span class="s_22m8k2"> v</span><span class="s_13ahmt"> }</span><span class="s_1th9sy"> // [] when cleared</span></span></code></pre></div><p>Reactive forms that pass the value through unchanged are not affected.</p><h4 id="datepicker-family-—-footer-removed-new-actions-sidebar-slot" tabindex="-1">DatePicker family — footer removed; new <code>#actions</code> sidebar slot <a class="header-anchor" href="#datepicker-family-—-footer-removed-new-actions-sidebar-slot" aria-label="Permalink to “DatePicker family — footer removed; new #actions sidebar slot”">​</a></h4><p>The popover footer on <code>DatePicker</code>, <code>DateRangePicker</code> and <code>DateTimePicker</code> is removed, including the Clear button it rendered automatically when <code>clearable &amp;&amp; hasValue</code>. <code>clearable</code> still controls the clear control on the input.</p><ul><li>The new <code>#actions</code> slot renders as a <strong>left sidebar</strong> inside the popover. Its slot props include <code>close</code>, <code>setDate</code> / <code>setRange</code>, and <code>clear</code>. <code>DateRangePicker</code>&#39;s <code>setRange([from, to])</code> sets both ends in one step. Use it for fixed-window presets (&quot;Last 7 days&quot;).</li><li>The popover content is <code>w-fit</code> when you provide the slot.</li><li>The sidebar <code>&lt;aside&gt;</code> carries <code>data-slot=&quot;actions&quot;</code> for CSS.</li></ul><p><strong>What to do:</strong> if you relied on the automatic Clear button, render your own Clear row inside <code>#actions</code>, using the <code>clear</code> slot prop.</p><h4 id="datetimepicker-—-date-selection-keeps-popover-open-breaking" tabindex="-1">DateTimePicker — date selection keeps popover open (breaking) <a class="header-anchor" href="#datetimepicker-—-date-selection-keeps-popover-open-breaking" aria-label="Permalink to “DateTimePicker — date selection keeps popover open (breaking)”">​</a></h4><p>Selecting a date in <code>DateTimePicker</code> no longer closes the popover. Focus moves into the embedded <code>TimePicker</code> instead, so the user goes straight from date to time. The popover closes on <code>Esc</code>, on a click outside, or on a call to <code>close()</code>.</p><p><strong>What to do:</strong> if you relied on the automatic close, bind <code>v-model:open</code> and close it from <code>@update:modelValue</code>. Or render an &quot;Apply&quot; button in <code>#actions</code>, which receives <code>close</code> in its slot props.</p><h4 id="timepicker-—-v1-refresh" tabindex="-1">TimePicker — v1 refresh <a class="header-anchor" href="#timepicker-—-v1-refresh" aria-label="Permalink to “TimePicker — v1 refresh”">​</a></h4><p>The same names as the DatePicker family, plus a flexible parser.</p><ul><li><code>side</code> / <code>align</code> / <code>offset</code> replace <code>placement</code> (removed).</li><li><code>keepOpen</code> (default <code>false</code>) replaces <code>autoClose</code> (removed).</li><li><code>typeable</code> (default <code>true</code>) replaces picker-level <code>readonly</code> / <code>allowCustom</code> (both removed).</li><li><code>v-model:open</code> through <code>open</code> + <code>update:open</code>. New <code>openOnFocus</code> (default <code>false</code>) and <code>openOnClick</code> (default <code>true</code>) props.</li><li>Flexible typed input: <code>&quot;3pm&quot;</code>, <code>&quot;3.30pm&quot;</code>, <code>&quot;1500&quot;</code> and <code>&quot;9:30:15 am&quot;</code> parse to the canonical <code>HH:mm[:ss]</code>.</li><li><code>min</code> / <code>max</code> replace <code>minTime</code> / <code>maxTime</code> (removed).</li><li><code>scrollMode</code> is removed. The list is always centered on the selection.</li><li>The template ref exposes only <code>focus()</code> (ADR-0012). <code>selectAll()</code> and <code>blurInput()</code> had no callers and are removed.</li></ul><h4 id="datepicker-family-—-keyboard-navigation" tabindex="-1">DatePicker family — keyboard navigation <a class="header-anchor" href="#datepicker-family-—-keyboard-navigation" aria-label="Permalink to “DatePicker family — keyboard navigation”">​</a></h4><p>Full keyboard navigation inside the calendar grid, following the WAI-ARIA APG Date Picker Dialog pattern.</p><ul><li><code>↓</code> on the trigger input opens the popover and moves focus to the selected cell, or to today.</li><li>In the grid: <code>←</code>/<code>→</code> move ±1 day, <code>↑</code>/<code>↓</code> ±1 week, <code>Home</code>/<code>End</code> to the week edges, <code>PageUp</code>/<code>PageDown</code> ±1 month, <code>Shift+PageUp</code>/<code>Shift+PageDown</code> ±1 year.</li><li><code>Enter</code> / <code>Space</code> selects. <code>Esc</code> closes and returns focus to the input.</li><li>Disabled dates (from <code>min</code> / <code>max</code> / <code>isDateUnavailable</code>) are skipped.</li><li>Arrow keys move across month boundaries on their own.</li><li><code>DateRangePicker</code> shows two months: arrow keys cross between them without moving the view, and the range shading follows the cell with keyboard focus.</li></ul><p>Only one cell is in the tab order at a time, so <code>Tab</code> enters and leaves the grid as one unit. Custom <code>#trigger</code> slots get this too: any way of opening moves focus into the grid, because a trigger that is not a <code>TextInput</code> has nothing to type into.</p><h4 id="datepicker-family-—-legacy-composable-removed" tabindex="-1">DatePicker family — legacy composable removed (breaking, loud) <a class="header-anchor" href="#datepicker-family-—-legacy-composable-removed" aria-label="Permalink to “DatePicker family — legacy composable removed (breaking, loud)”">​</a></h4><p><code>useDatePicker</code> and its helpers (<code>getDate</code>, <code>getDatesAfter</code>, <code>getDaysInMonth</code>, <code>isLeapYear</code>) were not used by any picker component and were not part of the v1 API. They are deleted outright. The import fails, so the break is loud.</p><p><strong>What to do:</strong> use the picker components directly.</p><h4 id="datepicker-timepicker-family-—-deprecated-aliases-removed-adr-0008" tabindex="-1">DatePicker / TimePicker family — deprecated aliases removed (breaking, silent) <a class="header-anchor" href="#datepicker-timepicker-family-—-deprecated-aliases-removed-adr-0008" aria-label="Permalink to “DatePicker / TimePicker family — deprecated aliases removed (breaking, silent)”">​</a></h4><p>The compatibility aliases these components carried through the betas are deleted, not kept as shims that warn and map to the new name. Per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>, no deprecated member ships in <code>1.0.0</code>.</p><ul><li><strong><code>placement</code>, <code>autoClose</code>, <code>allowCustom</code>, picker-level <code>readonly</code>, <code>inputClass</code>, and the <code>value</code> prop are removed.</strong> All silent: a leftover prop lands as an unused extra attribute and does nothing.</li><li><strong>The <code>#target</code> slot is removed.</strong> Content in a leftover <code>&lt;template #target&gt;</code> silently stops rendering. Use <code>#trigger</code>.</li><li><strong><code>DateTimePicker.minDateTime</code>/<code>maxDateTime</code> and <code>TimePicker.minTime</code>/<code>maxTime</code> are removed.</strong> Silent: the limit is no longer enforced. Use <code>min</code>/<code>max</code>.</li><li><strong><code>TimePicker.scrollMode</code> is removed.</strong> Silent. The list is always centered.</li></ul><p><code>change</code> stays as a supported second emit next to <code>update:modelValue</code>. It was never deprecated on <code>TimePicker</code>, and <code>DateTimePicker</code> depends on it internally, so removing it from the other two pickers would have been an inconsistent break that nothing required.</p><p><strong>What to do:</strong> see the before/after for each in the <a href="/docs/migration#datepicker-timepicker-family">migration guide</a>, and the <a href="#deprecation-log">Deprecation log</a> for each replacement.</p><h4 id="monthpicker-—-removed-breaking" tabindex="-1">MonthPicker — removed (breaking) <a class="header-anchor" href="#monthpicker-—-removed-breaking" aria-label="Permalink to “MonthPicker — removed (breaking)”">​</a></h4><p><code>MonthPicker</code> and all its files (<code>MonthPicker.vue</code>, types, stories) are deleted. It duplicated <code>Select</code> for a narrower case. The import fails, so the break is loud.</p><p><strong>What to do:</strong> use <code>Select</code> with month options. See the <a href="/docs/migration#monthpicker">migration guide</a>.</p><h3 id="select-combobox-multiselect-and-dropdown" tabindex="-1">Select, Combobox, MultiSelect and Dropdown <a class="header-anchor" href="#select-combobox-multiselect-and-dropdown" aria-label="Permalink to “Select, Combobox, MultiSelect and Dropdown”">​</a></h3><h4 id="selection-emit-interfaces-drop-the-model-events-breaking-in-ts-only" tabindex="-1">Selection emit interfaces drop the model events (breaking in TS only) <a class="header-anchor" href="#selection-emit-interfaces-drop-the-model-events-breaking-in-ts-only" aria-label="Permalink to “Selection emit interfaces drop the model events (breaking in TS only)”">​</a></h4><p><code>ComboboxEmits</code> and <code>MultiSelectEmits</code> no longer declare <code>&#39;update:open&#39;</code> and <code>&#39;update:query&#39;</code>. <code>SelectEmits</code> no longer declares <code>&#39;update:modelValue&#39;</code>. Each component already declares those events through <code>defineModel</code>. Declaring them twice collapsed <code>$emit</code>&#39;s signature to <code>(event, ...args: unknown[])</code>, so a typed <code>@update:open</code> listener did not compile. <code>SelectEmits</code> also disagreed with the generated payload, which carries <code>undefined</code> because the model prop is optional. So a wrapper that re-bound <code>@update:model-value</code> failed to compile.</p><p><strong>The runtime events are unchanged.</strong> <code>v-model</code>, <code>v-model:open</code>, <code>v-model:query</code> and the matching listeners all fire exactly as before, and the API tables still list every event. Only these five interface <em>members</em> are gone:</p><table tabindex="0"><thead><tr><th>Removed member</th><th>Still emitted at runtime</th></tr></thead><tbody><tr><td><code>ComboboxEmits[&#39;update:open&#39;]</code></td><td>yes</td></tr><tr><td><code>ComboboxEmits[&#39;update:query&#39;]</code></td><td>yes</td></tr><tr><td><code>MultiSelectEmits[&#39;update:open&#39;]</code></td><td>yes</td></tr><tr><td><code>MultiSelectEmits[&#39;update:query&#39;]</code></td><td>yes</td></tr><tr><td><code>SelectEmits[&#39;update:modelValue&#39;]</code></td><td>yes</td></tr></tbody></table><p>You are affected only if you indexed those interfaces by hand, as in <code>type Handler = ComboboxEmits[&#39;update:open&#39;]</code>, or passed one to <code>defineEmits</code> in a wrapper.</p><p><strong>What to do:</strong> type the handler from the model instead: <code>(value: boolean) =&gt; void</code> for <code>open</code>, and <code>(value: SelectOptionValue | null | undefined) =&gt; void</code> for a selection model. The <code>undefined</code> is there because the model prop is optional. The component never emits it.</p><h4 id="select-—-nothing-selected-is-null-breaking-silent" tabindex="-1"><code>Select</code> — nothing selected is <code>null</code> (breaking, silent) <a class="header-anchor" href="#select-—-nothing-selected-is-null-breaking-silent" aria-label="Permalink to “Select — nothing selected is null (breaking, silent)”">​</a></h4><p><code>Select</code> emitted <code>undefined</code> for an empty value, while <code>Combobox</code> emitted <code>null</code>. So one single-value family had two answers. Both are <code>null</code> now. <code>MultiSelect</code> keeps <code>[]</code>, because apps loop over an empty array.</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">// Before — Select</span></span>
<span class="line"><span class="s_indoxt">watch</span><span class="s_13ahmt">(</span><span class="s_22m8k2">value</span><span class="s_1jjt6x">,</span><span class="s_13ahmt"> (</span><span class="s_fsg3al">v</span><span class="s_13ahmt">)</span><span class="s_50ecpt"> =&gt;</span><span class="s_13ahmt"> { </span><span class="s_50ecpt">if</span><span class="s_13ahmt"> (</span><span class="s_22m8k2">v</span><span class="s_50ecpt"> ===</span><span class="s_40mev6"> undefined</span><span class="s_13ahmt">) </span><span class="s_indoxt">reset</span><span class="s_13ahmt">() })</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">// After</span></span>
<span class="line"><span class="s_indoxt">watch</span><span class="s_13ahmt">(</span><span class="s_22m8k2">value</span><span class="s_1jjt6x">,</span><span class="s_13ahmt"> (</span><span class="s_fsg3al">v</span><span class="s_13ahmt">)</span><span class="s_50ecpt"> =&gt;</span><span class="s_13ahmt"> { </span><span class="s_50ecpt">if</span><span class="s_13ahmt"> (</span><span class="s_22m8k2">v</span><span class="s_50ecpt"> ===</span><span class="s_40mev6"> null</span><span class="s_13ahmt">) </span><span class="s_indoxt">reset</span><span class="s_13ahmt">() })</span></span></code></pre></div><p>An empty string is still a real value, so a &quot;None&quot; row with <code>value: &#39;&#39;</code> still round-trips. <code>clear()</code> and the <code>clear</code> slot prop both write <code>null</code>.</p><h4 id="autocomplete-—-removed-breaking" tabindex="-1">Autocomplete — removed (breaking) <a class="header-anchor" href="#autocomplete-—-removed-breaking" aria-label="Permalink to “Autocomplete — removed (breaking)”">​</a></h4><ul><li><strong>Breaking:</strong> <code>Autocomplete</code> and its <code>AutocompleteProps</code> type are deleted. Use <code>Combobox</code> for one value and <code>MultiSelect</code> for several. The import fails, so the build names every call site. <code>trigger=&quot;button&quot;</code> on either replacement gives the shape of <code>Autocomplete</code>&#39;s default target: a button that shows the selection, with the search box inside the popover.</li><li><strong>Breaking, silent:</strong> <code>FormControl type=&quot;autocomplete&quot;</code> is removed. <code>FormControl</code> falls through to <code>TextInput</code> and still forwards the type, so the result is <code>&lt;input type=&quot;autocomplete&quot;&gt;</code>: a plain text box, with no build or runtime error. A <code>console.error</code> in development names the removal. Use <code>type=&quot;combobox&quot;</code>, or <code>Combobox</code> on its own.</li><li><strong>Breaking, silent:</strong> the <code>v-model</code> value changes shape. <code>Autocomplete</code> held the whole option object. Both replacements hold only the value. Listen to <code>@update:selectedOption</code> where you need the whole option.</li><li><strong>Breaking, silent:</strong> <code>#target</code>&#39;s <code>open</code> slot prop was the <em>function</em> that opened the popover. <code>#trigger</code>&#39;s <code>open</code> is the open <em>state</em>. Anything that read it as a value (<code>v-if=&quot;open&quot;</code>) was always true, and now is not.</li><li>Otherwise, <code>#target</code> becomes <code>#trigger</code>. <code>Combobox</code> and <code>MultiSelect</code> attach the open toggle to the trigger element themselves, so drop your click handler. A <code>togglePopover()</code> carried through the rename throws on click. The popover still opens, so it looks like it works while it logs an error.</li><li>Grouped options use <code>{ group, options }</code>, not <code>{ group, items }</code>. Both option normalizers now throw an error that names the group and the rename, instead of failing inside a <code>map</code> call.</li></ul><p><strong>What to do:</strong> see the before/after for each silent break in the <a href="/docs/migration#autocomplete-removed">migration guide</a>.</p><h4 id="dropdown-contextmenu-—-deprecated-members-removed-adr-0008" tabindex="-1">Dropdown / ContextMenu — deprecated members removed (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#dropdown-contextmenu-—-deprecated-members-removed-adr-0008" aria-label="Permalink to “Dropdown / ContextMenu — deprecated members removed (breaking; loud in TS, silent in JS)”">​</a></h4><p>Per ADR-0008, three surfaces that shipped as deprecated aliases in the betas are deleted, not aliased. All three are <strong>silent breaks</strong> in plain-JavaScript apps. TypeScript callers get compile errors, because the removed keys stay typed as <code>never</code>. A warning also fires in development when the old shape reaches the menu at runtime.</p><ul><li><strong>The <code>placement</code> prop and the <code>DropdownPlacement</code> type are removed.</strong> Use <code>align</code> (<code>left</code>→<code>start</code>, <code>center</code>→<code>center</code>, <code>right</code>→<code>end</code>). A leftover <code>placement</code> is ignored, and the menu falls back to <code>align=&quot;start&quot;</code>.</li><li><strong><code>{ group, items }</code> is removed.</strong> Use <code>{ group, options }</code>, like <code>Combobox</code> / <code>MultiSelect</code> / <code>Select</code>. A leftover <code>items</code> group renders as an empty menu.</li><li><strong><code>component:</code> option rows are removed</strong> (<code>DropdownComponentOption</code>, <code>ContextMenuComponentOption</code>). Use <code>slots: { item: fn }</code>. A leftover <code>component:</code> row renders as a plain action row from its <code>label</code>.</li></ul><p>Also removed: the <strong><code>DropdownExposed</code> type</strong>. It described a <code>close()</code> template-ref member that <code>Dropdown</code> never had. <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0012-template-ref-surface.md" target="_blank" rel="noreferrer">ADR-0012</a> keeps <code>Dropdown</code>&#39;s template ref empty: <code>v-model:open</code> and the <code>close</code> slot prop cover it. It is type-only, so this break is loud.</p><p><strong>What to do:</strong> see the before/afters in the <a href="/docs/migration#dropdown-and-contextmenu">migration guide</a>.</p><h4 id="dropdown-—-disabled-state-reaches-the-menu-primitive" tabindex="-1">Dropdown — disabled state reaches the menu primitive <a class="header-anchor" href="#dropdown-—-disabled-state-reaches-the-menu-primitive" aria-label="Permalink to “Dropdown — disabled state reaches the menu primitive”">​</a></h4><p>The trigger now passes its disabled state (from <code>button.disabled</code> or a <code>disabled</code> attribute you pass) to the underlying menu primitive. Before, only the generated <code>Button</code> was natively disabled. A custom trigger slot with a <code>disabled</code> attribute could still open the menu from the keyboard or with synthetic clicks.</p><h4 id="select-—-item-slot-prop-renamed-to-item" tabindex="-1">Select — <code>#item-*</code> slot prop renamed to <code>item</code> (breaking, silent) <a class="header-anchor" href="#select-—-item-slot-prop-renamed-to-item" aria-label="Permalink to “Select — #item-* slot prop renamed to item (breaking, silent)”">​</a></h4><p><code>#item-prefix</code>, <code>#item-label</code> and <code>#item-suffix</code> on <code>Select</code> pass the option as <code>item</code>, like <code>Combobox</code> and <code>MultiSelect</code>. The old <code>option</code> key is removed with the rest of the deprecated surface (ADR-0008). Destructuring <code>{ option }</code> gives <code>undefined</code>, silently. No runtime warning is possible, because destructuring a slot prop cannot be detected.</p><p><strong>What to do:</strong> search for <code>#item-</code> slots that destructure <code>option</code>, and rename them to <code>item</code>.</p><div class="language-vue"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">&lt;!-- before --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">Select</span><span class="s_1jjt6x"> :</span><span class="s_1i4ay4">options</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_22m8k2">people</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2">&gt;</span></span>
<span class="line"><span class="s_6am9cx">  &lt;template #item-prefix=&quot;{ option }&quot;&gt;</span></span>
<span class="line"><span class="s_6am9cx">    &lt;Avatar :image=&quot;option.image&quot; /&gt;</span></span>
<span class="line"><span class="s_6am9cx">  &lt;/template&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;/</span><span class="s_1uuh8p">Select</span><span class="s_1zd9e2">&gt;</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">&lt;!-- after --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">Select</span><span class="s_1jjt6x"> :</span><span class="s_1i4ay4">options</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_22m8k2">people</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2">&gt;</span></span>
<span class="line"><span class="s_6am9cx">  &lt;template #item-prefix=&quot;{ item }&quot;&gt;</span></span>
<span class="line"><span class="s_6am9cx">    &lt;Avatar :image=&quot;item.image&quot; /&gt;</span></span>
<span class="line"><span class="s_6am9cx">  &lt;/template&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;/</span><span class="s_1uuh8p">Select</span><span class="s_1zd9e2">&gt;</span></span></code></pre></div><h4 id="combobox-—-trigger-sizing-matches-select" tabindex="-1">Combobox — trigger sizing matches Select <a class="header-anchor" href="#combobox-—-trigger-sizing-matches-select" aria-label="Permalink to “Combobox — trigger sizing matches Select”">​</a></h4><p>The root renders as a transparent layout box, so the trigger sizes like <code>Select</code> inside flex and grid containers. In button mode, the query is separate from the model.</p><h4 id="combobox-multiselect-—-suffix-slot-replaces-the-chevron" tabindex="-1">Combobox / MultiSelect — <code>#suffix</code> slot replaces the chevron <a class="header-anchor" href="#combobox-multiselect-—-suffix-slot-replaces-the-chevron" aria-label="Permalink to “Combobox / MultiSelect — #suffix slot replaces the chevron”">​</a></h4><p>A new <code>#suffix</code> slot on <code>Combobox</code> (input and button modes) and <code>MultiSelect</code>, like the one <code>Select</code> already has. When you provide the slot, it replaces the default chevron. If your content is conditional, render a chevron yourself as the fallback. The main use is an inline clear button. See <code>Combobox/stories/Clearable.vue</code>.</p><h4 id="combobox-—-condition-authoritative-for-type-custom-rows" tabindex="-1">Combobox — <code>condition</code> authoritative for <code>type: &#39;custom&#39;</code> rows <a class="header-anchor" href="#combobox-—-condition-authoritative-for-type-custom-rows" aria-label="Permalink to “Combobox — condition authoritative for type: &#39;custom&#39; rows”">​</a></h4><p>A custom row&#39;s <code>condition({ query })</code> is now called even before the user types after opening. So it can fully control its own visibility, based on the selection and the typed query. Selectable rows are unchanged. This lets you build &quot;create new&quot; rows directly with <code>condition</code>, with no need for a separate <code>createOption</code> prop. See <code>Combobox/stories/CreateNew.vue</code>.</p><h4 id="multiselect-—-summary-suppresses-the-phantom-sizer" tabindex="-1">MultiSelect — <code>#summary</code> suppresses the phantom sizer <a class="header-anchor" href="#multiselect-—-summary-suppresses-the-phantom-sizer" aria-label="Permalink to “MultiSelect — #summary suppresses the phantom sizer”">​</a></h4><p>By default, the trigger sets a minimum width from the longest default summary (<code>placeholder</code> or <code>&quot;N selected&quot;</code>), so the trigger does not change width as the count changes. That sizing cannot predict custom text, so it is now skipped when you provide <code>#summary</code>. The trigger then sizes to its content, and you control the width.</p><h3 id="dialog-popover-tooltip-hovercard-and-toast" tabindex="-1">Dialog, Popover, Tooltip, HoverCard and Toast <a class="header-anchor" href="#dialog-popover-tooltip-hovercard-and-toast" aria-label="Permalink to “Dialog, Popover, Tooltip, HoverCard and Toast”">​</a></h3><h4 id="toast-the-compat-shims-are-removed-breaking-one-of-them-silent" tabindex="-1">Toast: the compat shims are removed (breaking, one of them silent) <a class="header-anchor" href="#toast-the-compat-shims-are-removed-breaking-one-of-them-silent" aria-label="Permalink to “Toast: the compat shims are removed (breaking, one of them silent)”">​</a></h4><p><code>spec/toast.md</code> defines the official API as sonner&#39;s namespace. These four surfaces were never part of it. They carried no <code>@deprecated</code> tag, only a runtime warning, so a search for the tag could not find them. (A &quot;census&quot; below is a count of call sites across the surveyed apps.)</p><p><strong>Loud: the member is gone and the call throws.</strong></p><ul><li><code>toast.create({ message, … })</code> → <code>toast(message, { … })</code> or <code>toast.message(…)</code>. Census: 7 sites, helpdesk 5 and suite 2. Suite&#39;s two are local wrapper functions, so fixing them covers roughly 42 files downstream.</li><li><code>toast.remove(id)</code> → <code>toast.dismiss(id)</code>. Census: 0 sites.</li><li><code>toast.removeAll()</code> → <code>toast.dismiss()</code>. Census: 4 sites in suite (mail 3, calendar 1). An earlier count said 0; that was wrong.</li></ul><p><strong>Silent, and this is the dangerous one.</strong></p><ul><li>The legacy object form <code>toast({ title, text, … })</code> is gone. Nothing throws. The object goes straight to sonner as the message, and sonner expects a string, a component or a VNode, so the toast renders empty or wrong. The census found no direct call sites in the surveyed apps. But a search for <code>toast(</code> will not find these calls; search for the <code>title</code>, <code>text</code> and <code>message</code> keys instead. See the before/after in the <a href="/docs/migration#toast-legacy-object">migration guide</a>.</li></ul><p><code>renderSafeHTML</code>, <code>dispatch</code> and the four semantic creators (<code>success</code>/<code>error</code>/<code>warning</code>/<code>info</code>) are unchanged.</p><h4 id="toast-description-supports-the-same-limited-inline-html-as-the-message-breaking-silent" tabindex="-1">Toast: <code>description</code> supports the same limited inline HTML as the message (breaking, silent) <a class="header-anchor" href="#toast-description-supports-the-same-limited-inline-html-as-the-message-breaking-silent" aria-label="Permalink to “Toast: description supports the same limited inline HTML as the message (breaking, silent)”">​</a></h4><p><code>description</code> passed through the options object untouched, so sonner rendered it as text, while the message was sanitized and rendered as HTML. It now goes through the same DOMPurify safelist (<code>a</code>, <code>em</code>, <code>strong</code>, <code>i</code>, <code>b</code>, <code>u</code>).</p><ul><li><strong>Breaking, silent:</strong> a description that holds a <code>&lt;</code> outside that safelist loses those characters. <code>description: &#39;Set &lt;Button&gt; variant&#39;</code> rendered literally before. Now DOMPurify strips <code>&lt;Button&gt;</code> and the user sees <code>Set variant</code>. There is no warning and no error.</li><li>A census across builder, crm, gameplan, helpdesk, insights and suite found no toast <code>description</code> containing a <code>&lt;</code>. The one hit was a Gameplan Cypress fixture for a space description, not a toast.</li><li>Descriptions that are not strings (components, VNodes, render functions) pass through untouched, as before.</li><li><strong>Also breaking, silent:</strong> <code>toast.message</code> and <code>toast.loading</code> now sanitize their <em>message</em> and render it as inline HTML too. Before, they came from vue-sonner&#39;s namespace untouched, so <code>toast.message(&#39;&lt;b&gt;hi&lt;/b&gt;&#39;)</code> printed the tags literally. Now it renders bold. This makes them consistent with <code>toast()</code> and the four semantic creators, and the <code>toast.create</code> migration advice in the entry above depends on it.</li><li><code>toast.custom</code> takes a component rather than a message, so only its <code>description</code> is covered. <code>toast.promise</code> keys its strings by state, and its <code>success</code>/<code>error</code> may be async functions, so only its <code>description</code> is covered. The state strings render as vue-sonner renders them.</li></ul><p>This lands before the tag on purpose. Doing it in a <code>1.x</code> release would silently change the rendered output for every existing caller.</p><h4 id="hovercard-s-side-align-and-portalto-use-our-own-vocabulary-breaking-in-ts" tabindex="-1">HoverCard&#39;s <code>side</code>, <code>align</code> and <code>portalTo</code> use our own vocabulary (breaking in TS) <a class="header-anchor" href="#hovercard-s-side-align-and-portalto-use-our-own-vocabulary-breaking-in-ts" aria-label="Permalink to “HoverCard&#39;s side, align and portalTo use our own vocabulary (breaking in TS)”">​</a></h4><p><code>HoverCard</code> took the types of these three props from <code>reka-ui</code> instead of declaring them. So <code>portalTo</code> accepted <code>null</code> and any object, while the other six overlays accepted <code>string | HTMLElement</code>. <code>:portal-to=&quot;document.querySelector(&#39;#panel&#39;)&quot;</code> compiled against <code>HoverCard</code> and failed against <code>Popover</code>.</p><ul><li><code>side</code> is now <code>PopoverSide</code>, <code>align</code> is <code>PopoverAlign</code>, and <code>portalTo</code> is <code>PortalTarget</code> (<code>string | HTMLElement</code>), like every other overlay.</li><li><strong>Breaking in TS:</strong> passing <code>null</code> or an object to <code>portalTo</code> no longer compiles. Runtime behavior is unchanged.</li><li><code>side</code> and <code>align</code> are not a break. reka&#39;s <code>Side</code> and <code>Align</code> resolve to exactly <code>PopoverSide</code> and <code>PopoverAlign</code> today.</li></ul><p>A type cannot be narrowed inside <code>1.x</code> once it is wide, which is why this lands before the tag rather than after.</p><p><strong>What to do:</strong> pass a selector string or an element to <code>portalTo</code>.</p><h4 id="settingsdialog-—-open-state-moves-to-v-model-open-breaking-silent" tabindex="-1">SettingsDialog — open state moves to <code>v-model:open</code> (breaking, silent) <a class="header-anchor" href="#settingsdialog-—-open-state-moves-to-v-model-open-breaking-silent" aria-label="Permalink to “SettingsDialog — open state moves to v-model:open (breaking, silent)”">​</a></h4><p><code>v-model</code> → <code>v-model:open</code>, and <code>update:modelValue</code> → <code>update:open</code> (#1054). <code>CONTEXT.md</code> says the visibility of an overlay is <code>open</code>, &quot;always bound via <code>v-model:open</code>&quot;, and lists bare <code>v-model</code> for visibility among the names to avoid. <code>Dialog</code>&#39;s support for both bindings is a documented exception for <code>Dialog</code> only, and new components do not copy it. <code>v-model:tab</code> is unchanged.</p><p><strong>Silent break:</strong> Vue accepts the unknown <code>modelValue</code> prop with no error, so the dialog never opens.</p><p><strong>What to do:</strong> change <code>v-model</code> to <code>v-model:open</code>. See the <a href="/docs/migration#settingsdialog">migration guide</a>.</p><h4 id="dialog-—-theme-yellow-renamed-to-amber-breaking-silent" tabindex="-1">Dialog — <code>theme: &#39;yellow&#39;</code> renamed to <code>&#39;amber&#39;</code> (breaking, silent) <a class="header-anchor" href="#dialog-—-theme-yellow-renamed-to-amber-breaking-silent" aria-label="Permalink to “Dialog — theme: &#39;yellow&#39; renamed to &#39;amber&#39; (breaking, silent)”">​</a></h4><p><code>DialogTheme</code> held the library&#39;s last <code>yellow</code> (#1054). <code>Alert</code>, <code>SidebarCard</code>, <code>Badge</code> and <code>Avatar</code> all spell the warning tone <code>amber</code>, and <code>Dialog</code> already rendered <code>yellow</code> with the amber tokens (<code>bg-surface-amber-2</code>, <code>text-ink-amber-5</code>). So the value name disagreed with the token it resolved to. This applies to <code>icon.theme</code> and to the <code>theme</code> argument of <code>dialog.confirm</code> / <code>dialog.danger</code>. Only the word changes, not the color.</p><p><strong>Silent break</strong> for JavaScript call sites: <code>yellow</code> is no longer a key in the tone maps, so the icon renders with no tint and nothing throws. TypeScript call sites get a union error. The same change corrected the mapping in the <a href="#dialog-—-deprecated-surface-removed-breaking"><code>icon.appearance</code> entry</a>, which used to say <code>warning → yellow</code> and now says <code>warning → amber</code>.</p><p><strong>What to do:</strong> replace <code>yellow</code> with <code>amber</code>. See the <a href="/docs/migration#dialog">migration guide</a>.</p><h4 id="dialog-—-icon-takes-a-string-or-a-component-tone-moves-to-theme-breaking" tabindex="-1">Dialog — <code>icon</code> takes a string or a component, tone moves to <code>theme</code> (breaking) <a class="header-anchor" href="#dialog-—-icon-takes-a-string-or-a-component-tone-moves-to-theme-breaking" aria-label="Permalink to “Dialog — icon takes a string or a component, tone moves to theme (breaking)”">​</a></h4><p>The structured <code>DialogIcon</code> object is gone. <code>icon</code> is a <code>lucide-*</code> class name or a Vue component, and <code>theme</code> (<code>amber | blue | red | green</code>) colors the badge behind it. The same split applies to <code>dialog.confirm</code>, <code>dialog.danger</code> and <code>dialog.prompt</code>, which already had a top-level <code>theme</code>.</p><ul><li><strong>Loud in development, silent in production:</strong> an object still passed to <code>icon</code> renders an empty icon badge. The circle paints in the neutral tone with no glyph in it. A development build warns once per component and prop: <code>[frappe-ui] Dialog.icon received a plain object ...</code>.</li><li><strong>Loud in TypeScript:</strong> the <code>DialogIcon</code> export is removed.</li><li><code>paddingTop</code> accepts a number again. A length with no unit never reached the CSSOM, so <code>:padding-top=&quot;80&quot;</code> removed the position padding and added nothing back. A number is pixels now.</li><li>Styling hooks: <code>data-slot=&quot;content&quot;</code> on the card, <code>data-slot=&quot;icon&quot;</code> on the header badge, <code>data-slot=&quot;actions&quot;</code> on the footer row. BottomSheet&#39;s content carries <code>data-slot=&quot;content&quot;</code> too, and the sheet finds itself through that hook instead of a class name.</li></ul><p><strong>What to do:</strong> pass the icon name or component to <code>icon</code>, and the tone to <code>theme</code>. See the <a href="/docs/migration#dialog-icon-theme">migration guide</a>.</p><h4 id="dialog-actions-are-typed-imperativedialogaction-breaking-loud" tabindex="-1"><code>dialog.*</code> actions are typed <code>ImperativeDialogAction</code> (breaking, loud) <a class="header-anchor" href="#dialog-actions-are-typed-imperativedialogaction-breaking-loud" aria-label="Permalink to “dialog.* actions are typed ImperativeDialogAction (breaking, loud)”">​</a></h4><p>Two different action shapes shared the name <code>DialogAction</code>. The component&#39;s <code>actions</code> prop keeps it. The array for the imperative helpers is <code>ImperativeDialogAction</code>, which the root now exports. Its <code>onClick</code> receives <code>{ close, setError }</code> and is awaited. This changes types only.</p><h4 id="toast-—-the-default-duration-is-4000ms-and-the-option-types-are-exported" tabindex="-1">Toast — the default duration is 4000ms, and the option types are exported <a class="header-anchor" href="#toast-—-the-default-duration-is-4000ms-and-the-option-types-are-exported" aria-label="Permalink to “Toast — the default duration is 4000ms, and the option types are exported”">​</a></h4><ul><li>No behavior change. The toast container never set a duration, so vue-sonner&#39;s own 4000ms applied, while <code>spec/toast.md</code> promised 5000ms. The spec and the docs say 4000ms now. Pass <code>duration</code> per toast to change it.</li><li><code>ToastOptions</code>, <code>ToastAction</code> and <code>ToastId</code> are exported from <code>frappe-ui</code>. An app that wraps <code>toast</code> no longer imports types from vue-sonner, which it does not depend on.</li><li><code>ToastProvider</code> still takes no props, on purpose.</li></ul><h4 id="documented-not-changed-dialog-breadcrumbs-and-alert-contracts" tabindex="-1">Documented, not changed: Dialog, Breadcrumbs and Alert contracts <a class="header-anchor" href="#documented-not-changed-dialog-breadcrumbs-and-alert-contracts" aria-label="Permalink to “Documented, not changed: Dialog, Breadcrumbs and Alert contracts”">​</a></h4><ul><li><code>Dialog.Title</code>, <code>Dialog.Description</code> and <code>Dialog.Close</code> are public parts. A <code>bare</code> dialog needs <code>Dialog.Title</code> for its accessible name.</li><li>Dialog <code>message</code> stays. It is the body of a confirm-style dialog, and it is announced with the dialog through reka&#39;s <code>DialogDescription</code>.</li><li><code>BreadcrumbItem</code> keeps its open index signature, so a crumb can carry extra fields for the <code>#prefix</code> / <code>#suffix</code> slots.</li><li>Alert and SidebarCard keep <code>data-color</code> for tone. <code>data-theme</code> is the light/dark attribute on the document, which is why the tone hook does not use that name. <code>PHILOSOPHY.md</code> lists it with the other styling hooks.</li><li>Alert <code>icon: true</code> means &quot;the theme&#39;s automatic icon&quot;, the same as leaving it unset.</li></ul><h4 id="settingsdialog-—-settingsbody-s-exposed-type" tabindex="-1">SettingsDialog — <code>SettingsBody</code>&#39;s exposed type <a class="header-anchor" href="#settingsdialog-—-settingsbody-s-exposed-type" aria-label="Permalink to “SettingsDialog — SettingsBody&#39;s exposed type”">​</a></h4><p><code>SettingsBody</code>&#39;s exposed <code>viewportElement</code> now has a type, <code>SettingsBodyExposed</code> (ADR-0012), exported from <code>frappe-ui</code>. No behavior change.</p><h4 id="portal-target-for-embedded-apps" tabindex="-1">Portal target for embedded apps (additive) <a class="header-anchor" href="#portal-target-for-embedded-apps" aria-label="Permalink to “Portal target for embedded apps (additive)”">​</a></h4><ul><li><code>portalTo</code> on <code>Popover</code>, <code>HoverCard</code>, <code>Dropdown</code>, <code>Select</code>, <code>Combobox</code> and <code>MultiSelect</code> no longer declares a <code>&#39;body&#39;</code> prop default. An app that is not embedded still gets <code>&#39;body&#39;</code>, now as a fallback. No existing call behaves differently.</li><li>New <code>usePortalTarget</code> / <code>providePortalTarget</code> / <code>portalTargetKey</code> exports let a host page that embeds your app redirect every overlay at once. See <a href="https://github.com/frappe/frappe-ui/blob/main/spec/portal-target.md" target="_blank" rel="noreferrer"><code>spec/portal-target.md</code></a>.</li></ul><h4 id="dialog-—-v1-spec" tabindex="-1">Dialog — v1 spec <a class="header-anchor" href="#dialog-—-v1-spec" aria-label="Permalink to “Dialog — v1 spec”">​</a></h4><ul><li>Flat top-level props (<code>title</code>, <code>message</code>, <code>icon</code>, <code>size</code>, <code>position</code>, <code>paddingTop</code>, <code>actions</code>) are the standard way. The legacy <code>options</code> blob is removed; see <a href="#dialog-—-deprecated-surface-removed-breaking">deprecated surface removed</a>.</li><li><code>v-model:open</code> is the standard binding. <code>v-model</code> (<code>modelValue</code>) still works, with no warning.</li><li>New props: <code>dismissible</code> (default <code>true</code>, replaces <code>disableOutsideClickToClose</code>), <code>bare</code>, and <code>showCloseButton</code> (default <code>true</code>, independent of the automatic header).</li><li>The standard slots are <code>#default</code>, <code>#title</code> and <code>#actions</code> (with slot props <code>{ close, actions }</code>). The legacy <code>#body*</code> slots are removed.</li><li><code>icon.theme</code> (<code>amber | blue | red | green</code>) replaces <code>icon.appearance</code>, which is removed.</li><li>The automatic header no longer shows &quot;Untitled&quot; when there is no title.</li></ul><h4 id="dialog-—-imperative-dialog-api" tabindex="-1">Dialog — imperative <code>dialog.*</code> API <a class="header-anchor" href="#dialog-—-imperative-dialog-api" aria-label="Permalink to “Dialog — imperative dialog.* API”">​</a></h4><ul><li>New callback-based helpers: <code>dialog.confirm()</code>, <code>dialog.danger()</code>, <code>dialog.prompt()</code>. The <code>onConfirm</code> callback runs on click. If it resolves, the dialog closes. If it throws, the dialog stays open and shows the thrown message inline. The action button shows a loading state until <code>onConfirm</code> settles. Each helper also returns a handle right away, with <code>close()</code> to dismiss the dialog from code.</li><li><code>&lt;FrappeUIProvider&gt;</code> now renders <code>&lt;Dialogs /&gt;</code> next to <code>&lt;Toasts /&gt;</code>, so apps wrapped with the provider get the imperative dialogs with no extra setup. <code>&lt;Dialogs /&gt;</code> is still exported for apps that do not use the provider.</li><li><code>ConfirmDialog</code> and <code>confirmDialog()</code> are removed (see below). Use <code>dialog.confirm()</code> / <code>dialog.danger()</code>.</li><li>New root exports: <code>DangerArgs</code>, <code>DialogControl</code>, <code>PromptControl</code>, <code>DialogHandle</code>, <code>PromptFieldValidator</code>. <code>DialogSlotProps</code> is exported from the <code>Dialog</code> entry file.</li></ul><h4 id="dialog-—-deprecated-surface-removed-breaking" tabindex="-1">Dialog — deprecated surface removed (breaking) <a class="header-anchor" href="#dialog-—-deprecated-surface-removed-breaking" aria-label="Permalink to “Dialog — deprecated surface removed (breaking)”">​</a></h4><p>Every member marked <code>@deprecated</code> is deleted, per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>. Nothing is aliased and nothing warns.</p><ul><li><strong>Breaking, silent:</strong> the <code>options</code> blob prop and the <code>DialogOptions</code> type are gone. It bundled <code>title</code>/<code>size</code>/<code>icon</code>/<code>actions</code> into one object. An <code>:options=&quot;{...}&quot;</code> call site still compiles, because Vue drops the unknown prop as an unused attribute. But the dialog silently loses its title, size and actions. Use the flat top-level props.</li><li><strong>Breaking, silent:</strong> <code>disableOutsideClickToClose</code> is gone. It still lands as an unused attribute, and <code>dismissible</code> (the inverse) defaults to <code>true</code>, so the dialog silently becomes dismissible. Use <code>dismissible</code>.</li><li><strong>Breaking, silent:</strong> <code>icon.appearance</code> and <code>DialogIconAppearance</code> are gone. Only <code>icon.theme</code> remains. An <code>appearance</code> key is dropped, so the icon renders with no tone. Map <code>warning → amber</code>, <code>info → blue</code>, <code>danger → red</code>, <code>success → green</code>.</li><li><strong>Breaking, silent:</strong> the legacy <code>#body</code>, <code>#body-content</code>, <code>#body-main</code>, <code>#body-title</code> and <code>#body-header</code> slots are gone. Vue drops an unknown named slot with no error, so a call site you missed renders nothing where that slot&#39;s content used to be. Use <code>#default</code>, <code>#title</code> and <code>#actions</code>. <code>#body</code> maps to <code>bare</code> + <code>#default</code>.</li><li><strong>Breaking:</strong> the callable context on action <code>onClick</code> is gone. The context used to be callable as well as a plain object (<code>ctx()</code> closed the dialog). It is <code>{ close }</code> only now, so calling it as a function throws <code>TypeError: ctx is not a function</code>.</li><li><strong>Breaking:</strong> <code>defineExpose({ close })</code> and the <code>DialogExposed</code> type are gone. Dialog exposes nothing on its template ref (ADR-0012). A template-ref <code>.close()</code> call throws a <code>TypeError</code>. Use <code>v-model:open = false</code>, or the <code>close</code> slot prop. There were zero known call sites.</li><li><strong>Breaking:</strong> <code>ConfirmDialog</code> and <code>confirmDialog()</code> are deleted. The import fails, so the build names every call site. Use <code>dialog.confirm()</code> / <code>dialog.danger()</code>.</li></ul><p><strong>What to do:</strong> see the before/after for the silent breaks in the <a href="/docs/migration#dialog">migration guide</a>.</p><h4 id="popover-—-v0-api-removed-breaking" tabindex="-1">Popover — v0 API removed (breaking) <a class="header-anchor" href="#popover-—-v0-api-removed-breaking" aria-label="Permalink to “Popover — v0 API removed (breaking)”">​</a></h4><p>Every member marked <code>@deprecated</code> is deleted, per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>. Nothing is aliased and nothing warns.</p><ul><li><strong>Breaking, silent:</strong> the <code>#target</code>, <code>#body</code> and <code>#body-main</code> slots are gone. Vue drops an unknown slot with no error, so a call site you missed renders a popover with no trigger, or an empty one. Use <code>#trigger</code> and <code>#default</code>.</li><li><strong>Breaking, silent:</strong> <code>#trigger</code> wires the click itself, through reka&#39;s <code>PopoverTrigger</code>. A click handler carried over from <code>#target</code> toggles the popover a second time, so it opens and closes on one click.</li><li><strong>Breaking, silent:</strong> the <code>togglePopover</code> and <code>updatePosition</code> slot props are gone. <code>toggle</code> replaces the first. reka repositions on its own, so the second has no replacement.</li><li><strong>Breaking, silent:</strong> <code>placement</code>, <code>show</code>, <code>hideOnBlur</code>, <code>matchTargetWidth</code>, <code>trigger</code>, <code>hoverDelay</code>, <code>leaveDelay</code>, <code>popoverClass</code> and <code>transition</code> are removed, and the <code>update:show</code> emit no longer fires. An unknown prop is ignored, so the popover renders in its default position and state.</li><li><strong>Breaking, silent:</strong> attributes on <code>&lt;Popover&gt;</code> are no longer inherited. They used to land on a wrapper that the legacy <code>#target</code> rendered. <code>#trigger</code> renders its child directly (as-child), with no wrapper. Move <code>class</code> and <code>style</code> onto the element inside <code>#trigger</code>.</li><li><strong>Breaking:</strong> the <code>PopoverPlacement</code> and <code>PopoverLegacySlotProps</code> types are removed. Use <code>PopoverSide</code> + <code>PopoverAlign</code> and <code>PopoverSlotProps</code>. The import fails, so the build names every call site.</li><li>Fixed: <code>CalendarWeekDayEvent</code> passed <code>placement=&quot;center&quot;</code> in month view, which is not a side, and reached reka as one. It is now <code>side=&quot;bottom&quot;</code> + <code>align=&quot;center&quot;</code>.</li><li><strong>Breaking, silent:</strong> the slot props are <code>{ open, close, toggle }</code>. <code>open</code> is now the boolean state, like <code>Dropdown</code>, <code>Select</code>, <code>MultiSelect</code>, <code>HoverCard</code> and <code>Sidebar</code>. The <code>open()</code> method it used to be had no callers, because <code>#trigger</code> opens itself. <code>isOpen</code> is gone; read <code>open</code> instead. A destructured <code>isOpen</code> becomes <code>undefined</code> with no error, so styling that depends on it silently stops applying.</li><li>Fixed: <code>MonthPicker</code> styled its panel through <code>popoverClass</code>, which already did nothing, so the panel rendered with no surface at all. It uses the standard panel now.</li><li>Fixed: <code>:dismissible=&quot;false&quot;</code> still closed on <code>Escape</code>. Only the outside-click path was wired, while <code>CONTEXT.md</code> defines <code>dismissible</code> as covering both.</li></ul><p><strong>What to do:</strong> see the before/after for each silent break in the <a href="/docs/migration#popover-hovercard-tooltip">migration guide</a>.</p><h4 id="nestedpopover-—-removed-breaking" tabindex="-1">NestedPopover — removed (breaking) <a class="header-anchor" href="#nestedpopover-—-removed-breaking" aria-label="Permalink to “NestedPopover — removed (breaking)”">​</a></h4><p><strong>Breaking:</strong> <code>NestedPopover</code> is deleted. It never nested anything. It was the library&#39;s last popover built on <code>@headlessui/vue</code> + <code>@popperjs/core</code>, and <code>@popperjs/core</code> leaves <code>dependencies</code> with it. The import fails, so the build names every call site.</p><p><strong>What to do:</strong> use <code>Popover</code>.</p><h4 id="tooltip-—-vocabulary-aligned-with-popover-and-hovercard-breaking" tabindex="-1">Tooltip — vocabulary aligned with Popover and HoverCard (breaking) <a class="header-anchor" href="#tooltip-—-vocabulary-aligned-with-popover-and-hovercard-breaking" aria-label="Permalink to “Tooltip — vocabulary aligned with Popover and HoverCard (breaking)”">​</a></h4><ul><li><strong>Breaking, silent:</strong> the delay props on Tooltip, TooltipProvider and HoverCard now use milliseconds. For example, change <code>0.5</code> to <code>500</code>. Run <code>overlays-v1</code> to convert fixed values. It reports expressions it cannot convert, for you to check by hand.</li><li><strong>Breaking, silent:</strong> <code>placement</code> is renamed to <code>side</code>, like <code>Popover</code> and <code>HoverCard</code>. An unknown prop is ignored, so the tooltip keeps working and points at its default side.</li><li><strong>Breaking, silent:</strong> <code>arrowClass</code> is removed, because the library does not use props that inject classes. Style the arrow through <code>[data-slot=&quot;arrow&quot;]</code>. It was documented as the arrow&#39;s fill, but was mostly used to nudge the bubble&#39;s position, which the new <code>offset</code> prop does directly.</li><li>Added: <code>offset</code> sets the gap in px between the trigger and the bubble, like <code>Popover</code> and <code>HoverCard</code>. The bubble is no longer fixed at 4px.</li><li>Added: <code>[data-slot=&quot;content&quot;]</code>, <code>[data-slot=&quot;bubble&quot;]</code> and <code>[data-slot=&quot;arrow&quot;]</code> styling hooks.</li><li><strong>Breaking, silent:</strong> the <code>#body</code> slot is replaced by <code>#content</code>, which renders <em>inside</em> the bubble instead of replacing it. <code>#body</code> is not one of the library&#39;s standard slot names, and it had the wrong shape: it removed the bubble&#39;s surface. So six of the seven call sites in the apps copied <code>rounded bg-surface-gray-10 px-2 py-1 text-xs text-ink-base shadow-xl</code> by hand to put it back. Moving to <code>#content</code> usually means deleting that wrapper. Vue drops an unknown slot with no error, so a call site you missed shows an empty tooltip.</li><li>Added: <code>bare</code> renders <code>#content</code> without the bubble, for content that brings its own surface, such as an image preview. The arrow still renders. This is the proper way to do what <code>#body</code> was used for.</li><li><code>TooltipBubble</code> is no longer exported. It is the internal bubble shared by <code>Tooltip</code> and <code>Button</code>, with no call sites outside the library.</li><li><code>Tooltip</code> keeps <code>#default</code> as the <strong>trigger</strong>, on purpose. It is the one exception in the library to the standard slot names, and <code>PHILOSOPHY.md</code> records it. Over 200 call sites use the <code>&lt;Tooltip text=&quot;…&quot;&gt;&lt;Button /&gt;&lt;/Tooltip&gt;</code> shorthand, and renaming the slot would change every one of them for no change in behavior.</li></ul><p><strong>What to do:</strong> see the before/after in the <a href="/docs/migration#tooltip">migration guide</a>.</p><h4 id="hovercard-—-open-and-close-on-the-template-ref" tabindex="-1">HoverCard — <code>open()</code> and <code>close()</code> on the template ref <a class="header-anchor" href="#hovercard-—-open-and-close-on-the-template-ref" aria-label="Permalink to “HoverCard — open() and close() on the template ref”">​</a></h4><ul><li>Added: <code>open()</code> and <code>close()</code> on the component instance, like <code>Popover</code>.</li><li>The trigger slot&#39;s props are now typed (<code>HoverCardSlotProps</code>) instead of <code>any</code>.</li></ul><h4 id="bottomsheet-—-focus-stays-inside-an-open-sheet" tabindex="-1">BottomSheet — focus stays inside an open sheet <a class="header-anchor" href="#bottomsheet-—-focus-stays-inside-an-open-sheet" aria-label="Permalink to “BottomSheet — focus stays inside an open sheet”">​</a></h4><p>The sheet does not focus its first field on open, so it does not bring up the keyboard on a phone. That also left focus on the trigger behind the overlay, with nothing holding it, so <code>Tab</code> moved through the page behind an open modal. The sheet now takes focus itself on open. The keyboard still stays down.</p><h3 id="sidebar-tabs-and-navigation" tabindex="-1">Sidebar, tabs and navigation <a class="header-anchor" href="#sidebar-tabs-and-navigation" aria-label="Permalink to “Sidebar, tabs and navigation”">​</a></h3><h4 id="sidebaritem-attributes-land-on-the-link-or-the-button-breaking-silent" tabindex="-1"><code>SidebarItem</code> attributes land on the link or the button (breaking, silent) <a class="header-anchor" href="#sidebaritem-attributes-land-on-the-link-or-the-button-breaking-silent" aria-label="Permalink to “SidebarItem attributes land on the link or the button (breaking, silent)”">​</a></h4><p>A <code>SidebarItem</code> is a row container that holds the link or the button, with the <code>#suffix</code> area next to it. Every attribute you wrote on the component used to stop at that container, so <code>target</code>, <code>rel</code>, <code>id</code>, <code>title</code>, <code>data-*</code> and <code>aria-*</code> never reached the control. They now land on the control. <code>class</code>, <code>style</code> and event listeners stay on the container, which is what a row background, a drag-and-drop ring and a <code>@drop</code> over the suffix need.</p><ul><li><strong>Who is affected:</strong> apps that select a row by an attribute they pass in (<code>data-testid</code>, <code>id</code>), and apps that set <code>aria-label</code> on a row.</li></ul><p><strong>What to do:</strong> select the container with <code>[data-slot=&quot;sidebar-item&quot;]</code>, and the control with <code>[data-slot=&quot;sidebar-item&quot;] &gt; a</code> or <code>&gt; button</code>. An <code>aria-label</code> you pass now wins over the one taken from the label text, so drop any workaround for the old behavior.</p><h4 id="sidebar-is-the-nav-landmark-sections-are-groups" tabindex="-1"><code>Sidebar</code> is the <code>&lt;nav&gt;</code> landmark; sections are groups <a class="header-anchor" href="#sidebar-is-the-nav-landmark-sections-are-groups" aria-label="Permalink to “Sidebar is the &lt;nav&gt; landmark; sections are groups”">​</a></h4><p><code>Sidebar</code>&#39;s root element is a <code>&lt;nav&gt;</code>, labelled <code>Main</code>. The new <code>ariaLabel</code> prop changes the label. <code>SidebarSection</code>&#39;s body is a <code>&lt;div role=&quot;group&quot;&gt;</code>, not a nested <code>&lt;nav&gt;</code>, and it takes its accessible name from the section&#39;s heading. Before, only a <code>collapsible</code> section had a name.</p><ul><li><strong>Who is affected:</strong> apps whose sidebar body wraps rows in their own <code>&lt;nav&gt;</code>, and CSS or tests that select <code>nav</code> inside the sidebar.</li></ul><p><strong>What to do:</strong> turn the <code>&lt;nav&gt;</code> wrappers your app writes inside <code>Sidebar</code> into <code>div</code>s, so the page reports one navigation landmark. Select <code>[data-slot=&quot;sidebar-section&quot;] [role=&quot;group&quot;]</code> for a section body.</p><h4 id="sidebarheader-without-menuitems-is-no-longer-a-button" tabindex="-1"><code>SidebarHeader</code> without <code>menuItems</code> is no longer a button <a class="header-anchor" href="#sidebarheader-without-menuitems-is-no-longer-a-button" aria-label="Permalink to “SidebarHeader without menuItems is no longer a button”">​</a></h4><p>A header with no <code>menuItems</code> rendered a dropdown trigger anyway: it took focus, showed a hover background and a chevron, and opened an empty menu. It now renders a plain <code>&lt;div&gt;</code> with the same box, without the chevron and the tab stop. The logo and the title sit in the same place either way.</p><ul><li><strong>Who is affected:</strong> apps that style or query the header&#39;s inner <code>&lt;button&gt;</code>, or that relied on the chevron to signal a menu that was never there.</li></ul><p><strong>What to do:</strong> nothing, unless a selector targets that button. Pass <code>menuItems</code> to get the trigger back.</p><h4 id="sidebaritem-icon-takes-the-same-strings-as-every-other-icon-breaking-silent" tabindex="-1"><code>SidebarItem.icon</code> takes the same strings as every other icon (breaking, silent) <a class="header-anchor" href="#sidebaritem-icon-takes-the-same-strings-as-every-other-icon-breaking-silent" aria-label="Permalink to “SidebarItem.icon takes the same strings as every other icon (breaking, silent)”">​</a></h4><p><code>SidebarItem</code> rendered its <code>icon</code> prop through a private <code>SidebarItemIcon</code> component, which printed any string that was not a lucide name as text. It now uses the shared <code>Icon</code>, so a <code>lucide-*</code> class, an emoji and a component render exactly as before, at the same <code>size-4 text-ink-gray-6</code>.</p><ul><li><strong>Who is affected:</strong> rows with a plain-text <code>icon</code>, for example <code>icon=&quot;home&quot;</code> or <code>icon=&quot;AB&quot;</code>. The text used to show. It now renders nothing and warns once in development, the same as <code>Icon</code>, <code>Button</code> and <code>MobileNavItem</code>.</li></ul><p><strong>What to do:</strong> pass a <code>lucide-*</code> class for an icon. Put initials and other text in the <code>#prefix</code> slot: <code>&lt;SidebarItem&gt;&lt;template #prefix&gt;AB&lt;/template&gt;&lt;/SidebarItem&gt;</code>.</p><h4 id="tabs-—-a-clicked-tab-clears-when-the-route-leaves-its-page-fix" tabindex="-1">Tabs — a clicked tab clears when the route leaves its page (fix) <a class="header-anchor" href="#tabs-—-a-clicked-tab-clears-when-the-route-leaves-its-page-fix" aria-label="Permalink to “Tabs — a clicked tab clears when the route leaves its page (fix)”">​</a></h4><p>In route mode, a tab without a route that you click stays selected until the route moves. &quot;Moves&quot; used to mean &quot;a different tab matches&quot;. So opening a child route under one routed tab (<code>/inbox</code> → <code>/inbox/42</code>) left the clicked tab selected on a page it does not stand for. A navigation that lands on a different path now clears it too.</p><ul><li>A navigation that did not land keeps the clicked tab: one stopped by a guard, one cancelled by a newer navigation, or a duplicate of the URL already showing. A redirect clears it, because the redirect target lands.</li><li><strong>The trade-off:</strong> a navigation that changes only the query or the hash and keeps the same tab matched (<code>/inbox</code> → <code>/inbox?page=2</code>) no longer clears the click. A panel that keeps its own state in the URL makes exactly that change, and clearing there threw the user out of the panel they were in. A change of matched tab with no navigation behind it does still clear it: a trigger&#39;s <code>route</code> prop changing, or a routed trigger mounting that matches the current URL.</li><li>Tabs still works with no router installed. The listener is registered only when a router is present, and is removed on unmount.</li></ul><h4 id="tabbuttons-—-focus-on-the-component-ref" tabindex="-1">TabButtons — <code>focus()</code> on the component ref <a class="header-anchor" href="#tabbuttons-—-focus-on-the-component-ref" aria-label="Permalink to “TabButtons — focus() on the component ref”">​</a></h4><p>A template ref now exposes <code>focus(options?)</code>, the method every focusable control in the library shares. It focuses the selected option, or the first enabled one when nothing is selected. Disabled options are skipped, including <code>route</code> and <code>href</code> options that render disabled. A group with no options, or with every option disabled, does nothing. <code>FocusOptions</code> is passed through, <code>preventScroll</code> included. The type is exported as <code>TabButtonsExposed</code>.</p><h4 id="rail-renamed-to-sidebarrail-railitem-to-sidebarrailitem-breaking-loud" tabindex="-1"><code>Rail</code> renamed to <code>SidebarRail</code>, <code>RailItem</code> to <code>SidebarRailItem</code> (breaking) <a class="header-anchor" href="#rail-renamed-to-sidebarrail-railitem-to-sidebarrailitem-breaking-loud" aria-label="Permalink to “Rail renamed to SidebarRail, RailItem to SidebarRailItem (breaking)”">​</a></h4><p>The rail joins the Sidebar family by name. Nothing else changes. <code>SidebarRail</code> is still a bare frame that renders on its own or next to <code>Sidebar</code>. <code>Sidebar</code> does not own the rail layout, and the rail is not a collapsed mode of the sidebar. Props, slots and events are unchanged.</p><ul><li><code>RailItemProps</code> is now <code>SidebarRailItemProps</code>, and the root exports it the way it exports <code>SidebarItemProps</code>.</li><li><strong>Breaking, silent:</strong> the styling hooks follow the name. <code>data-slot=&quot;rail&quot;</code> becomes <code>&quot;sidebar-rail&quot;</code>, <code>rail-item</code> becomes <code>sidebar-rail-item</code>, and the same goes for <code>rail-item-indicator</code> and <code>rail-item-badge-dot</code>. A CSS rule written against an old value still parses. It just stops matching.</li><li><code>DesktopShell</code>&#39;s <code>#rail</code> slot keeps its name. It names a layout area, not the component that goes in it.</li></ul><p>There is no alias export. The import fails, so the build names every call site.</p><p><strong>What to do:</strong> find and replace the old names, as with every other component rename in v1. Update CSS selectors on the <code>data-slot</code> values.</p><h4 id="tabs-tabbuttons-—-side-is-now-edge-with-start-and-end-breaking-loud-in-ts-silent-in-js" tabindex="-1">Tabs, TabButtons — <code>side</code> is now <code>edge</code>, with <code>start</code> and <code>end</code> (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#tabs-tabbuttons-—-side-is-now-edge-with-start-and-end-breaking-loud-in-ts-silent-in-js" aria-label="Permalink to “Tabs, TabButtons — side is now edge, with start and end (breaking; loud in TS, silent in JS)”">​</a></h4><p>On a vertical <code>browser-tab</code> list, the prop that picks which edge of the list the tabs attach to is now <code>edge=&quot;start&quot; | &quot;end&quot;</code> instead of <code>side=&quot;left&quot; | &quot;right&quot;</code>. <code>start</code> and <code>end</code> follow text direction: <code>start</code> is the left edge in left-to-right text and the right edge in right-to-left text. The type <code>TabsSide</code> is now <code>TabsEdge</code>.</p><p><strong>What to do:</strong> replace <code>side=&quot;left&quot;</code> with <code>edge=&quot;start&quot;</code> and <code>side=&quot;right&quot;</code> with <code>edge=&quot;end&quot;</code>. In JS, the old prop is ignored and the tabs attach to the start edge.</p><h4 id="tabbuttons-class-on-an-option-is-replaced-by-data-value-breaking-silent-in-js" tabindex="-1">TabButtons: <code>class</code> on an option is replaced by <code>data-value</code> (breaking, silent in JS) <a class="header-anchor" href="#tabbuttons-class-on-an-option-is-replaced-by-data-value-breaking-silent-in-js" aria-label="Permalink to “TabButtons: class on an option is replaced by data-value (breaking, silent in JS)”">​</a></h4><p>The library customizes through slots and <code>data-*</code> attributes, never through props that take class names. <code>class</code> on a <code>TabButton</code> option was the last per-item class field in the library, and it added two more names to the frozen API.</p><ul><li>The tab button now renders <code>:data-value</code>, so CSS can target one tab: <code>[data-slot=&quot;tab-button&quot;][data-value=&quot;open&quot;] { … }</code>. This was not possible before: the rendered button carried <code>data-slot</code>, <code>data-state</code> and <code>data-disabled</code>, but no per-value hook.</li><li><strong>Breaking, silent in JS:</strong> <code>class</code> on an option object stops applying. A JavaScript caller keeps compiling and loses the styling with no warning.</li><li><strong>Breaking, loud:</strong> the <code>NativeButtonClass</code> type is no longer exported.</li><li><strong>Breaking:</strong> <code>customClass</code> is removed from the <code>#prefix</code> and <code>#suffix</code> slot props. This is loud if you destructure it, and silent if you spread it.</li></ul><p>The composed <code>Tabs</code> family is unchanged. There you write the <code>&lt;TabTrigger&gt;</code> yourself, so a class goes on the element directly.</p><p><strong>What to do:</strong> move the styling to a <code>[data-value=&quot;…&quot;]</code> selector. See the <a href="/docs/migration#tabbuttons-class">migration guide</a> for the before/after.</p><h4 id="sidebar-—-deprecated-config-api-removed-breaking" tabindex="-1">Sidebar — deprecated config API removed (breaking) <a class="header-anchor" href="#sidebar-—-deprecated-config-api-removed-breaking" aria-label="Permalink to “Sidebar — deprecated config API removed (breaking)”">​</a></h4><p>Per ADR-0008, every member marked <code>@deprecated</code> is deleted. <code>Sidebar</code> is now a bare frame that you compose: <code>SidebarHeader</code> / <code>SidebarSection</code> / <code>SidebarLabel</code> / <code>SidebarItem</code> go in the default slot, as agreed in <code>v1-release/plan.md</code>.</p><ul><li><strong>Breaking, silent:</strong> <code>Sidebar</code>&#39;s <code>header</code> and <code>sections</code> config-object props are gone. Old code still compiles, because Vue drops them as unused attributes, but the sidebar renders empty instead of the configured header and sections. Compose <code>SidebarHeader</code> and <code>SidebarLabel</code> + <code>SidebarItem</code> (or <code>SidebarSection</code>) directly in the default slot.</li><li><strong>Breaking, silent:</strong> <code>Sidebar</code>&#39;s <code>#header-logo</code> and <code>#footer-items</code> slots are gone. They only existed to reach into the config-object layout. Old <code>&lt;template #header-logo&gt;</code> / <code>#footer-items&gt;</code> content stops rendering. Put that markup directly in the default slot.</li><li><strong>Breaking, silent:</strong> <code>SidebarSection</code>&#39;s <code>items</code> prop and <code>#sidebar-item</code> scoped slot are gone. It is now a plain collapsible group (<code>label</code>, <code>collapsible</code>, <code>v-model:collapsed</code>) whose children are <code>SidebarItem</code>s you put directly in its default slot. Before, it took an <code>items</code> array plus a slot to customize each row.</li><li><strong>Breaking, silent:</strong> <code>SidebarItemProps.isActive</code> (an alias for <code>active</code>) and <code>.condition</code> (a config-object visibility filter) are gone. Use <code>active</code>. Use <code>v-if</code> on the composed <code>SidebarItem</code> instead of <code>condition</code>.</li><li><strong>Breaking, silent:</strong> <code>SidebarHeader</code>&#39;s <code>#logo</code> slot is renamed to <code>#prefix</code>, because the library uses generic slot names when one covers the case. Old <code>&lt;template #logo&gt;</code> content stops rendering, and the default logo/initial box shows instead.</li><li><code>SidebarItem</code>&#39;s icon in the collapsed rail no longer jumps to a centered square and back while the sidebar&#39;s width animates. It holds one position through the transition. This also fixes the icon sitting 2px off the rail&#39;s center line.</li><li><code>SidebarSection</code>&#39;s collapsible label is now a real <code>&lt;button&gt;</code> with <code>aria-expanded</code> / <code>aria-controls</code>, and works from the keyboard. It was a <code>&lt;div&gt;</code> with a click handler and no keyboard path.</li></ul><p><strong>What to do:</strong> see the <a href="/docs/migration#sidebar">migration guide</a>.</p><h4 id="navigation-destinations-use-route-and-href-breaking" tabindex="-1">Navigation destinations use <code>route</code> and <code>href</code> (breaking) <a class="header-anchor" href="#navigation-destinations-use-route-and-href-breaking" aria-label="Permalink to “Navigation destinations use route and href (breaking)”">​</a></h4><p>Router destinations are now named <code>route</code> on <code>ListRow</code>, <code>SidebarItem</code>, <code>SidebarRailItem</code> and <code>MobileNavItem</code>. Plain external URLs use <code>href</code>, and that includes the former Button <code>link</code> prop.</p><p>TabButton options keep <code>route</code>, <code>href</code> and <code>onClick</code>, require a string <code>label</code>, and no longer accept <code>tooltip</code>. Move extra help text into UI your app owns.</p><p><strong>What to do:</strong> run <code>npx destinations-v1 .</code>. It rewrites template props with fixed names on components imported from <code>frappe-ui</code>. Then check by hand: globally registered components, option objects, render functions, and <code>v-bind</code> spreads. See the <a href="/docs/migration#navigation-destinations">migration guide</a>.</p><h4 id="pageheaderbackbutton-—-to-is-now-fallbackroute" tabindex="-1">PageHeaderBackButton — <code>to</code> is now <code>fallbackRoute</code> (breaking) <a class="header-anchor" href="#pageheaderbackbutton-—-to-is-now-fallbackroute" aria-label="Permalink to “PageHeaderBackButton — to is now fallbackRoute (breaking)”">​</a></h4><p><code>fallbackRoute</code> is used only when there is no in-app history to go back to, such as a cold load onto a deep link. Every other tap goes back through history. The old <code>to</code> prop is removed.</p><p>A back button that always lands on one fixed route is not a back button. It sends the user wherever the page author guessed they came from, which is wrong for every other way into the page.</p><p><strong>What to do:</strong> rename <code>to</code> to <code>fallbackRoute</code> if it already named the page users came from. If you need a push that always happens, use a plain <code>Button</code> with your own <code>router.push</code>.</p><h3 id="app-shell-page-header-and-color-scheme" tabindex="-1">App shell, page header and color scheme <a class="header-anchor" href="#app-shell-page-header-and-color-scheme" aria-label="Permalink to “App shell, page header and color scheme”">​</a></h3><h4 id="themeswitcher-—-moved-to-frappe-ui-experimental-breaking-loud" tabindex="-1">ThemeSwitcher — moved to <code>frappe-ui/experimental</code> (breaking, loud) <a class="header-anchor" href="#themeswitcher-—-moved-to-frappe-ui-experimental-breaking-loud" aria-label="Permalink to “ThemeSwitcher — moved to frappe-ui/experimental (breaking, loud)”">​</a></h4><p><code>ThemeSwitcher</code> is not being brought up to the v1 quality bar at the root for <code>1.0.0</code> (#1094). It moves to <code>frappe-ui/experimental</code>, which has no stability promise. It stays deprecated there while apps migrate.</p><ul><li><strong>Breaking, loud:</strong> <code>import { ThemeSwitcher } from &#39;frappe-ui&#39;</code> fails to resolve. Import <code>ThemeSwitcher</code> and the type <code>ThemeSwitcherProps</code> from <code>frappe-ui/experimental</code> instead.</li><li><strong>The replacement covers the behavior, not the look.</strong> <code>ThemeSwitcher</code> renders a reka-ui <code>RadioGroupRoot</code> of theme preview cards. A <code>Select</code> bound to the <code>useColorScheme</code> composable replaces the behavior, not the markup, so an app that wants the cards has to rebuild them. Changing the import is the smaller change and keeps the current UI.</li></ul><p><strong>What to do:</strong> change the import, or build your switcher from <code>Select</code> and <code>useColorScheme</code>. See the before/after in the <a href="/docs/migration#themeswitcher">migration guide</a>.</p><h4 id="shells-—-a-page-reads-the-shell-it-is-inside-fix" tabindex="-1">Shells — a page reads the shell it is inside (fix) <a class="header-anchor" href="#shells-—-a-page-reads-the-shell-it-is-inside-fix" aria-label="Permalink to “Shells — a page reads the shell it is inside (fix)”">​</a></h4><p><code>DesktopShell</code> and <code>MobileShell</code> now pass their scroll element and their <code>PageHeaderTarget</code> to everything they render, and a page prefers the shell above it over the module-level registry. No caller code changes.</p><p>The registries answer &quot;the shell that mounted most recently&quot;. That is the wrong answer while two shells are mounted at once: during a desktop-to-mobile swap, or in a test that mounts both. A <code>PageHeader</code> could teleport into the other frame, and <code>useShellScrolled()</code> could track the other scroll element. The registries stay as the fallback for code that <code>inject</code> cannot reach: a router <code>scrollBehavior</code>, a navigation guard, a header teleported out of the shell. <code>shellScrollContainer</code> itself is unchanged.</p><h4 id="desktopshell-—-scroll-false-additive" tabindex="-1"><code>DesktopShell</code> — <code>:scroll=&quot;false&quot;</code> (additive) <a class="header-anchor" href="#desktopshell-—-scroll-false-additive" aria-label="Permalink to “DesktopShell — :scroll=&quot;false&quot; (additive)”">​</a></h4><p><code>scroll</code> defaults to <code>true</code>. Pass <code>false</code> and the content area fills the remaining height and never scrolls as a page. This is for layouts whose panes handle their own overflow: a list-and-detail split, or a board where each column scrolls. Apps used to fake this with <code>absolute inset-0</code>, a hardcoded <code>h-[calc(100vh-3rem)]</code>, or <code>[&amp;&gt;div]:h-full</code>.</p><p>With <code>:scroll=&quot;false&quot;</code> the shell has no scroll element, so <code>shellScrollContainer</code> is <code>null</code> and <code>useShellScrolled()</code> stays <code>false</code>. <code>DesktopShellProps</code> is exported and now includes the prop.</p><h4 id="useshellscrolled-—-threshold-is-required-breaking-loud-in-ts" tabindex="-1"><code>useShellScrolled</code> — <code>threshold</code> is required (breaking, loud in TS) <a class="header-anchor" href="#useshellscrolled-—-threshold-is-required-breaking-loud-in-ts" aria-label="Permalink to “useShellScrolled — threshold is required (breaking, loud in TS)”">​</a></h4><p><code>useShellScrolled()</code> with no argument is a type error. At runtime it warns in development and stays <code>false</code>. The old default was 200px, which suits a long document and nothing else: a header border that appears 200px late looks like a bug, not like a missing argument.</p><p><strong>What to do:</strong> pass <code>{ threshold: 12 }</code>.</p><h4 id="scrollbar-—-no-longer-exported-breaking-loud" tabindex="-1"><code>ScrollBar</code> — no longer exported (breaking, loud) <a class="header-anchor" href="#scrollbar-—-no-longer-exported-breaking-loud" aria-label="Permalink to “ScrollBar — no longer exported (breaking, loud)”">​</a></h4><p><code>import { ScrollBar } from &#39;frappe-ui&#39;</code> fails, and so does <code>ScrollBarProps</code>. There is no replacement. <code>ScrollArea</code> draws its own scrollbars, and <code>ScrollBar</code> only worked inside reka-ui&#39;s <code>ScrollAreaRoot</code>, which frappe-ui does not export. So the export named a component you could not use.</p><p><code>ScrollArea.viewportClass</code> stays. It is the one class-name prop the library ships, and the documented exception to the rule against class-name props in <code>PHILOSOPHY.md</code>: the scrolling viewport is an element reka-ui owns inside the root, so a <code>class</code> on the root cannot reach it.</p><p><strong>What to do:</strong> use <code>ScrollArea</code> and its <code>orientation</code> prop. <code>orientation=&quot;both&quot;</code> renders one scrollbar per axis.</p><h4 id="usesheetdrag-—-no-longer-exported-breaking-loud" tabindex="-1"><code>useSheetDrag</code> — no longer exported (breaking, loud) <a class="header-anchor" href="#usesheetdrag-—-no-longer-exported-breaking-loud" aria-label="Permalink to “useSheetDrag — no longer exported (breaking, loud)”">​</a></h4><p><code>useSheetDrag</code>, <code>UseSheetDrag</code> and <code>UseSheetDragOptions</code> leave the root. <code>BottomSheet</code> still uses the composable internally and is unchanged. There is no standalone replacement: the drag thresholds are constants tuned for that one component. It can come back when a second component needs it.</p><h4 id="documented-not-changed-shell-slot-names" tabindex="-1">Documented, not changed: shell slot names <a class="header-anchor" href="#documented-not-changed-shell-slot-names" aria-label="Permalink to “Documented, not changed: shell slot names”">​</a></h4><p><code>DesktopShell</code> keeps <code>#rail</code> and <code>#sidebar</code>. <code>MobileShell</code> keeps <code>#nav</code>. The names describe areas of the screen, not components. The desktop frame has two side areas that can appear together, and the mobile frame has one bar along the bottom, so one shared name would have to mean three things at once.</p><h4 id="resolvedcolorscheme-is-a-ref-on-usecolorscheme-breaking-loud" tabindex="-1"><code>resolvedColorScheme</code> is a ref on <code>useColorScheme()</code> (breaking, loud) <a class="header-anchor" href="#resolvedcolorscheme-is-a-ref-on-usecolorscheme-breaking-loud" aria-label="Permalink to “resolvedColorScheme is a ref on useColorScheme() (breaking, loud)”">​</a></h4><p><code>import { resolvedColorScheme } from &#39;frappe-ui&#39;</code> fails. The same name now comes from <code>useColorScheme()</code> as a read-only <code>Ref&lt;&#39;light&#39; | &#39;dark&#39;&gt;</code>. So an app reads <code>.value</code> instead of calling a function, and drops the <code>MutationObserver</code> it needed to know when to call it again. The ref follows <code>setColorScheme</code>, and follows the OS setting while the preference is <code>system</code>.</p><p><code>colorScheme</code> is the preference and can be <code>system</code>. <code>resolvedColorScheme</code> is what the page shows. <code>ColorScheme</code> and <code>ResolvedColorScheme</code> stay exported. Internally the function is <code>getResolvedColorScheme</code>, which charts use to pick a palette outside a component. It is not part of the package&#39;s public API.</p><p>There is no codemod, because a call has to become a <code>.value</code> read, and that depends on the code around it.</p><p><strong>What to do:</strong> replace <code>resolvedColorScheme()</code> with <code>useColorScheme().resolvedColorScheme.value</code>, and delete your observer.</p><h4 id="useresolvedcolorscheme-reads-the-scheme-without-owning-it-addition" tabindex="-1"><code>useResolvedColorScheme()</code> reads the scheme without owning it (addition) <a class="header-anchor" href="#useresolvedcolorscheme-reads-the-scheme-without-owning-it-addition" aria-label="Permalink to “useResolvedColorScheme() reads the scheme without owning it (addition)”">​</a></h4><p>A new root export for a page that must not write <code>data-theme</code>: an app that applies its own theme before paint, a page inside a host shell, or a demo in an iframe. It returns the same read-only <code>Ref&lt;&#39;light&#39; | &#39;dark&#39;&gt;</code>, read from the document and kept current. It writes no attribute, no class and no storage key. <code>useColorScheme()</code> applies the saved preference on its first call, so calling it only to read creates a second writer.</p><p>Nothing changes for an app that owns the scheme: keep reading <code>useColorScheme().resolvedColorScheme</code>.</p><h4 id="togglecolorscheme-flips-what-is-on-screen-breaking-silent" tabindex="-1"><code>toggleColorScheme()</code> flips what is on screen (breaking, silent) <a class="header-anchor" href="#togglecolorscheme-flips-what-is-on-screen-breaking-silent" aria-label="Permalink to “toggleColorScheme() flips what is on screen (breaking, silent)”">​</a></h4><p>It used to read the stored preference. So under <code>system</code> on a dark OS, it wrote <code>dark</code>, the value the page already showed, and the first press did nothing visible. It now reads the resolved value, so every press changes the scheme. Apps that shipped their own toggle for this reason can delete it.</p><h4 id="mobile-header-height-—-removed-breaking-silent" tabindex="-1"><code>--mobile-header-height</code> — removed (breaking, silent) <a class="header-anchor" href="#mobile-header-height-—-removed-breaking-silent" aria-label="Permalink to “--mobile-header-height — removed (breaking, silent)”">​</a></h4><p><code>PageHeaderMobile</code> is 52px tall, fixed, and reads no CSS variable for its height. Setting <code>--mobile-header-height</code> changes nothing in the library. Your app can keep using the name for its own rules. The variable had no prefix, no docs, and was not named after a component, which is the kind of variable ADR-0017 exists to prevent.</p><p>The internal title inset follows the ADR&#39;s naming for internal variables and is now <code>--_page-header-mobile-title-inset</code>.</p><p><strong>What to do:</strong> for a header of another height, use <code>PageHeaderBase</code> with your own class.</p><h4 id="pageheader-—-every-prop-type-has-a-name-additive" tabindex="-1">PageHeader — every prop type has a name (additive) <a class="header-anchor" href="#pageheader-—-every-prop-type-has-a-name-additive" aria-label="Permalink to “PageHeader — every prop type has a name (additive)”">​</a></h4><p><code>PageHeaderTitleProps</code>, <code>PageHeaderMobileProps</code>, <code>PageHeaderMobileTitleProps</code> and <code>PageHeaderBackButtonProps</code> are exported from the root, so a wrapper component can extend them. <code>PageHeader</code>, <code>PageHeaderBase</code> and <code>PageHeaderTarget</code> take no props and get no empty interface.</p><h4 id="documented-not-changed-header-click-to-top" tabindex="-1">Documented, not changed: header click-to-top <a class="header-anchor" href="#documented-not-changed-header-click-to-top" aria-label="Permalink to “Documented, not changed: header click-to-top”">​</a></h4><p>A single click on the empty area of the header scrolls the page to the top. The attributes that turn this off keep their names: <code>data-no-scroll-top</code> on <code>PageHeader</code>, and <code>data-no-sheet-drag</code> on <code>BottomSheet</code>. Both are now documented. There is no <code>data-fui-</code> prefix rule for data attributes.</p><h4 id="root-composables-and-directives-—-renamed-and-shrunk" tabindex="-1">Root composables and directives — renamed and shrunk (breaking, loud) <a class="header-anchor" href="#root-composables-and-directives-—-renamed-and-shrunk" aria-label="Permalink to “Root composables and directives — renamed and shrunk (breaking, loud)”">​</a></h4><p>Every change below is a <strong>loud break</strong>: the import fails, so the build, the type-check or the dev server tells you. There are no silent behavior changes, and none of these needs a before/after in the migration guide.</p><p><strong><code>useTheme</code> is now <code>useColorScheme</code>.</strong> <code>theme</code> means color tone everywhere else in the library (<code>theme=&quot;blue&quot;</code> on a Button, ~300 sites), so the light/dark composable stops using the word.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">// before</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">useTheme</span><span class="s_1jjt6x">,</span><span class="s_50ecpt"> type</span><span class="s_4q1z3w"> Theme</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"><span class="s_252irl">const</span><span class="s_1jjt6x"> {</span><span class="s_295sjd"> currentTheme</span><span class="s_1jjt6x">,</span><span class="s_295sjd"> setTheme</span><span class="s_1jjt6x">,</span><span class="s_295sjd"> toggleTheme</span><span class="s_1jjt6x"> }</span><span class="s_2ekfrt"> =</span><span class="s_indoxt"> useTheme</span><span class="s_13ahmt">()</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">// after</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">useColorScheme</span><span class="s_1jjt6x">,</span><span class="s_50ecpt"> type</span><span class="s_4q1z3w"> ColorScheme</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"><span class="s_252irl">const</span><span class="s_1jjt6x"> {</span><span class="s_295sjd"> colorScheme</span><span class="s_1jjt6x">,</span><span class="s_295sjd"> setColorScheme</span><span class="s_1jjt6x">,</span><span class="s_295sjd"> toggleColorScheme</span><span class="s_1jjt6x"> }</span><span class="s_2ekfrt"> =</span><span class="s_indoxt"> useColorScheme</span><span class="s_13ahmt">()</span></span></code></pre></div><ul><li><code>colorScheme</code> is <strong>read-only</strong>. Assigning to the old <code>currentTheme</code> ref changed the ref without setting <code>data-theme</code> or <code>localStorage</code>, so the app silently fell out of sync. Use <code>setColorScheme</code>.</li><li><code>initializeTheme</code> and <code>getSystemTheme</code> are gone. <code>useColorScheme()</code> already restores the saved preference and follows the OS on its first call.</li><li><strong>The <code>data-theme</code> attribute and the <code>theme</code> <code>localStorage</code> key are unchanged.</strong> App CSS that targets <code>[data-theme=&#39;dark&#39;]</code>, and users&#39; saved preferences, keep working.</li></ul><p><strong>Nine scroll members become two.</strong></p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">// before</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">activeScrollContainer</span><span class="s_1jjt6x">,</span><span class="s_4q1z3w"> useScrollContainer</span><span class="s_1jjt6x">,</span><span class="s_4q1z3w"> scrollToTop</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"><span class="s_252irl">const</span><span class="s_1jjt6x"> {</span><span class="s_295sjd"> isScrolled</span><span class="s_1jjt6x"> }</span><span class="s_2ekfrt"> =</span><span class="s_indoxt"> useScrollContainer</span><span class="s_13ahmt">({</span><span class="s_r4oegk"> threshold</span><span class="s_1jjt6x">:</span><span class="s_40mev6"> 12</span><span class="s_13ahmt"> })</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">// after</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">shellScrollContainer</span><span class="s_1jjt6x">,</span><span class="s_4q1z3w"> useShellScrolled</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"><span class="s_252irl">const</span><span class="s_295sjd"> scrolled</span><span class="s_2ekfrt"> =</span><span class="s_indoxt"> useShellScrolled</span><span class="s_13ahmt">({</span><span class="s_r4oegk"> threshold</span><span class="s_1jjt6x">:</span><span class="s_40mev6"> 12</span><span class="s_13ahmt"> })</span></span>
<span class="line"><span class="s_22m8k2">shellScrollContainer</span><span class="s_1jjt6x">.</span><span class="s_11933w">value</span><span class="s_1jjt6x">?.</span><span class="s_indoxt">scrollTo</span><span class="s_13ahmt">({</span><span class="s_r4oegk"> top</span><span class="s_1jjt6x">:</span><span class="s_40mev6"> 0</span><span class="s_1jjt6x">,</span><span class="s_r4oegk"> behavior</span><span class="s_1jjt6x">:</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">smooth</span><span class="s_w1p9wo">&#39;</span><span class="s_13ahmt"> })</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Removed</th><th>Use instead</th></tr></thead><tbody><tr><td><code>activeScrollContainer</code></td><td><code>shellScrollContainer</code></td></tr><tr><td><code>useScrollContainer().isScrolled</code></td><td><code>useShellScrolled({ threshold })</code></td></tr><tr><td><code>useScrollContainer().el</code></td><td><code>shellScrollContainer</code></td></tr><tr><td><code>getScrollContainer()</code></td><td><code>shellScrollContainer.value</code> (works outside <code>setup()</code> too)</td></tr><tr><td><code>scrollTo(o)</code></td><td><code>shellScrollContainer.value?.scrollTo(o)</code></td></tr><tr><td><code>scrollToTop()</code></td><td><code>shellScrollContainer.value?.scrollTo({ top: 0, behavior: &#39;smooth&#39; })</code></td></tr><tr><td><code>registerScrollContainer</code> / <code>unregisterScrollContainer</code></td><td>internal to <code>DesktopShell</code> / <code>MobileShell</code></td></tr><tr><td><code>UseScrollContainer</code>, <code>UseScrollContainerOptions</code></td><td>no replacement needed</td></tr></tbody></table><p>The <code>shell</code> prefix is on purpose: both work only while a <code>DesktopShell</code> or <code>MobileShell</code> is mounted. <code>useShellScrolled</code> now warns once in development when no shell is registered, instead of silently reporting <code>false</code> forever.</p><p><strong>Directives are <code>vFocus</code> and <code>vOnOutsideClick</code>.</strong> <code>&lt;script setup&gt;</code> registers a directive automatically only when the binding is named <code>vFoo</code>, so the old names forced a manual alias at every call site.</p><div class="language-vue"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">&lt;!-- before --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">script</span><span class="s_1i4ay4"> setup</span><span class="s_1zd9e2">&gt;</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_22m8k2">onOutsideClickDirective</span><span class="s_2ekfrt"> as</span><span class="s_4q1z3w"> vOnOutsideClick</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;/</span><span class="s_1uuh8p">script</span><span class="s_1zd9e2">&gt;</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">&lt;!-- after --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">script</span><span class="s_1i4ay4"> setup</span><span class="s_1zd9e2">&gt;</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">vOnOutsideClick</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;/</span><span class="s_1uuh8p">script</span><span class="s_1zd9e2">&gt;</span></span></code></pre></div><p><code>visibilityDirective</code> is removed with no replacement (0 call sites). Use an <code>IntersectionObserver</code> directly, or <code>@vueuse/core</code>&#39;s <code>useIntersectionObserver</code>.</p><p><strong><code>useScreenSize</code>, <code>useIsMobile</code> and <code>ScreenSize</code> are no longer exported.</strong> They were a thin wrapper over a <code>resize</code> listener that the library itself never used. Copy the ~20 lines into your app, or use <code>@vueuse/core</code>&#39;s <code>useWindowSize</code> / <code>useMediaQuery</code>.</p><h4 id="pagemetaplugin-—-removed" tabindex="-1">pageMetaPlugin — removed (breaking) <a class="header-anchor" href="#pagemetaplugin-—-removed" aria-label="Permalink to “pageMetaPlugin — removed (breaking)”">​</a></h4><ul><li><strong>Breaking, loud:</strong> <code>pageMetaPlugin</code> is no longer exported, so <code>import { pageMetaPlugin } from &#39;frappe-ui&#39;</code> fails. Delete <code>app.use(pageMetaPlugin)</code>.</li><li><strong>Silent break:</strong> <code>pageMetaPlugin</code> and the global mixin it installed are gone. A leftover <code>pageMeta()</code> component option still compiles, but nothing reads it, so <code>document.title</code> and the favicon quietly stop updating. See the <a href="/docs/migration#pagemetaplugin-removed">migration guide</a>.</li><li><code>usePageMeta</code> is unchanged, and now exports its <code>PageMeta</code> type.</li></ul><p><strong>What to do:</strong> move <code>pageMeta()</code> options to <code>usePageMeta</code>.</p><h4 id="app-shell-family-—-brought-to-bar" tabindex="-1">App shell family — docs, tests and slot renames (breaking) <a class="header-anchor" href="#app-shell-family-—-brought-to-bar" aria-label="Permalink to “App shell family — docs, tests and slot renames (breaking)”">​</a></h4><p><code>DesktopShell</code>, <code>MobileShell</code>, <code>MobileNav</code>, <code>PageHeader</code>, <code>ScrollArea</code> and <code>FrappeUIProvider</code> all keep their current exports and names. The rail was in this list until it was renamed to <code>SidebarRail</code>; see <a href="#rail-renamed-to-sidebarrail-railitem-to-sidebarrailitem-breaking-loud">the rename entry</a>.</p><ul><li>Every slot across the family now has a documented description. Each component has a docs page, a story, and Cypress tests (several had none).</li><li><strong>Breaking, silent:</strong> <code>PageHeaderMobile</code>&#39;s <code>#left</code>/<code>#right</code> slots and <code>PageHeaderMobileTitle</code>&#39;s <code>#icon</code> slot are renamed to the shared <code>#prefix</code>/<code>#suffix</code> names. <code>PHILOSOPHY.md</code> does not allow type-specific slots like <code>#icon</code> outside <code>Button</code>, and <code>#left</code>/<code>#right</code> were never among the standard names. Vue drops content passed to an unknown slot name with no error, so the old names do not warn. They just stop rendering. See the <a href="/docs/migration#pageheadermobile-family-slot-names">migration guide</a>.</li><li><code>ScrollArea</code> gets a <code>types.ts</code> (<code>ScrollAreaProps</code>, <code>ScrollAreaExposed</code>) and the styling hooks <code>data-slot=&quot;scroll-area&quot;</code> / <code>&quot;scroll-area-viewport&quot;</code> / <code>&quot;scroll-area-scrollbar&quot;</code> / <code>&quot;scroll-area-thumb&quot;</code>. It had none. <code>viewportElement</code> on the template ref is now typed through <code>ScrollAreaExposed</code>. (<code>SettingsDialog</code>&#39;s <code>SettingsBody</code> exposes the same shape, but it is typed by its own <code>SettingsBodyExposed</code>, not by <code>ScrollAreaExposed</code>; see <a href="#settingsdialog-—-settingsbody-s-exposed-type">its entry</a>.)</li><li><code>FrappeUIProvider</code>&#39;s source folder moved from <code>src/components/Provider</code> to <code>src/components/FrappeUIProvider</code>, to match its file name. This is internal only: <code>import { FrappeUIProvider } from &#39;frappe-ui&#39;</code> is not affected.</li><li><strong>Breaking:</strong> <code>FrappeUIProviderProps</code> is no longer exported. The component has no props, so the type was empty and was never wired to <code>defineProps</code>. Freezing it would lock in nothing. There were zero known consumers. The mismatched folder name had also hidden the whole component from the docs generator, so it had no docs page before.</li></ul><h3 id="keyboard-shortcuts" tabindex="-1">Keyboard shortcuts <a class="header-anchor" href="#keyboard-shortcuts" aria-label="Permalink to “Keyboard shortcuts”">​</a></h3><h4 id="useshortcut-renamed-to-usekeyboardshortcut-config-reshaped-breaking-loud-in-ts-silent-in-js" tabindex="-1"><code>useShortcut</code> renamed to <code>useKeyboardShortcut</code>, config reshaped (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#useshortcut-renamed-to-usekeyboardshortcut-config-reshaped-breaking-loud-in-ts-silent-in-js" aria-label="Permalink to “useShortcut renamed to useKeyboardShortcut, config reshaped (breaking; loud in TS, silent in JS)”">​</a></h4><p>The import fails to resolve, so the rename itself is loud. The config is the silent part: 14 fields become 10, and Vue drops the removed ones without a word.</p><ul><li><code>key</code> + <code>ctrl</code> + <code>shift</code> + <code>alt</code> become one <code>combo</code> string, written <code>Mod+Ctrl+Alt+Shift+&lt;Key&gt;</code>. <code>ctrl</code> never meant Control. It matched <code>ctrlKey || metaKey</code>, so <code>{ key: &#39;s&#39;, ctrl: true }</code> fired on ⌘S, on ⌃S and on Win+S alike. <code>Mod+S</code> compares every modifier exactly, so only ⌘S fires on macOS, and only Ctrl+S elsewhere. The two extra ways to trigger it stop.</li><li><code>condition</code> becomes <code>enabled</code>, and takes a ref, a getter or a boolean.</li><li><code>triggeredOn</code> is removed. <code>onHold</code> selects hold mode, and a hold registration takes no <code>handler</code>. This ends the old surprise where <code>triggeredOn: &#39;hold&#39;</code> fired <code>handler</code> too.</li><li>Punctuation and digits now use a key name (<code>Mod+Slash</code>, <code>Mod+Shift+Digit1</code>), because <code>+</code> is the separator and <code>&#39;Mod++&#39;</code> splits into empty parts. Digits and punctuation match <code>event.code</code>, so a shifted character still resolves. Letters and named keys match <code>event.key</code>. The old US-layout guesswork is deleted.</li></ul><p>TypeScript rejects an unknown combo at compile time. A JavaScript call site that still passes the v0 shape logs one warning in development and never fires.</p><p><strong>What to do:</strong> see the <a href="/docs/migration#useshortcut-is-now-usekeyboardshortcut"><code>useShortcut</code> migration</a>.</p><h4 id="shortcut-precedence-is-now-last-registered-wins-breaking-silent" tabindex="-1">Shortcut precedence is now last-registered-wins (breaking, silent) <a class="header-anchor" href="#shortcut-precedence-is-now-last-registered-wins-breaking-silent" aria-label="Permalink to “Shortcut precedence is now last-registered-wins (breaking, silent)”">​</a></h4><p>When two shortcuts share one combo, the first one the registry reached used to run. Now the last registration that is <strong>enabled at the time of the keypress</strong> wins. <code>enabled</code> is checked before precedence, so two registrations with mutually exclusive conditions both keep working. Suite&#39;s slides app uses that pattern on seven combos.</p><p>A real collision, where two live shortcuts match one keypress, logs one warning per combo in development. It names the hidden shortcut and the active one.</p><h4 id="formatshortcutlabel-and-getactiveshortcuts-removed-breaking-loud" tabindex="-1"><code>formatShortcutLabel</code> and <code>getActiveShortcuts</code> removed (breaking, loud) <a class="header-anchor" href="#formatshortcutlabel-and-getactiveshortcuts-removed-breaking-loud" aria-label="Permalink to “formatShortcutLabel and getActiveShortcuts removed (breaking, loud)”">​</a></h4><p>Neither had a consumer in ten apps, and <code>formatShortcutLabel</code> was not used inside the library either. Reading the registry is now internal. <code>KeyboardShortcutsDialog</code>&#39;s new default slot passes out the same data.</p><h4 id="keyboardshortcutsmodal-renamed-to-keyboardshortcutsdialog-breaking-loud" tabindex="-1"><code>KeyboardShortcutsModal</code> renamed to <code>KeyboardShortcutsDialog</code> (breaking, loud) <a class="header-anchor" href="#keyboardshortcutsmodal-renamed-to-keyboardshortcutsdialog-breaking-loud" aria-label="Permalink to “KeyboardShortcutsModal renamed to KeyboardShortcutsDialog (breaking, loud)”">​</a></h4><p>The library calls every modal a dialog. Props are unchanged. The component gains a default slot that carries the grouped shortcuts, and a <code>data-slot</code> on every part.</p><p><strong>What to do:</strong> rename the import. See the <a href="/docs/migration#keyboardshortcutsmodal-is-now-keyboardshortcutsdialog"><code>KeyboardShortcutsModal</code> migration</a>.</p><h4 id="keyboardshortcut-—-one-combo-vocabulary-and-data-slot-hooks-breaking-silent" tabindex="-1">KeyboardShortcut — one combo vocabulary, and <code>data-slot</code> hooks (breaking, silent) <a class="header-anchor" href="#keyboardshortcut-—-one-combo-vocabulary-and-data-slot-hooks-breaking-silent" aria-label="Permalink to “KeyboardShortcut — one combo vocabulary, and data-slot hooks (breaking, silent)”">​</a></h4><p>The root, each key, the <code>+</code> separators and the alternative combos carry a <code>data-slot</code>. The root also carries <code>data-bg</code> when <code>bg</code> is set. Style through those, not through a class prop. The parser also reads the key names a combo uses, so <code>Digit1</code> renders <code>1</code> and <code>Slash</code> renders <code>/</code>.</p><p><code>combo</code> now reads the one grammar that <code>useKeyboardShortcut</code> fires on. The older display-only spellings are gone: a chip for a combo that can never fire is the exact problem this family exists to remove. An unknown token renders as written and warns once in development.</p><p><strong>Silent break:</strong> <code>combo</code> stays typed <code>string</code>, because callers compute it. So no type-check finds the call sites, and production logs nothing.</p><table tabindex="0"><thead><tr><th>Gone</th><th>Write</th></tr></thead><tbody><tr><td><code>Cmd</code>, <code>Command</code>, <code>⌘</code>, <code>Meta</code></td><td><code>Mod</code></td></tr><tr><td><code>Control</code></td><td><code>Ctrl</code></td></tr><tr><td><code>Option</code>, <code>Opt</code>, <code>⌥</code></td><td><code>Alt</code></td></tr><tr><td><code>⇧</code></td><td><code>Shift</code></td></tr><tr><td><code>Win</code>, <code>Windows</code></td><td>nothing; the grammar has no Windows key</td></tr><tr><td><code>Esc</code></td><td><code>Escape</code></td></tr><tr><td><code>Return</code></td><td><code>Enter</code></td></tr><tr><td><code>Del</code></td><td><code>Delete</code></td></tr><tr><td><code>Up</code>, <code>Down</code>, <code>Left</code>, <code>Right</code></td><td><code>ArrowUp</code>, <code>ArrowDown</code>, <code>ArrowLeft</code>, <code>ArrowRight</code></td></tr><tr><td><code>=</code></td><td><code>Equal</code></td></tr><tr><td><code>F13</code> and above</td><td>nothing; the grammar stops at <code>F12</code></td></tr></tbody></table><p>The root&#39;s <code>role</code> is now <code>img</code> when <code>combo</code> is set, and absent otherwise. It was always <code>note</code> before. A labelled <code>img</code> replaces its content for screen readers, so a screen reader reads each key once, instead of once per chip.</p><p><code>useIcons</code> now also applies in <code>bg</code> mode, where it was ignored. <code>:use-icons=&quot;false&quot;</code> now drops the arrow, Enter, Backspace and Delete icons in both modes. The default is <code>true</code>, so a chip that never set the prop draws what it drew before.</p><p><strong>What to do:</strong> rewrite old spellings with the table above. See the <a href="/docs/migration#keyboardshortcut"><code>KeyboardShortcut</code> migration</a>.</p><h4 id="keyboardshortcut-—-deprecated-shortcut-and-unused-modifier-props-removed-breaking" tabindex="-1">KeyboardShortcut — deprecated <code>shortcut</code> and unused modifier props removed (breaking) <a class="header-anchor" href="#keyboardshortcut-—-deprecated-shortcut-and-unused-modifier-props-removed-breaking" aria-label="Permalink to “KeyboardShortcut — deprecated shortcut and unused modifier props removed (breaking)”">​</a></h4><p>Per ADR-0008, the deprecated <code>shortcut</code> prop (replaced by <code>combo</code>) is removed rather than frozen. It had zero call sites. The <code>meta</code> / <code>ctrl</code> / <code>shift</code> / <code>alt</code> boolean props are also removed: they had no real usage, and <code>combo</code> replaces them. Both are <strong>loud</strong> breaks (removed props on a typed component).</p><p>The component also moved from a bare <code>KeyboardShortcut.vue</code> into its own folder, with a <code>types.ts</code>, a docs page and a Cypress test.</p><h4 id="useshortcut-—-matchesshortcut-no-longer-public-breaking" tabindex="-1">useShortcut — <code>matchesShortcut</code> no longer public (breaking) <a class="header-anchor" href="#useshortcut-—-matchesshortcut-no-longer-public-breaking" aria-label="Permalink to “useShortcut — matchesShortcut no longer public (breaking)”">​</a></h4><p><code>matchesShortcut</code> is removed from the <code>frappe-ui</code> package export. Its own doc comment already said &quot;exported for unit tests only&quot;. It was never meant to be public API, and it had no real usage. This is a loud break (an import error) for anyone who imported it directly. There is no sign of any real consumer doing so. The composable itself is now <code>useKeyboardShortcut</code>; see <a href="#useshortcut-renamed-to-usekeyboardshortcut-config-reshaped-breaking-loud-in-ts-silent-in-js">the rename entry</a>.</p><h4 id="keyboardshortcutsmodal-useshortcut-—-brought-to-bar" tabindex="-1">KeyboardShortcutsModal / useShortcut — types, tests and docs <a class="header-anchor" href="#keyboardshortcutsmodal-useshortcut-—-brought-to-bar" aria-label="Permalink to “KeyboardShortcutsModal / useShortcut — types, tests and docs”">​</a></h4><p><code>KeyboardShortcutsModal</code> gained a <code>types.ts</code> and a Cypress test (it only had unit tests before). <code>useShortcut</code> gained a short entry on the <a href="/docs/other/composables">composables page</a>. Both were renamed later in this release, to <code>KeyboardShortcutsDialog</code> and <code>useKeyboardShortcut</code>. Read the entries at the top of this section for the API that ships.</p><h4 id="keyboardshortcut-—-only-between-two-text-keys" tabindex="-1">KeyboardShortcut — <code>+</code> only between two text keys <a class="header-anchor" href="#keyboardshortcut-—-only-between-two-text-keys" aria-label="Permalink to “KeyboardShortcut — + only between two text keys”">​</a></h4><p>A plain <code>KeyboardShortcut</code> drew a <code>+</code> between every pair of keys. <code>showPlus</code> now takes <code>&#39;auto&#39;</code>, the new default, which draws one only where two keys drawn as text meet, as in &quot;Ctrl+K&quot;. A key drawn as an icon, like ⌘, ⇧, ⌥, an arrow or ↵, needs none, so <code>Mod+K</code> renders &quot;⌘K&quot; on macOS and <code>Shift+F2</code> renders &quot;⇧F2&quot; everywhere. <code>show-plus</code> still draws every plus, and <code>:show-plus=&quot;false&quot;</code> none. <code>bg</code> chips never drew it.</p><h3 id="other-components" tabindex="-1">Other components <a class="header-anchor" href="#other-components" aria-label="Permalink to “Other components”">​</a></h3><h4 id="tree-—-expansion-moves-to-a-keyed-v-model-expanded-breaking-silent" tabindex="-1">Tree — expansion moves to a keyed <code>v-model:expanded</code> (breaking, silent) <a class="header-anchor" href="#tree-—-expansion-moves-to-a-keyed-v-model-expanded-breaking-silent" aria-label="Permalink to “Tree — expansion moves to a keyed v-model:expanded (breaking, silent)”">​</a></h4><p><code>expanded</code> was a boolean that expanded everything, and each node&#39;s open or closed state was the <code>expanded</code> field on the node object, which the component wrote (#1142). A library must not write to the data its caller passes in: that defeats <code>readonly</code> and frozen data, writes through to a store, and a shallow watcher never sees it.</p><p><code>expanded</code> is now <code>TreeKey[]</code>, the keys of the open nodes. It is replaced as a whole on every toggle. The per-node <code>expanded</code> field is gone. <code>expand</code>, <code>collapse</code>, <code>toggle</code>, <code>expandAll</code> and <code>collapseAll</code> on the component ref cover what the boolean did. Their type is exported as <code>TreeExposed</code>.</p><ul><li><strong>The default flips:</strong> an unbound tree used to open every node under <code>:expanded=&quot;true&quot;</code>. It now renders only its root nodes.</li><li><strong>Silent break:</strong> a node that keeps <code>expanded: true</code> still type-checks, because <code>TreeNode</code> has an index signature, and the node stays closed. Development mode warns about the leftover field, and about the removed boolean model.</li></ul><p><strong>What to do:</strong> bind <code>v-model:expanded</code> to an array of keys, or call <code>expandAll()</code> on the ref. See the <a href="/docs/migration#tree">migration guide</a>.</p><h4 id="base-component-contracts-for-v1-breaking" tabindex="-1">Base component contracts for v1 (breaking) <a class="header-anchor" href="#base-component-contracts-for-v1-breaking" aria-label="Permalink to “Base component contracts for v1 (breaking)”">​</a></h4><ul><li><code>Icon</code> adds the standard <code>icon</code> prop and keeps <code>name</code> fully supported. Both accept <code>string | Component | null</code>. <code>icon</code> wins when both are passed. No migration is needed.</li><li><code>Progress.intervals</code> is now the number of segments, and <code>intervalCount</code> is removed. Leave out <code>intervals</code> for a continuous bar. Labels and hints now render independently.</li><li><code>Badge.label</code> accepts only <code>string | number</code>. Put rich content in the default slot.</li><li><code>Divider.position</code> is replaced by <code>align</code>. An old <code>position</code> value is ignored, and the action falls back to the center. <code>DividerAction</code> now accepts the shared Button action fields.</li></ul><p><strong>What to do:</strong> the <a href="/docs/migration#base-component-props">base component migration guide</a> has the codemod and the manual steps.</p><h4 id="commandpalette-—-removed-from-the-root-export-rebuilt-in-frappe-ui-experimental-breaking-loud" tabindex="-1">CommandPalette — removed from the root export, rebuilt in <code>frappe-ui/experimental</code> (breaking, loud) <a class="header-anchor" href="#commandpalette-—-removed-from-the-root-export-rebuilt-in-frappe-ui-experimental-breaking-loud" aria-label="Permalink to “CommandPalette — removed from the root export, rebuilt in frappe-ui/experimental (breaking, loud)”">​</a></h4><p><code>CommandPalette</code> and <code>CommandPaletteItem</code> leave the root export. The family is rebuilt as seven parts you compose (<code>CommandPalette</code>, <code>CommandPaletteInput</code>, <code>CommandPaletteList</code>, <code>CommandPaletteGroup</code>, <code>CommandPaletteItem</code>, <code>CommandPaletteEmpty</code> and <code>CommandPaletteFooter</code>) in <code>frappe-ui/experimental</code>, which has no stability promise. It stays there until gameplan, helpdesk and this site all run on it.</p><p>Four apps copied and changed the old palette instead of using it, because it had one shape and no filtering. The new parts fit all four.</p><ul><li><strong>Breaking, loud:</strong> <code>import { CommandPalette } from &#39;frappe-ui&#39;</code> fails to resolve.</li><li><strong>Breaking:</strong> the <code>groups</code> prop is gone. Groups and items are markup now, so a group renders whatever it needs, without a <code>component</code> escape hatch.</li><li><strong>Breaking:</strong> <code>select</code> carries the value and the click that picked it. Call <code>event.preventDefault()</code> to keep the palette open.</li><li><strong>Behavior change:</strong> the palette filters against the query. The old one rendered <code>groups</code> as given and left filtering to the caller. <code>filterable</code> (default <code>true</code>) turns it off for server search. It is the same word <code>Combobox</code> and <code>MultiSelect</code> use (ADR-0009).</li><li><strong>Breaking:</strong> <code>Mod+K</code> moves to the caller. The old component registered it itself, and skipped it whenever a rich-text editor had focus, which put knowledge of the editor inside the palette.</li><li>Every part sets <code>data-slot</code>. An item sets <code>data-state=&quot;active&quot;</code> and <code>data-disabled</code>, and passes <code>active</code> and <code>disabled</code> to its slots.</li><li>Rows go inside <code>CommandPaletteList</code>, the only part that scrolls. A list may hold rows and groups and nothing else, so the input, the empty state and the footer sit next to it.</li><li><code>@headlessui/vue</code> leaves <code>dependencies</code>. The palette was its last import in the library.</li></ul><p><strong>What to do:</strong> see the before/after for each break in the <a href="/docs/migration#commandpalette">migration guide</a>.</p><h4 id="button-no-longer-hands-back-rootref-breaking-loud-in-ts-silent-in-js" tabindex="-1">Button no longer hands back <code>rootRef</code> (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#button-no-longer-hands-back-rootref-breaking-loud-in-ts-silent-in-js" aria-label="Permalink to “Button no longer hands back rootRef (breaking; loud in TS, silent in JS)”">​</a></h4><p><code>Button</code> exposed an untyped, writable template ref to its root element. ADR-0012 does not allow that: a component exposes a template ref only when a parent&#39;s script needs it and nothing else can reach it.</p><ul><li><strong>Breaking:</strong> <code>buttonRef.value.rootRef</code> is gone. In TypeScript it fails to compile. In JavaScript it reads <code>undefined</code>, so guard for it.</li><li>What it returned was never one thing. Depending on props, it was a <code>&lt;button&gt;</code>, an <code>&lt;a&gt;</code>, or a vue-router component instance, and you could assign to it.</li><li>Nothing replaces it. If you need control from script, ask for it: a typed <code>focus(options?)</code> can ship in a <code>1.x</code> minor release, and additions are cheap.</li></ul><h4 id="badge-—-theme-orange-removed-breaking-loud-in-ts-silent-in-js" tabindex="-1">Badge — <code>theme=&quot;orange&quot;</code> removed (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#badge-—-theme-orange-removed-breaking-loud-in-ts-silent-in-js" aria-label="Permalink to “Badge — theme=&quot;orange&quot; removed (breaking; loud in TS, silent in JS)”">​</a></h4><p><code>orange</code> was a deprecated alias that resolved to <code>amber</code>, so <code>theme=&quot;amber&quot;</code> renders exactly what <code>theme=&quot;orange&quot;</code> used to. ADR-0008 keeps nothing deprecated in <code>1.0.0</code>. The alias was never on the removal list, so no earlier survey counted it (found by #1054).</p><p>TypeScript call sites fail at <code>vue-tsc</code>, because the <code>theme</code> union no longer accepts the string. JavaScript call sites and bound values render the default <code>gray</code> theme, and log a one-time warning in development that names the component, prop and value. Check bound themes as well as literal attributes. A status-to-theme map that returns <code>&#39;orange&#39;</code>, or an <code>?? &#39;orange&#39;</code> default, turns grey, and a search for <code>theme=&quot;orange&quot;</code> does not find it.</p><p><strong>What to do:</strong> replace <code>orange</code> with <code>amber</code>, including in maps and defaults. See the <a href="/docs/migration#badge">migration guide</a>.</p><h4 id="badge-—-an-unsupported-theme-variant-or-size-no-longer-crashes-fix" tabindex="-1">Badge — an unsupported <code>theme</code>, <code>variant</code> or <code>size</code> no longer crashes (fix) <a class="header-anchor" href="#badge-—-an-unsupported-theme-variant-or-size-no-longer-crashes-fix" aria-label="Permalink to “Badge — an unsupported theme, variant or size no longer crashes (fix)”">​</a></h4><p><code>Badge</code> looked up a class map by theme, and then looked up that result by variant. A theme outside the union made the second lookup read a property of <code>undefined</code>, which threw <code>TypeError: Cannot read properties of undefined (reading &#39;subtle&#39;)</code> during render. The badge disappeared and the error took the parent render with it, so one old colour name in a status map could blank a page.</p><p>All three props now fall back to their defaults (<code>gray</code> / <code>subtle</code> / <code>md</code>) and warn once per bad value in development. <code>variant</code> and <code>size</code> never threw: their lookups ended early and rendered without tint or size. But they were silently wrong, and now they report themselves too.</p><p>The public types are unchanged. <code>theme</code> still accepts only the six supported values, so TypeScript still rejects anything else at compile time. The fallback is a runtime safety net for JavaScript call sites and bound values. It does not widen the API.</p><h4 id="sprite-icon-trio-—-moved-to-frappe-ui-experimental-breaking" tabindex="-1">Sprite icon trio — moved to <code>frappe-ui/experimental</code> (breaking) <a class="header-anchor" href="#sprite-icon-trio-—-moved-to-frappe-ui-experimental-breaking" aria-label="Permalink to “Sprite icon trio — moved to frappe-ui/experimental (breaking)”">​</a></h4><p>The sprite-based <code>Icon</code>, <code>IconPicker</code> and <code>spritePlugin</code> leave <code>frappe-ui/icons</code> (#904). Apps still use them, so they move to <code>frappe-ui/experimental</code>, which has no stability promise, instead of being deleted. <code>lucide-*</code> classes are the standard way to render icons. The named SFC icons (<code>CircleCheckIcon</code>, <code>HelpIcon</code>, ...) stay on <code>frappe-ui/icons</code>.</p><ul><li><strong>Breaking, loud:</strong><code>import { Icon, IconPicker, spritePlugin } from &#39;frappe-ui/icons&#39;</code> fails to resolve. Import them from <code>frappe-ui/experimental</code> instead. Changing the import path is the whole migration. Apps that spread <code>content</code> from <code>frappe-ui/tailwind</code> keep the <code>IconPicker</code> styles automatically, with no Tailwind change.</li><li><code>frappe-ui/experimental</code> exports <code>Icon</code> (sprite), and the root <code>frappe-ui</code> exports a different <code>Icon</code>. If you import both, rename one: <code>import { Icon as SpriteIcon } from &#39;frappe-ui/experimental&#39;</code>.</li></ul><p>See the <a href="/docs/migration#sprite-icons-—-moved-to-frappe-ui-experimental">migration guide</a>.</p><h4 id="circularprogressbar-—-removed" tabindex="-1">CircularProgressBar — removed (breaking, loud) <a class="header-anchor" href="#circularprogressbar-—-removed" aria-label="Permalink to “CircularProgressBar — removed (breaking, loud)”">​</a></h4><p><strong>Breaking:</strong> <code>CircularProgressBar</code> is no longer exported. The import fails. It was a second component for the same idea as <code>Progress</code>, with hardcoded light-mode colors and a structured <code>theme</code> object prop. One call site existed across all consumer apps.</p><p><strong>What to do:</strong> use <code>Progress</code>, or copy the old SFC into your app if you need the round form.</p><h4 id="feathericon-—-removed-breaking" tabindex="-1">FeatherIcon — removed (breaking) <a class="header-anchor" href="#feathericon-—-removed-breaking" aria-label="Permalink to “FeatherIcon — removed (breaking)”">​</a></h4><p>Per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>, the deprecated <code>FeatherIcon</code> component is deleted, along with the <code>feather-icons</code> dependency. <code>lucide-*</code> strings (or a <code>Component</code>) are now the only supported icon forms.</p><ul><li><strong>Breaking, loud:</strong> <code>import { FeatherIcon } from &#39;frappe-ui&#39;</code> and <code>&lt;FeatherIcon&gt;</code> fail at the import.</li><li><strong>Breaking, silent:</strong> every icon-name prop across the library (<code>Button.icon</code> / <code>iconLeft</code> / <code>iconRight</code>, <code>Dialog.icon</code>, <code>Dropdown</code> / <code>ContextMenu</code> item <code>icon</code>, <code>TabButtons</code> options <code>icon</code> / <code>iconLeft</code> / <code>iconRight</code>, <code>Icon.name</code>) used to fall back to <code>FeatherIcon</code> for a bare feather-style name (for example <code>&quot;edit&quot;</code>). That fallback is gone. An unrecognized string now renders nothing, with a console warning in development, once per component and prop.</li></ul><p><strong>What to do:</strong> add the <code>lucide-</code> prefix to the name.</p><div class="language-vue"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">&lt;!-- before --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">FeatherIcon</span><span class="s_1i4ay4"> name</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">plus</span><span class="s_w1p9wo">&quot;</span><span class="s_1i4ay4"> class</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">size-4</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2"> /&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">Button</span><span class="s_1i4ay4"> icon</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">plus</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2"> /&gt;</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">&lt;!-- after --&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">span</span><span class="s_1i4ay4"> class</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">lucide-plus size-4</span><span class="s_w1p9wo">&quot;</span><span class="s_1i4ay4"> aria-hidden</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">true</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2"> /&gt;</span></span>
<span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">Button</span><span class="s_1i4ay4"> icon</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_2575z4">lucide-plus</span><span class="s_w1p9wo">&quot;</span><span class="s_1zd9e2"> /&gt;</span></span></code></pre></div><p>The core components&#39; own hardcoded <code>FeatherIcon</code> usages were moved to <code>lucide-*</code> in this release.</p><p>See the before/after for the silent break in the <a href="/docs/migration#icons">migration guide</a>.</p><h4 id="card-listitem-standalone-toast-—-removed-breaking" tabindex="-1">Card, ListItem, standalone Toast — removed (breaking) <a class="header-anchor" href="#card-listitem-standalone-toast-—-removed-breaking" aria-label="Permalink to “Card, ListItem, standalone Toast — removed (breaking)”">​</a></h4><p>Per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>, three unmaintained wrappers that shipped <code>@deprecated</code> in code are deleted, not carried forward. All three had zero call sites across the survey of downstream apps.</p><ul><li><strong>Breaking:</strong> <code>Card</code> and its <code>.vue</code> file are removed. There is no drop-in replacement. Rebuild the title/subtitle/actions/loading layout with plain markup, and use <code>LoadingText</code> or <code>Skeleton</code> for the loading state.</li><li><strong>Breaking:</strong> <code>ListItem</code> and its <code>.vue</code> file are removed. There is no drop-in replacement. Rebuild the title/subtitle/actions row with plain markup.</li><li><strong>Breaking:</strong> the standalone <code>Toast</code> SFC (<code>import { Toast } from &#39;frappe-ui&#39;</code>) is removed. This only affects direct use of the raw <code>ToastRoot</code>-based component. The imperative API (<code>toast()</code> / <code>toast.success()</code> / <code>toast.error()</code> / <code>toast.info()</code>) and <code>&lt;ToastProvider&gt;</code> are not affected.</li></ul><p>All three fail loudly at the import. The <a href="/docs/migration#card-listitem-standalone-toast-removed">migration guide</a> has before/after examples.</p><h4 id="loadingindicator-loadingtext-—-moved-to-component-directories-non-breaking" tabindex="-1">LoadingIndicator / LoadingText — moved to component directories (non-breaking) <a class="header-anchor" href="#loadingindicator-loadingtext-—-moved-to-component-directories-non-breaking" aria-label="Permalink to “LoadingIndicator / LoadingText — moved to component directories (non-breaking)”">​</a></h4><p>The same move as <code>FormLabel</code>, for the same reason: both now live at <code>src/components/LoadingIndicator/</code> and <code>src/components/LoadingText/</code>, instead of as bare <code>.vue</code> files directly under <code>src/components/</code>. Each gains <code>types.ts</code> (<code>LoadingIndicatorProps</code>, <code>LoadingTextProps</code>), stories, a docs page and Cypress tests. The import path (<code>import { LoadingIndicator, LoadingText } from &#39;frappe-ui&#39;</code>) is unchanged.</p><p>They stay separate from <code>Spinner</code> and <code>Skeleton</code>. Usage across the surveyed apps shows real, separate demand: <code>LoadingIndicator</code> (~60 files) and <code>LoadingText</code> (~11 files) are both in active use, not duplicates.</p><h4 id="loadingindicator-is-now-spinner-under-its-older-name" tabindex="-1"><code>LoadingIndicator</code> is now <code>Spinner</code> under its older name (breaking; loud in TS, silent in JS) <a class="header-anchor" href="#loadingindicator-is-now-spinner-under-its-older-name" aria-label="Permalink to “LoadingIndicator is now Spinner under its older name (breaking; loud in TS, silent in JS)”">​</a></h4><p>This replaces the entry above. <code>LoadingIndicator</code> was a <code>Spinner</code> scaled by a percentage. It is now the same component as <code>Spinner</code>, so it takes <code>Spinner</code>&#39;s props, and it has no docs page of its own. Its <code>scale</code> prop is removed: use <code>size</code> or a class instead. See the <a href="/docs/migration#loadingindicator-is-spinner">migration guide</a>.</p><h4 id="icon-—-docs-page-and-stories-added" tabindex="-1">Icon — docs page and stories added <a class="header-anchor" href="#icon-—-docs-page-and-stories-added" aria-label="Permalink to “Icon — docs page and stories added”">​</a></h4><p><code>Icon</code> had no <code>stories/</code> folder, so it did not appear in the docs site, even though it is a public export. It now has a docs page and two stories (the lucide string form, and the <code>Component</code> form).</p><h4 id="legacy-components-—-dev-mode-warnings" tabindex="-1"><code>Pill</code> unexported, <code>ThemeSwitcher</code> warns in development <a class="header-anchor" href="#legacy-components-—-dev-mode-warnings" aria-label="Permalink to “Pill unexported, ThemeSwitcher warns in development”">​</a></h4><p><code>Pill</code> is no longer exported from the package entry point. It stays an internal part of <code>TabButtons</code>.</p><p><code>ThemeSwitcher</code> moved to <code>frappe-ui/experimental</code> and stays deprecated there. It logs a one-time deprecation warning in development. For new theme switchers, compose <code>Select</code> with the <code>useColorScheme</code> composable. See <a href="#themeswitcher-—-moved-to-frappe-ui-experimental-breaking-loud">the ThemeSwitcher entry</a>.</p><h4 id="divider-—-action-handler-removed-breaking" tabindex="-1">Divider — <code>action.handler</code> removed (breaking) <a class="header-anchor" href="#divider-—-action-handler-removed-breaking" aria-label="Permalink to “Divider — action.handler removed (breaking)”">​</a></h4><p>Per <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0008-no-deprecated-members-in-1-0-0.md" target="_blank" rel="noreferrer">ADR-0008</a>, <code>action.handler</code> is deleted, not kept with a warning. There were zero call sites across the survey. It is a silent break: a leftover <code>handler</code> is dropped as an unknown key, so the action button renders but does nothing on click. In action mode the divider still reads as a separator for assistive technologies.</p><p><strong>What to do:</strong> use <code>action.onClick</code>. See the <a href="/docs/migration#divider">migration guide</a>.</p><h4 id="gridlayout-—-removed-breaking" tabindex="-1">GridLayout — removed (breaking) <a class="header-anchor" href="#gridlayout-—-removed-breaking" aria-label="Permalink to “GridLayout — removed (breaking)”">​</a></h4><ul><li><strong>Breaking:</strong> <code>GridLayout</code> is no longer exported. It was a thin passthrough to <code>grid-layout-plus</code>, with no docs page and no tests. The import fails, so the build names every call site.</li><li><code>grid-layout-plus</code> is removed from <code>dependencies</code>. Nothing else in <code>src/</code> imported it.</li><li>The deleted component had two bugs, so if you now wire up <code>grid-layout-plus</code> yourself, expect different behavior: <ul><li><code>cols</code> and <code>rowHeight</code> were read once at setup inside a <code>reactive()</code> options object, not <code>computed</code>, so changing either prop after mount did nothing.</li><li>The drag placeholder color was a hardcoded <code>#b1b1b1</code>, not a theme token, so it ignored dark mode.</li></ul></li></ul><p><strong>What to do:</strong> depend on <code>grid-layout-plus</code> directly.</p><h3 id="lists" tabindex="-1">Lists <a class="header-anchor" href="#lists" aria-label="Permalink to “Lists”">​</a></h3><h4 id="list-—-select-all-follows-items-that-change-in-place-fix" tabindex="-1">List — select-all follows items that change in place (fix) <a class="header-anchor" href="#list-—-select-all-follows-items-that-change-in-place-fix" aria-label="Permalink to “List — select-all follows items that change in place (fix)”">​</a></h4><p><code>&lt;ListRows&gt;</code> updated the set of rows that the header&#39;s select-all covers only when <code>items</code> was replaced with a different array. A row pushed in or spliced out, an entry swapped in place, a changed id, or a changed <code>rowKey</code> left select-all working from old values: it checked rows that were gone and missed rows that were there. It now tracks the same identities the rows render with.</p><p>Selection itself is still yours to manage: a removed row&#39;s value stays in <code>v-model:selection</code> until you drop it.</p><h4 id="frappe-ui-list-—-responsive-columns-and-the-css-hook-contract-frozen-breaking" tabindex="-1"><code>frappe-ui/list</code> — responsive columns, and the CSS hook contract frozen (breaking) <a class="header-anchor" href="#frappe-ui-list-—-responsive-columns-and-the-css-hook-contract-frozen-breaking" aria-label="Permalink to “frappe-ui/list — responsive columns, and the CSS hook contract frozen (breaking)”">​</a></h4><p><code>List.columns</code> now takes a breakpoint object as well as a plain array:</p><div class="language-vue"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1zd9e2">&lt;</span><span class="s_1uuh8p">List</span></span>
<span class="line"><span class="s_1jjt6x">  :</span><span class="s_1i4ay4">columns</span><span class="s_1jjt6x">=</span><span class="s_w1p9wo">&quot;</span><span class="s_13ahmt">{</span></span>
<span class="line"><span class="s_r4oegk">    base</span><span class="s_1jjt6x">: </span><span class="s_13ahmt">[</span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">minmax(0,1fr)</span><span class="s_w1p9wo">&#39;</span><span class="s_1jjt6x">, </span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">80px</span><span class="s_w1p9wo">&#39;</span><span class="s_1jjt6x">, </span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">64px</span><span class="s_w1p9wo">&#39;</span><span class="s_13ahmt">]</span><span class="s_1jjt6x">,</span></span>
<span class="line"><span class="s_r4oegk">    md</span><span class="s_1jjt6x">: </span><span class="s_13ahmt">[</span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">minmax(0,1fr)</span><span class="s_w1p9wo">&#39;</span><span class="s_1jjt6x">, </span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">140px</span><span class="s_w1p9wo">&#39;</span><span class="s_1jjt6x">, </span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">100px</span><span class="s_w1p9wo">&#39;</span><span class="s_13ahmt">]</span><span class="s_1jjt6x">,</span></span>
<span class="line"><span class="s_r4oegk">    lg</span><span class="s_1jjt6x">: </span><span class="s_13ahmt">[</span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">minmax(0,2fr)</span><span class="s_w1p9wo">&#39;</span><span class="s_1jjt6x">, </span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">180px</span><span class="s_w1p9wo">&#39;</span><span class="s_1jjt6x">, </span><span class="s_w1p9wo">&#39;</span><span class="s_2575z4">120px</span><span class="s_w1p9wo">&#39;</span><span class="s_13ahmt">]</span><span class="s_1jjt6x">,</span></span>
<span class="line"><span class="s_13ahmt">  }</span><span class="s_w1p9wo">&quot;</span></span>
<span class="line"><span class="s_1zd9e2">&gt;</span></span></code></pre></div><p><code>base</code> is required and applies from zero width. Every other key names a breakpoint from your own Tailwind <code>screens</code>. It applies from that width up to the next key you supplied, so a breakpoint you leave out keeps the template below it. Each value replaces the whole template. Nothing is merged track by track, so a breakpoint can also change the number of tracks. Changing the number does not hide cells: pair it with matching visibility classes on the header and rows.</p><p>The switch is plain CSS, generated from your app&#39;s own breakpoint values. So the tracks change together with your <code>md:</code> utilities, and the server-rendered markup is already correct: there is no viewport measurement, no resize listener, and no flash on first paint. Arrays keep working exactly as before. A key can name any screen your config defines, not only a plain width. A <code>{ min, max }</code> screen gives a tier that ends where the screen ends, and <code>{ max }</code> and <code>{ raw }</code> screens work too. Each tier applies in exactly the same places as that screen&#39;s own variants.</p><p>Responsive columns need the frappe-ui Tailwind preset, because the preset knows your breakpoints. Without it, a breakpoint object uses its <code>base</code> tier at every width.</p><p>A key that is not one of your screens is ignored: <code>{ base, medium }</code> renders <code>base</code> everywhere, because <code>medium</code> names no breakpoint. Screen names come from your Tailwind config, so <code>ListColumnsByBreakpoint</code> keeps an open index signature, and the type cannot reject the key. A development build now warns instead. The warning names the key and lists the screens it could have been. It is removed from production builds.</p><p>The list family&#39;s public CSS hooks for v1 are now exactly <code>--list-gap</code> and <code>--list-row-padding-x</code> (with the preset shortcuts <code>list-gap-*</code> and <code>list-row-px-*</code>). The changes you can see from freezing them:</p><ul><li><strong><code>--list-columns</code> and <code>list-cols-[…]</code> are gone.</strong> Column templates come from the <code>columns</code> prop, which now covers the responsive case the hook existed for. The variable is internal (<code>--_list-columns</code>), and setting it by hand is not supported.</li><li><strong>Every <code>List</code> owns its columns.</strong> A wrapper&#39;s <code>--list-columns</code> used to override a nested list&#39;s own <code>columns</code> prop, with <code>[--list-columns:initial]</code> as the way out. Nested lists now always keep their own template, or the default feed template when they set none.</li><li><strong>Both remaining hooks work from any ancestor.</strong> They resolve through <code>var()</code> fallbacks where they are used, so one declaration on a wrapper styles every list inside it.</li><li><strong>Internal variables are renamed with a <code>--_list</code> prefix</strong>, and are explicitly not API: <code>--list-columns-default</code> → <code>--_list-columns</code> (plus one <code>--_list-columns-&lt;breakpoint&gt;</code> variable per tier and one generated <code>--_list-tier-&lt;screen&gt;</code>), <code>--list-checkbox-width</code> → <code>--_list-checkbox-width</code>, <code>--list-row-height</code> → <code>--_list-row-height</code>. None of them was documented. Anything that targets the old names breaks. Row height is the <code>rowHeight</code> prop. Overriding the variable would break <code>virtual</code> windowing, so it is not a hook on purpose, and it stays one number at every width.</li><li><strong>Internal variables reset at every <code>List</code>.</strong> A list nested inside another list no longer inherits the outer list&#39;s <code>columns</code> template, checkbox inset, or <code>rowHeight</code> when it does not set those props itself. This was an existing leak, fixed during the freeze.</li><li><strong><code>--list-row-padding-x</code> now reaches static rows too.</strong> Before, only interactive rows and the header used the hook, so a static table with a header came out misaligned when it was set. Now one declared value applies to every row and the header. The defaults when it is unset do not change (interactive rows <code>0.75rem</code>, everything else flush).</li></ul><p><strong>What to do:</strong> replace <code>class=&quot;max-sm:list-cols-[auto_minmax(0,1fr)_auto]&quot;</code> with a <code>base</code> tier in the <code>columns</code> prop. Delete any <code>[--list-columns:initial]</code>; they are no longer needed. Stop targeting the renamed internal variables.</p><p>The rules behind this, which apply to every future component with CSS hooks, are in <a href="https://github.com/frappe/frappe-ui/blob/main/spec/adr/0017-css-variable-styling-hooks.md" target="_blank" rel="noreferrer">ADR-0017</a>.</p><h4 id="frappe-ui-list-—-row-state-and-slot-vocabulary-breaking-silent" tabindex="-1"><code>frappe-ui/list</code> — row state and slot vocabulary (breaking, silent) <a class="header-anchor" href="#frappe-ui-list-—-row-state-and-slot-vocabulary-breaking-silent" aria-label="Permalink to “frappe-ui/list — row state and slot vocabulary (breaking, silent)”">​</a></h4><ul><li><code>ListRowBase</code> now uses <code>data-state=&quot;active|inactive&quot;</code>, plus separate boolean <code>data-selected</code> and <code>data-interactive</code> attributes.</li><li><code>ListGroup</code>&#39;s <code>#header</code> slot is renamed to <code>#label</code>.</li><li><code>ListHeaderCellSort</code>&#39;s <code>#suffix</code> slot is renamed to <code>#sort-indicator</code>. Its edge-aware placement is unchanged.</li></ul><p><strong>What to do:</strong> run <code>npx list-v1 .</code> for the mechanical selectors and the slots with fixed names. See the <a href="/docs/migration#list-family">migration guide</a> for the manual checks.</p><h4 id="listfilter-—-removed-breaking" tabindex="-1">ListFilter — removed (breaking) <a class="header-anchor" href="#listfilter-—-removed-breaking" aria-label="Permalink to “ListFilter — removed (breaking)”">​</a></h4><p><strong>Breaking, loud:</strong> <code>ListFilter</code> is no longer exported, so the import fails. Its internals (<code>SearchComplete</code>, <code>FilterIcon</code>) are gone with it (#992, #999). No consumer app used it.</p><p><strong>What to do:</strong> build filter UI in your app with <code>Select</code> and <code>Combobox</code>.</p><h4 id="listview-family-—-moved-to-frappe-ui-experimental-breaking" tabindex="-1">ListView family — moved to <code>frappe-ui/experimental</code> (breaking) <a class="header-anchor" href="#listview-family-—-moved-to-frappe-ui-experimental-breaking" aria-label="Permalink to “ListView family — moved to frappe-ui/experimental (breaking)”">​</a></h4><p><code>ListView</code> is not being brought up to the v1 quality bar at the root for <code>1.0.0</code>. <code>frappe-ui/list</code> is the recommended building block for new code, but it is a narrower family that you compose, by design. It has no equivalent for <code>ListView</code>&#39;s config-driven columns: resizable widths, per-column <code>getLabel</code>/<code>prefix</code> functions, cell tooltips, excluding disabled rows, and the built-in select banner. So instead of freezing all 13 exports at the root without deprecation (12 components: <code>List</code> and <code>ListView</code> are two names for one component), the family moves to <code>frappe-ui/experimental</code>, which has no stability promise. It stays there until <code>frappe-ui/list</code> can do everything it does.</p><p><strong>Breaking, loud:</strong> <code>import { ListView, ... } from &#39;frappe-ui&#39;</code> fails to resolve.</p><p><strong>What to do:</strong> import from <code>frappe-ui/experimental</code> instead: <code>List</code>, <code>ListView</code>, <code>ListEmptyState</code>, <code>ListFooter</code>, <code>ListGroupHeader</code>, <code>ListGroupRows</code>, <code>ListGroups</code>, <code>ListHeader</code>, <code>ListHeaderItem</code>, <code>ListRow</code>, <code>ListRowItem</code>, <code>ListRows</code>, <code>ListSelectBanner</code>.</p><h3 id="editor" tabindex="-1">Editor <a class="header-anchor" href="#editor" aria-label="Permalink to “Editor”">​</a></h3><h4 id="the-editor-s-uploadedfile-is-now-uploadedmedia-breaking" tabindex="-1">The editor&#39;s <code>UploadedFile</code> is now <code>UploadedMedia</code> (breaking) <a class="header-anchor" href="#the-editor-s-uploadedfile-is-now-uploadedmedia-breaking" aria-label="Permalink to “The editor&#39;s UploadedFile is now UploadedMedia (breaking)”">​</a></h4><p><code>frappe-ui</code> and <code>frappe-ui/editor</code> both exported a type named <code>UploadedFile</code>, and the two shapes did not match. The editor type is now <code>UploadedMedia</code>. The root <code>UploadedFile</code> keeps its name and its shape.</p><ul><li><strong><code>upload</code> fits <code>uploadFunction</code>.</strong> The root <code>UploadedFile</code> is now a type alias, not an interface, so <code>useEditor({ uploadFunction: upload })</code> compiles. Before, TypeScript reported TS2322 because the index signature was missing. A bare <code>upload</code> stores a private file that is attached to nothing, so <a href="/docs/migration#editor-option-types">pass an owner document or <code>private: false</code></a>.</li><li><strong><code>UploadedFile</code> no longer merges.</strong> A type alias does not support declaration merging, so <code>declare module &#39;frappe-ui&#39;</code> cannot add fields to it. Extend it instead: <code>type MyFile = UploadedFile &amp; { ... }</code>.</li><li><strong>Who is affected:</strong> code that imports <code>UploadedFile</code> from <code>frappe-ui/editor</code>. TypeScript reports the missing export.</li></ul><p><strong>What to do:</strong> rename the import to <code>UploadedMedia</code>. See the <a href="/docs/migration#editor-option-types">migration guide</a>.</p><h4 id="editor-suggestion-roles-and-fixed-menu-size-have-explicit-names-breaking" tabindex="-1">Editor suggestion roles and fixed-menu size have explicit names (breaking) <a class="header-anchor" href="#editor-suggestion-roles-and-fixed-menu-size-have-explicit-names-breaking" aria-label="Permalink to “Editor suggestion roles and fixed-menu size have explicit names (breaking)”">​</a></h4><p>Suggestion configuration now calls the rendered mention node <code>nodeView</code> and the suggestion popup <code>listComponent</code>. Both replace the unclear <code>component</code> key. <code>EditorFixedMenu.buttonSize</code> is now <code>size</code>, like the rest of the component library. TypeScript reports the removed names.</p><p><strong>What to do:</strong> in JavaScript, run the <code>editor-v1</code> codemod for fixed menus. Migrate suggestion options by hand, because the replacement depends on the component&#39;s role. See the <a href="/docs/migration#editor-suggestion-and-fixed-menu-names">migration guide</a>.</p><h4 id="editor-kit-upload-and-menu-options-are-typed-breaking" tabindex="-1">Editor kit, upload and menu options are typed (breaking) <a class="header-anchor" href="#editor-kit-upload-and-menu-options-are-typed-breaking" aria-label="Permalink to “Editor kit, upload and menu options are typed (breaking)”">​</a></h4><p>Every kit member, the upload handler and the two floating menus now have a named option type. TypeScript reports a misspelled or removed key at the call site. The runtime ignores it, as it always did.</p><ul><li><strong>StarterKit drops <code>code</code>, <code>codeBlock</code> and <code>link</code>.</strong> The frappe <code>Code</code>, <code>CodeBlock</code> and <code>Link</code> extensions own those names, so the three keys never reached an extension. Delete them.</li><li><strong>A kit&#39;s <code>starterKit</code> leaves out <code>heading</code>.</strong> The kit&#39;s own top-level <code>heading</code> member overwrites it. Move the value up one level.</li><li><strong><code>InlineKit.starterKit</code> accepts only <code>false</code> per member.</strong> InlineKit registers a stock extension or none, so <code>bold: { HTMLAttributes: … }</code> was read as &quot;keep bold&quot; and its options were dropped. The new <code>InlineStarterKitOptions</code> type says so.</li><li><strong><code>slashCommands</code> honours <code>items</code>.</strong> <code>{}</code> keeps the built-in menu, <code>false</code> removes it, and <code>{ items }</code> now replaces the built-in list, where it used to be ignored. <code>items</code> takes a ref, a getter or a plain array, and is read every time the menu opens.</li><li><strong><code>toc</code> and <code>styleClipboard</code> are off by default in <code>RichTextKit</code>.</strong> Both add UI most editors never show: a table-of-contents node and a format painter. Add <code>toc: {}</code> or <code>styleClipboard: {}</code> where you use them. <code>imageViewer</code> stays on.</li><li><strong>Mention and tag items are <code>{ label, value }</code>.</strong> <code>label</code> is the text, and <code>value</code> is the stored id. Extra fields are yours: the list passes your own object to the item slot untouched, so an avatar or a colour still reaches the template. <code>getMentions()</code> returns the same two fields.</li><li><strong><code>UploadedMedia.file_url</code> is required</strong>, and the handler type is exported as <code>UploadFunction</code>. A handler may take a second <code>MediaUploadRequestOptions</code> argument to report progress. A handler with one argument still compiles.</li><li><strong><code>EditorBubbleMenu</code> and <code>EditorFloatingMenu</code> share <code>EditorMenuOptions</code>.</strong><code>side</code>, <code>align</code>, <code>strategy</code>, <code>offset</code>, <code>flip</code>, <code>shift</code>, <code>hide</code>, <code>inline</code>, <code>scrollTarget</code> and <code>shouldShow</code> are the whole supported set.</li><li><strong>The menus position with <code>side</code> and <code>align</code></strong>, the same two axes as <code>Popover</code> and every overlay built on it, with the same <code>PopoverSide</code> and <code>PopoverAlign</code> types. <code>placement</code> and <code>EditorMenuPlacement</code> are removed. <code>placement: &#39;top-start&#39;</code> becomes <code>side: &#39;top&#39;, align: &#39;start&#39;</code>, and a bare <code>placement: &#39;bottom&#39;</code> becomes <code>side: &#39;bottom&#39;</code>. If you set neither, TipTap&#39;s own default still applies: <code>top</code> for the bubble menu and <code>right</code> for the floating menu.</li><li><strong>The rest of TipTap&#39;s Floating UI options are gone.</strong> The prop used to take all of them, so <code>arrow</code>, <code>size</code>, <code>autoPlacement</code>, <code>onShow</code>, <code>onHide</code>, <code>onUpdate</code>, <code>onDestroy</code>, and the object forms of the <code>offset</code>, <code>flip</code>, <code>shift</code>, <code>hide</code> and <code>inline</code> middleware type-checked and reached Floating UI. They are removed. Replace an object form with <code>true</code> to keep the middleware on its defaults, or drop the key: <code>flip: { fallbackPlacements: [&#39;bottom&#39;] }</code> becomes <code>flip: true</code>.</li><li><code>Editor.extensions</code> and <code>useEditor({ extensions })</code> take TipTap&#39;s <code>Extensions</code>, so a nested array of extensions is accepted.</li></ul><p>New exports: <code>UploadFunction</code>, <code>MediaUploadProgress</code>, <code>InlineStarterKitOptions</code>, <code>CommandItem</code>, <code>SlashCommandsOptions</code>, <code>EditorMenuOptions</code>, <code>EditorMenuShouldShowContext</code>.</p><p><strong>What to do:</strong> the <a href="/docs/migration#editor-option-types">editor migration guide</a> lists the edits per file.</p><h4 id="editor-—-mentions-open-after-brackets-and-quotes" tabindex="-1">Editor — mentions open after brackets and quotes <a class="header-anchor" href="#editor-—-mentions-open-after-brackets-and-quotes" aria-label="Permalink to “Editor — mentions open after brackets and quotes”">​</a></h4><p>Typing <code>@</code> after an opening bracket or quote (<code>(@jane</code>, <code>[@jane</code>, <code>&quot;@jane</code>) now opens the mention list. TipTap only allowed a space before the trigger, so those sequences never matched. Emails (<code>jane@example.com</code>) still do not open it.</p><p><code>SuggestionExtension.configure</code> accepts <code>allowedPrefixes</code> for custom suggestion extensions that need the same behaviour.</p><h4 id="editor-—-images-and-embeds-resize-from-a-bottom-right-corner-handle" tabindex="-1">Editor — images and embeds resize from a bottom-right corner handle <a class="header-anchor" href="#editor-—-images-and-embeds-resize-from-a-bottom-right-corner-handle" aria-label="Permalink to “Editor — images and embeds resize from a bottom-right corner handle”">​</a></h4><p>Selecting an image, video or embed used to show two vertical handles centered on its left and right edges. They sat in the middle of the media, were easy to miss, and only responded to horizontal drags. A selected image or embed now shows one handle in its bottom-right corner, with the diagonal resize icon that every OS window and image editor already uses. It uses the same 28px button as the actions menu in the opposite corner, mirrored across the media.</p><p>The drag reads both axes. The pointer&#39;s movement is projected onto the diagonal that keeps the aspect ratio, so dragging down grows the media (an edge handle could not do that), and a diagonal drag keeps the corner under the cursor. Media keeps its aspect ratio, as before. Keyboard resize on a focused handle now takes Up/Down as well as Left/Right. The embed&#39;s handle responds to the arrow keys for the first time; they used to move the caret out of the node instead.</p><p>Videos keep the edge handles. Their playback bar takes the bottom of the frame, so a corner handle there would either sit on the controls or float above them.</p><p>No API change: the handles and controls are internal to the media and iframe node views.</p><h4 id="editor-—-video-controls-sit-on-a-gradient-instead-of-a-floating-pill" tabindex="-1">Editor — video controls sit on a gradient instead of a floating pill <a class="header-anchor" href="#editor-—-video-controls-sit-on-a-gradient-instead-of-a-floating-pill" aria-label="Permalink to “Editor — video controls sit on a gradient instead of a floating pill”">​</a></h4><p>The playback row was a dark pill set in from the video&#39;s edges. It drew a hard rectangle across the picture, and had to stay dark enough for white icons on any frame. It now spans the full width of the video, on a gradient that fades up into the footage, so the contrast is only where the controls are. The gradient follows an eased curve to a lighter peak, instead of a straight two-stop fade, which left a visible edge where the band ended.</p><p>The row itself was restyled to look like a player rather than a toolbar: solid play/pause and volume icons, a seek bar the width of the video with a round thumb marking the playhead, and the elapsed/total time moved to the right, next to the volume and fullscreen buttons. The controls used <code>ink-*</code> color tokens, which flip with the theme and dimmed them into the video in dark mode. They now use the fixed white scale in both themes.</p><p>The playhead moves with the footage. It used to be driven by <code>timeupdate</code>, which fires about four times a second and moved the thumb along the track in steps. While the video plays, the position is now read once per frame. A tap elsewhere on the track slides the thumb there instead of jumping. A drag follows the pointer directly. This also fixes a scrub that stopped at the first pixel of movement: pressing and moving started a native drag of the node view, and the browser cancelled the pointer.</p><p>No API change: the handles and controls are internal to the media and iframe node views.</p><h4 id="editor-—-media-actions-live-in-a-single-menu" tabindex="-1">Editor — media actions live in a single menu <a class="header-anchor" href="#editor-—-media-actions-live-in-a-single-menu" aria-label="Permalink to “Editor — media actions live in a single menu”">​</a></h4><p>Selecting an image, video or embed used to show a row of six buttons across its top-right corner (caption, three alignments, replace, and on video a playback dropdown) on one 65%-black pill. It covered every selected image with controls, most of them rarely used, and it grew with each new action.</p><p>There is now one 28px <code>⋯</code> button there instead. It uses the <code>black-overlay-300</code> fill from the design (espresso-2.0, node 31403-45433), <code>rounded-4</code>, and a full-white 16px icon. It opens a menu that holds the same actions, grouped: a caption switch, Align (left/center/right, with the current one marked), the video-only Playback switches (autoplay, loop, muted), and replace. The resize handle in the opposite corner is the same button, so the media has one control style instead of the raw <code>black/65</code> and <code>white/50</code> it used to approximate.</p><p>No API change: the handles and controls are internal to the media and iframe node views.</p><h4 id="editor-—-selected-media-no-longer-wears-a-white-halo-in-dark-mode-fix" tabindex="-1">Editor — selected media no longer wears a white halo in dark mode (fix) <a class="header-anchor" href="#editor-—-selected-media-no-longer-wears-a-white-halo-in-dark-mode-fix" aria-label="Permalink to “Editor — selected media no longer wears a white halo in dark mode (fix)”">​</a></h4><p>The ring around a selected image, video, embed or gallery is drawn with a 2px offset, and Tailwind&#39;s default offset color is a hard <code>#fff</code>. On a white page that gap is invisible. In dark mode it was a bright white band around every selected node. The offset now uses <code>--surface-base</code>, the page background in both themes.</p><p>No API change: the handles and controls are internal to the media and iframe node views.</p><h4 id="editor-—-fullscreen-video-fills-the-screen-fix" tabindex="-1">Editor — fullscreen video fills the screen (fix) <a class="header-anchor" href="#editor-—-fullscreen-video-fills-the-screen-fix" aria-label="Permalink to “Editor — fullscreen video fills the screen (fix)”">​</a></h4><p>Fullscreen stretched the container to the viewport, but the video kept its saved pixel size. It sat small at the top of the screen, with the playback bar right under it and a black area filling everything below. The video is now centered and scaled to fit the screen, and the controls run along its bottom. The editing controls (toolbar, resize handles, caption) are not rendered in fullscreen, where you cannot use any of them.</p><p>No API change: the handles and controls are internal to the media and iframe node views.</p><h4 id="texteditor-and-its-v0-exports-—-removed-from-root-breaking" tabindex="-1"><code>TextEditor</code> and its v0 exports — removed from root (breaking) <a class="header-anchor" href="#texteditor-and-its-v0-exports-—-removed-from-root-breaking" aria-label="Permalink to “TextEditor and its v0 exports — removed from root (breaking)”">​</a></h4><p>Per ADR-0008, the deprecated v0 editor exports are removed from the top-level <code>frappe-ui</code>. These are loud breaks: the import fails to resolve.</p><ul><li><code>TextEditor</code>, <code>TextEditorBubbleMenu</code>, <code>TextEditorFixedMenu</code>, <code>TextEditorFloatingMenu</code>, <code>TextEditorContent</code>, <code>createEditorButton</code></li><li><code>ImageExtension</code>, <code>SetImageOptions</code>, <code>createSuggestionExtension</code>, <code>BaseSuggestionItem</code>, <code>CreateSuggestionExtensionOptions</code> (the two <code>TextEditor/extensions/*</code> entry files that were also re-exported from the root)</li></ul><p>This confirms <code>CONTEXT.md</code>&#39;s rule: the editor family is the only one that exports from a subpath instead of the root, and nothing editor-related is exported from the root anymore.</p><p>The v0 component files still ship, unchanged, as a safety net while apps move to <code>frappe-ui/editor</code>. They are parked in <code>frappe-ui/experimental</code> (<code>experimental/TextEditor/</code>, #1007), so apps in the middle of migrating keep an import path:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">TextEditor</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui/experimental</span><span class="s_w1p9wo">&#39;</span></span></code></pre></div><p>This path is unstable, with no deprecation period. Sharing the <code>experimental</code> entry costs its other importers nothing in production: #870&#39;s rollup measurement shows that unused re-export chains are removed before <code>sideEffects</code> marking applies, so the editor code is tree-shaken out of imports that do not use the editor. Removing the files is a separate cleanup, which a person decides, once every consumer has migrated (spec/editor.md §12). Redesigning the <code>TextEditor</code> public API is out of scope for <code>1.0.0</code> and moved to <code>1.1</code>.</p><p><strong>What to do:</strong> use <a href="/docs/molecules/editor"><code>Editor</code></a> and its kits and building blocks from the <code>frappe-ui/editor</code> subpath. See the migration guide&#39;s <a href="/docs/migration#editor">Editor section</a>.</p><h4 id="editor-and-texteditor-styles-—-tailwind-v4-theme-call-fixed" tabindex="-1">Editor and TextEditor styles — Tailwind v4 <code>theme()</code> call fixed <a class="header-anchor" href="#editor-and-texteditor-styles-—-tailwind-v4-theme-call-fixed" aria-label="Permalink to “Editor and TextEditor styles — Tailwind v4 theme() call fixed”">​</a></h4><p><code>.ProseMirror ul[data-type=&#39;taskList&#39;] input[type=&#39;checkbox&#39;]</code> used a <code>theme(&#39;colors.gray.900&#39;)</code> call that only works in Tailwind v3, in both <code>frappe-ui/editor</code>&#39;s and the v0 <code>TextEditor</code>&#39;s stylesheet. It broke Tailwind v4 builds (#861, a remaining case of #299). It is replaced with the same <code>var(--ink-gray-9)</code> token the rest of both files already use.</p><h4 id="editor-—-media-captions-moved-off-alt-breaking" tabindex="-1">Editor — media captions moved off <code>alt</code> (breaking) <a class="header-anchor" href="#editor-—-media-captions-moved-off-alt-breaking" aria-label="Permalink to “Editor — media captions moved off alt (breaking)”">​</a></h4><p>Text in an image&#39;s or video&#39;s <code>alt</code> no longer renders as a caption. Captions live in a separate <code>caption</code> attribute, saved as <code>data-caption</code>. <code>alt</code> goes back to being only the description for screen readers.</p><p>Existing <code>alt</code> values still parse and still round-trip untouched. They just do not show as a caption any more.</p><p>The editor no longer edits <code>alt</code> at all. The caption field used to write it. It now writes <code>caption</code>, and no other control took over <code>alt</code>. Set <code>alt</code> from your own content pipeline until an alt field is added.</p><p>There is no fallback from <code>caption</code> to <code>alt</code>, on purpose. Stored <code>alt</code> values are mostly upload filenames and emoji shortcodes, and a real caption cannot be told apart from those automatically. Showing all of them is worse than showing none.</p><p><strong>What to do:</strong> to keep a caption visible, copy the text into <code>caption</code>. A one-off content migration can do that where you know the old <code>alt</code> values were captions.</p><h3 id="code-editor" tabindex="-1">Code editor <a class="header-anchor" href="#code-editor" aria-label="Permalink to “Code editor”">​</a></h3><h4 id="code-editor-—-a-new-family-at-frappe-ui-code-editor-breaking-loud" tabindex="-1">Code editor — a new family at <code>frappe-ui/code-editor</code> (breaking, loud) <a class="header-anchor" href="#code-editor-—-a-new-family-at-frappe-ui-code-editor-breaking-loud" aria-label="Permalink to “Code editor — a new family at frappe-ui/code-editor (breaking, loud)”">​</a></h4><ul><li><p><strong>Breaking:</strong> the <code>CodeEditor</code> and <code>CodePreview</code> pair is deleted from <code>frappe-ui/experimental</code>, with no deprecation period (<code>experimental</code> has no stability promise). A CodeMirror 6 family ships at <code>frappe-ui/code-editor</code> instead: the <code>useCodeEditor</code> engine, a renderless <code>CodeEditor</code>, <code>CodeEditorContent</code>, <code>CodeKit</code>, the <code>codeChrome</code>, <code>codeHighlight</code> and <code>codeKeymap</code> extensions, and <code>loadLanguage</code>.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes tokyo-night github-light s_2cy0ei" tabindex="0" dir="ltr"><code><span class="line"><span class="s_1th9sy">// before</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">CodeEditor</span><span class="s_1jjt6x">,</span><span class="s_4q1z3w"> loadLanguage</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui/experimental</span><span class="s_w1p9wo">&#39;</span></span>
<span class="line"></span>
<span class="line"><span class="s_1th9sy">// after</span></span>
<span class="line"><span class="s_edvzsf">import</span><span class="s_13ahmt"> { </span><span class="s_4q1z3w">CodeEditor</span><span class="s_1jjt6x">,</span><span class="s_4q1z3w"> CodeEditorContent</span><span class="s_1jjt6x">,</span><span class="s_4q1z3w"> CodeKit</span><span class="s_13ahmt"> }</span><span class="s_edvzsf"> from</span><span class="s_w1p9wo"> &#39;</span><span class="s_2575z4">frappe-ui/code-editor</span><span class="s_w1p9wo">&#39;</span></span></code></pre></div><p>The library ships no labeled field. Each app builds its own thin component on <code>&lt;CodeEditor&gt;</code>, the way each app already builds its own editor component. The Desk/FormLayout field lives in <code>@framework/ui</code>.</p></li><li><p><strong>Breaking:</strong> features come from a required <code>extensions</code> array of raw CodeMirror extensions. The <code>language</code>, <code>variant</code>, <code>size</code> and <code>placeholder</code> props are gone, and so are the label, description, error and required props. <code>--cm-max-height</code> becomes <code>--code-max-height</code>.</p></li><li><p><strong>Breaking:</strong> <code>CodePreview</code> leaves frappe-ui. It is a markdown renderer, not a code editor, and it moves to <code>@framework/ui</code>.</p></li><li><p><strong>Breaking:</strong> the ten <code>@codemirror/lang-*</code> packages and <code>@codemirror/lint</code> move from dependencies to optional peer dependencies. Every app used to download all eleven. Install the ones you render. <code>loadLanguage</code> throws an error that names the missing package. <code>frappe-ui/vite</code> stubs the language packages an app did not install, so the build no longer fails on a language nobody renders.</p></li></ul><p><strong>What to do:</strong> see <a href="/docs/molecules/code-editor">the code editor docs</a> and <a href="/docs/migration#code-editor">the migration guide</a>.</p><h3 id="charts" tabindex="-1">Charts <a class="header-anchor" href="#charts" aria-label="Permalink to “Charts”">​</a></h3><h4 id="a-series-can-carry-its-own-number-format" tabindex="-1">A series can carry its own number <code>format</code> <a class="header-anchor" href="#a-series-can-carry-its-own-number-format" aria-label="Permalink to “A series can carry its own number format”">​</a></h4><p><code>SeriesStyle.format</code> prints one series&#39; values in the tooltip and in its data labels. Use it for a series whose unit differs from the others on its axis, such as a margin in percent next to revenue in currency. Without it, the tooltip uses the axis&#39; <code>format</code> and a label prints a compact number, as before. The axis ticks keep the axis&#39; <code>format</code>.</p><h4 id="line-and-area-data-labels-show-without-data-points" tabindex="-1">Line and area data labels show without data points <a class="header-anchor" href="#line-and-area-data-labels-show-without-data-points" aria-label="Permalink to “Line and area data labels show without data points”">​</a></h4><p>A line or area series with <code>showDataLabels</code> and no <code>showDataPoints</code> drew no labels, because echarts attaches a line&#39;s labels to its point symbols. The labels now show, and the points are drawn at zero size. This also applies to a combo chart&#39;s line series when <code>showDataLabels</code> is set on the chart.</p><h4 id="charts-—-a-new-family-at-frappe-ui-charts" tabindex="-1">Charts — a new family at <code>frappe-ui/charts</code> (breaking for beta users) <a class="header-anchor" href="#charts-—-a-new-family-at-frappe-ui-charts" aria-label="Permalink to “Charts — a new family at frappe-ui/charts (breaking for beta users)”">​</a></h4><p>This is the chart family for v1. It replaces the family that used to sit at the package root, which moved to <code>frappe-ui/experimental</code> (see <a href="#charts-v1-family-—-moved-to-frappe-ui-experimental-breaking">the next entry</a>). Import the components from <code>frappe-ui/charts</code>, which brings the <code>--chart-*</code> color tokens with it. <code>spec/charts.md</code> states the conventions, and <code>spec/adr/0015-what-enters-charts.md</code> records what the family accepts.</p><p>It first shipped in <code>1.0.0-beta.41</code>. If you used it in a beta, read the <strong>Breaking</strong> items below.</p><p><strong>What the family includes:</strong></p><ul><li>The chart frame parts are exported (<code>ChartCard</code>, <code>ChartContainer</code>, <code>ChartLegend</code> and <code>ChartTooltip</code>), so a plot an app draws itself can look like the rest of the family. <code>ChartCard</code> owns the card surface, and <code>card: false</code> turns it off.</li><li>Combo series through <code>seriesConfig[key].type</code>. This also merged the three axis option builders into one. Area fill per series comes with it.</li><li>Reference lines on the axis charts and on <code>ScatterChart</code>. A line outside the data range is clipped, instead of stretching the value axis.</li><li><code>stacked: &#39;normalized&#39;</code> for shares, and <code>maxSeries</code> to limit a long grouping column. <code>maxSeries</code> has no default.</li><li><code>ScatterChart</code> and <code>SankeyChart</code>.</li><li>Category labels fit themselves: the library measures, tilts and truncates them, instead of taking an angle prop.</li><li><code>xAxis.type: &#39;value&#39;</code> reads the x column as a quantity: a point sits at its own number instead of in its row&#39;s slot, and the rows draw in numeric order. You have to ask for it: a column of numbers still reads as categories by default.</li><li>The three states are slots (<code>#loading</code>, <code>#error</code> and <code>#empty</code>) on <code>ChartContainer</code> and on every chart component. So an app can put a retry button next to a failed query without drawing its own frame. <code>#loading</code> replaces the whole placeholder, not a caption under a spinner.</li><li>A loading chart draws a skeleton the size of its plot. It used to draw a spinner and the words &quot;Loading chart…&quot;. A dashboard fills in one card at a time, and a placeholder that keeps the grid&#39;s shape reads better than eight spinners turning out of step. <code>#loading</code> lets you replace it.</li><li><code>y2</code> names the value column or columns measured against the second value axis. This completes the pairs <code>x</code>/<code>xAxis</code>, <code>y</code>/<code>yAxis</code> and <code>y2</code>/<code>y2Axis</code>. The axis is drawn only when <code>y2</code> names a column. <code>y2Axis</code> is unchanged.</li><li><code>ScatterChart</code> takes <code>showDataLabels</code>, which prints each point&#39;s <code>label</code> next to it. Labels that collide with a neighbour are dropped.</li><li><code>NumberCard</code> takes <code>color</code>, the ink the reading is printed in, for a card that stands for a series drawn in that color elsewhere. It does not change the card, the title or the delta tone.</li><li><strong><code>dir</code> and <code>lang</code> are followed, not read once.</strong> If a page changes <code>document.documentElement.dir</code> or <code>lang</code> after a chart mounts, the chart redraws in the new direction and reprints its numbers in the new locale. A chart that sets its own <code>dir</code> is not affected.</li><li>One rule, across the family, for a value that cannot be plotted. A cell that does not read as a number has no mark, so <code>select</code> no longer fires for it on Enter, and the keyboard cursor no longer stops on a series that has nothing at that row. A row whose x cannot be read as a date is dropped from a <code>time</code> axis, the same way a non-numeric x is already dropped from a <code>value</code> axis, and the rows sort by time. The funnel drops a stage whose count is missing, unreadable or negative, instead of drawing it at 0, so its indexes and conversion rates count only the stages that remain. <strong>Breaking, silent:</strong> a chart that draws any of these now draws something else, with no error.</li><li><strong>The empty state is what the plot draws.</strong> Bar, line and area now show it when no visible series has a number at any row. They used to draw bare axes. A <code>y</code> key that no row carries reads as empty, and so does turning every series off through the legend. Also silent.</li><li><code>NumberCard</code> prints through <code>format</code> and <code>deltaFormat</code>, the same <code>ChartValueFormatter</code> every other chart takes. <code>precision</code> and <code>compact</code> are <strong>removed</strong>: write either one in <code>format</code> instead. <code>format</code> prints the value and the target. <code>deltaFormat</code> prints the delta and receives it without a sign. A card with neither prints what it printed before.</li></ul><p><strong>Changed since <code>1.0.0-beta.41</code>, the first beta that shipped the family:</strong></p><ul><li><strong>Breaking, silent:</strong> the six mark emits are renamed to <code>select</code>: <code>datapointClick</code> on <code>AreaChart</code>, <code>BarChart</code> and <code>LineChart</code>, <code>sliceClick</code> on <code>DonutChart</code>, <code>stageClick</code> on <code>FunnelChart</code>, <code>cellClick</code> on <code>HeatmapChart</code>, <code>linkClick</code> on <code>SankeyChart</code> and <code>pointClick</code> on <code>ScatterChart</code>. The library names an emit after the behavior, and all six now fire from the keyboard too, so &quot;click&quot; was wrong. A listener on an old name stops firing with no error. See the <a href="/docs/migration#charts">migration guide</a>. The payload types are unchanged.</li><li><strong>Breaking, loud:</strong> <code>ChartTheme</code> is now <code>ChartTokens</code>, and <code>useChartTheme</code> is now <code>useChartTokens</code>, which returns <code>{ tokens }</code> rather than <code>{ theme }</code>. <code>theme</code> means a color tone everywhere else in the library, and these are the resolved <code>--chart-*</code> values.</li><li><strong>Breaking, loud:</strong> <code>formatValue</code>, <code>formatDate</code>, <code>formatLabel</code>, <code>formatPercent</code>, <code>formatAxisValue</code>, <code>currentColorScheme</code> and <code>resolveChartTheme</code> are no longer exported. They had no documented use, and they are the library&#39;s own printing, not utilities to build on. Read the plot-area colors with <code>useChartTokens</code>, which re-resolves when the theme changes. <code>currentColorScheme</code> was the internal <code>getResolvedColorScheme</code> under another name.</li></ul><p><strong>Settled by the RC API review (<a href="https://github.com/frappe/frappe-ui/issues/1139" target="_blank" rel="noreferrer">#1139</a>) before the <code>frappe-ui/charts</code> entry freezes.</strong> Every item is loud in TypeScript unless it says otherwise. The <a href="/docs/migration#charts">migration guide</a> has the rewrites.</p><ul><li><strong>Breaking:</strong> one shape for every <code>select</code> payload. <code>ChartDatapointEvent.dataIndex</code> and <code>FunnelStageEvent.index</code> are gone; read the row, which every event carries. <code>seriesName</code> on <code>ChartDatapointEvent</code> and <code>ScatterPointEvent</code> is now <code>name</code>, the field every other payload, item and legend entry already used for an identity. <code>FunnelStageEvent</code> gains the <code>name</code> it never had: the category value, where <code>label</code> is what the column printed.</li><li><strong>Breaking, silent:</strong> <code>DonutSliceEvent.name</code> is now the slice&#39;s identity. It was the printed name, and the printed name moves to a new <code>label</code>. Both are strings, so a handler that reads <code>name</code> keeps running, on a different value: <code>OTHERS_KEY</code> for the collapsed tail, and <code>&quot;A (2)&quot;</code> for a label that a second row repeats. Search for <code>@select</code> handlers on <code>DonutChart</code> that read <code>.name</code>.</li><li><strong>Breaking:</strong> one tooltip slot shape. Every <code>#tooltip</code> slot, and <code>ChartTooltip</code> itself, carries <code>{ label, items, rows }</code>. <code>row</code> is gone. <code>rows</code> holds one row for a point, cell, band or stage; every grouped row for a donut&#39;s &quot;Others&quot; slice; and no rows for a sankey node, which stands for every row through it. <code>FunnelChart</code>&#39;s <code>stage</code> slot prop is gone too: the two conversion rates are items named <code>ofFirst</code> and <code>ofPrevious</code>. <code>ChartTooltipItem.kind</code> is required, and the value <code>&#39;column&#39;</code> is now <code>&#39;context&#39;</code>: a reading the plot does not draw, which is a <code>tooltipColumns</code> entry or a funnel rate. An item you build by hand needs <code>kind: &#39;series&#39;</code>.</li><li><strong>Breaking:</strong> <code>DonutChart</code>&#39;s <code>#center</code> slot passes <code>{ label, value, formattedValue, percent }</code> instead of preformatted strings, the way <code>ChartTooltipItem</code> does. <code>value</code> and <code>percent</code> are numbers, so your own readout can do its own arithmetic. <code>formattedValue</code> is what the default prints.</li><li><strong>Breaking:</strong> one name for the data label. <code>showValues</code> on <code>HeatmapChart</code> and <code>showInlineLabels</code> on <code>DonutChart</code> are both now <code>showDataLabels</code>. Axis charts take it at the chart level too, so one prop replaces one <code>seriesConfig</code> entry per series.</li><li><strong>Breaking, silent:</strong> <code>FunnelChart.showPercentages</code> is removed. A funnel now always prints its conversion rates. It defaulted to <code>true</code>, so a chart that set <code>false</code> gets them back with no error.</li><li><strong>Breaking, silent:</strong> numbers print in the page&#39;s language. Charts read <code>document.documentElement.lang</code>, the attribute <code>dir</code> is already read from. A page that declares no language, or a malformed one, prints what it printed before. Dates and time-axis labels still print in English; <code>xAxis.format</code> prints them in another language.</li><li><strong>Breaking:</strong> <code>ChartTokens.splitLine</code> is now <code>gridline</code>, after the <code>--chart-gridline</code> variable it reads. <code>splitLine</code> is still echarts&#39; own option key.</li><li><strong>Breaking:</strong> <code>NumberCardSparklineType</code> is gone. <code>NumberCardSparkline.type</code> takes a <code>ChartMark</code>.</li><li><strong>Breaking, silent:</strong> a sparkline&#39;s <code>&#39;line&#39;</code> draws only the stroke. It used to draw a stroke over a fill, which the family calls an area, so that shape is now <code>&#39;area&#39;</code>, and it is the default. <code>&#39;line&#39;</code> is still a <code>ChartMark</code>, so a card that names it compiles unchanged and draws something else. A card that names no type is not affected. Search for <code>sparkline</code> objects with <code>type: &#39;line&#39;</code>.</li><li><strong>Breaking:</strong> <code>OTHERS_LABEL</code> is no longer exported, and <code>ResolvedColorScheme</code> is exported from the root only. <code>OTHERS_KEY</code> stays. <code>ReferenceLineLabelPlacement</code> is now exported. <code>ChartExposed</code> is the caller&#39;s view of the template ref: <code>chart</code> is typed <code>ECharts | undefined</code>, the value Vue passes back, where the type said <code>ComputedRef&lt;ECharts | undefined&gt;</code>. <code>ChartContainer</code>&#39;s <code>plotLabel</code>, <code>plotLabelSecondary</code> and <code>plotLabelPlacement</code> are now <code>yAxisTitle</code>, <code>y2AxisTitle</code> and <code>axisTitlePlacement</code>, after the <code>yAxis.title</code> and <code>y2Axis.title</code> that every chart already uses. <code>PlotLabelPlacement</code> is now <code>AxisTitlePlacement</code>. <code>paletteColors</code> takes the <code>palette</code> prop&#39;s own value (a ramp name, a list of colors, or nothing) plus the fallback ramp, so the exported helper is the same resolver the charts call: <code>paletteColors(palette, tokens, count, fallback?)</code>.</li><li><strong>Breaking:</strong> four props that set a look, rather than state a reading, are gone. <code>SeriesStyle.lineWidth</code> and <code>fillOpacity</code> (at the chart level and per series) are removed: the library draws one stroke weight, and it decides the fill (a fading gradient for a free area, a solid wash for a banded one). The per-series <code>echartOptions</code> reaches both: <code>lineStyle: { width }</code> and <code>areaStyle: { opacity }</code>. <code>SeriesStyle.lineType</code> is now <code>dashed?: boolean</code>, the same shape as <code>ReferenceLine.dashed</code>, drawn with the same dash. <code>SankeyChart</code>&#39;s <code>orient=&quot;vertical&quot;</code> is now <code>vertical</code>, like <code>BarChart</code>&#39;s <code>horizontal</code>, and the <code>SankeyOrient</code> type is gone.</li><li><code>DonutChart</code> takes <code>v-model:hiddenSlices</code>. <code>AxisChartEmits</code> and <code>ScatterChartEmits</code> declare <code>update:hiddenSeries</code>, which both already fired.</li></ul><p><strong>Settled after the review, on the same terms:</strong></p><ul><li><strong>Breaking:</strong> the column that splits long data into series is <code>splitBy</code>, not <code>series</code>, on the axis charts and on <code>ScatterChart</code>. &quot;Series&quot; now means only a drawn series, which is what <code>seriesConfig</code>, <code>hiddenSeries</code> and <code>maxSeries</code> are keyed by. TypeScript reports the removed prop. But a plain template passes it through as an attribute and draws every row as one series, so search for <code>series=</code> and <code>:series=</code> on the charts after upgrading.</li><li>The axis charts take <code>smooth</code>, <code>showDataPoints</code> and <code>dashed</code> at the chart level, next to <code>showDataLabels</code> and <code>connectNulls</code>, and <code>connectNulls</code> joins the four in <code>seriesConfig</code>. One rule covers all five: the chart-level value is the default for every series, and a <code>seriesConfig</code> entry overrides it for one series, on or off. With <code>splitBy</code>, the data names the series, so a chart-level value is the only way to reach all of them.</li><li><strong>Breaking:</strong> <code>SeriesStyle.axis</code> is <strong>removed</strong>, replaced by the <code>y2</code> column prop above: one mechanism per concept. Series draw, and take palette slots, in <code>y</code> order and then <code>y2</code> order. <code>splitBy</code> splits <code>y</code> only. A <code>y2</code> column draws as the chart&#39;s own mark until <code>seriesConfig[key].type</code> says otherwise.</li><li><strong>Breaking:</strong> <code>DonutChart</code>&#39;s model is <code>v-model:hiddenSlices</code>, and its emit is <code>update:hiddenSlices</code>. A donut has no series (<code>maxSlices</code>, <code>DonutSlice</code> and <code>DonutSliceEvent</code> all say slice), so the one prop that said series was the odd one out. <code>hiddenSeries</code> is unchanged on the axis charts and on <code>ScatterChart</code>, and so is <code>ChartLegend</code>&#39;s own API.</li></ul><p><code>useChart</code>, <code>registerChartModules</code> and their three types stay on <code>frappe-ui/charts</code> and freeze there. frappe-ui owns the composable&#39;s shape and lifecycle. echarts owns the option and instance types it carries. The four <code>--chart-*</code> properties that are not ramps are documented on the <a href="/docs/charts/colors">chart colors</a> page, and the echarts template ref on the <a href="/docs/charts/overview">charts overview</a>.</p><h4 id="charts-v1-family-—-moved-to-frappe-ui-experimental-breaking" tabindex="-1">Charts (v1) family — moved to <code>frappe-ui/experimental</code> (breaking) <a class="header-anchor" href="#charts-v1-family-—-moved-to-frappe-ui-experimental-breaking" aria-label="Permalink to “Charts (v1) family — moved to frappe-ui/experimental (breaking)”">​</a></h4><p>The first chart family is not being brought up to the v1 quality bar at the root for <code>1.0.0</code> (#942). It moves to <code>frappe-ui/experimental</code>, which has no stability promise, with its public API unchanged, while apps migrate to <code>frappe-ui/charts</code>, which draws everything it did.</p><ul><li><strong>Breaking, loud:</strong> <code>import { AxisChart, ... } from &#39;frappe-ui&#39;</code> fails to resolve. Apps that spread <code>content</code> from <code>frappe-ui/tailwind</code> keep the styles automatically.</li><li>For new code, use <code>frappe-ui/charts</code>. Its props are flat and name the columns of your rows, so a <code>config</code> object becomes props. See the <a href="/docs/migration">migration guide</a>.</li></ul><p><strong>What to do:</strong> import <code>AxisChart</code>, <code>DonutChart</code>, <code>ECharts</code>, <code>FunnelChart</code>, <code>NumberChart</code> and <code>useAxisChartOptions</code> from <code>frappe-ui/experimental</code>. Changing the import path is the whole migration.</p><h3 id="calendar" tabindex="-1">Calendar <a class="header-anchor" href="#calendar" aria-label="Permalink to “Calendar”">​</a></h3><p><code>Calendar</code> is now in <code>frappe-ui/experimental</code>; see <a href="#calendar-family-—-moved-to-frappe-ui-experimental-breaking">the move entry</a>.</p><h4 id="calendar-—-the-month-view-is-a-continuous-strip-that-shows-every-event" tabindex="-1">Calendar — the Month view is a continuous strip that shows every event <a class="header-anchor" href="#calendar-—-the-month-view-is-a-continuous-strip-that-shows-every-event" aria-label="Permalink to “Calendar — the Month view is a continuous strip that shows every event”">​</a></h4><p>The Month view no longer draws a fixed 5- or 6-row grid that hid whatever did not fit behind an &quot;n more&quot; button. Its week rows grow to fit their events, the view scrolls when they outgrow it, and titles wrap instead of being cut off. Below the <code>sm</code> breakpoint it is the same grid one size smaller, and a cell shows what fits in it, with a <code>+n</code> count for the rest.</p><ul><li><strong>Behavior change:</strong> <code>rangeChange</code> for the <code>Month</code> view now reports the grid&#39;s full extent, including the padding days of its first and last weeks, rather than the first to the last day of the month.</li><li>Days outside the month in view are no longer dimmed. Instead, the first day of each month is labelled with its month (<code>Sep 1</code>).</li><li>Event titles in the Week and Day views are <code>ink-gray-8</code>, as in the Month view, rather than the text shade of the event&#39;s colour. The colour stays on the bar and the background.</li><li><code>CalendarActions</code> (the <code>CALENDAR_ACTIONS_KEY</code> injection) gains <code>setCalendarDate</code>.</li><li>An event with <code>isDraft: true</code> draws as a dashed outline in the event&#39;s colour, without the colour bar, rather than as a filled pill, in every view.</li><li>An event with <code>isDeclined: true</code> keeps its fill and bar, but its title is struck through and muted. In the Week and Day views it no longer takes a column of its own, so overlapping events lay out as if it were not there.</li><li><strong>Behavior change:</strong> <code>reloadEvents()</code> on the template ref now re-reads the <code>events</code> prop and discards edits made in the calendar since then (a drag or resize not yet saved). It used to hand the edited objects back.</li></ul><h4 id="calendar-—-todate-is-honored-so-events-span-the-days-they-cover-breaking-silent" tabindex="-1">Calendar — <code>toDate</code> is honored, so events span the days they cover (breaking, silent) <a class="header-anchor" href="#calendar-—-todate-is-honored-so-events-span-the-days-they-cover-breaking-silent" aria-label="Permalink to “Calendar — toDate is honored, so events span the days they cover (breaking, silent)”">​</a></h4><p>An event&#39;s <code>toDate</code> was ignored: everything rendered on <code>fromDate</code>. The event now shows on every day from <code>fromDate</code> to <code>toDate</code>, inclusive. In the Month view and the Week view&#39;s all-day row, it is one bar across those days. A timed event shorter than a day that crosses midnight shows as one piece per day in the time grid. A timed event that ends at <code>00:00</code> does not occupy that day.</p><ul><li><strong>Breaking, silent:</strong> an event passed with an <em>exclusive</em> end (<code>toDate</code> set to the day after its last day, the way iCalendar stores all-day spans) now shows one day too many.</li><li><strong>Behavior change:</strong> dragging keeps an event&#39;s length. <code>update</code> emits <code>toDate</code> shifted by the same number of days as <code>fromDate</code>. It used to set both to the drop day. A drag past midnight moves <code>toDate</code> (or <code>fromDate</code>) to the next day with a <code>00:00:00</code> time. It used to stop at <code>24:00:00</code>.</li><li>Full-day events (<code>isFullDay</code>) cover <code>fromDate</code>..<code>toDate</code> whole. Their times are ignored.</li></ul><p><strong>What to do:</strong> send the inclusive last day as <code>toDate</code>.</p><h4 id="calendar-family-—-moved-to-frappe-ui-experimental-breaking" tabindex="-1">Calendar family — moved to <code>frappe-ui/experimental</code> (breaking) <a class="header-anchor" href="#calendar-family-—-moved-to-frappe-ui-experimental-breaking" aria-label="Permalink to “Calendar family — moved to frappe-ui/experimental (breaking)”">​</a></h4><p>Calendar is not being brought up to the v1 quality bar at the root for <code>1.0.0</code> (#1020, redirect of #989). It moves to <code>frappe-ui/experimental</code>, which has no stability promise, with its public API unchanged, until a redesigned calendar family replaces it.</p><ul><li><strong>Breaking, loud:</strong> <code>import { Calendar, ... } from &#39;frappe-ui&#39;</code> fails to resolve. Changing the import path is the whole migration. Apps that spread <code>content</code> from <code>frappe-ui/tailwind</code> keep the Calendar styles automatically.</li><li><strong>Fix:</strong> the default header&#39;s month-title button renders again. It broke when DatePicker&#39;s <code>#target</code> slot became <code>#trigger</code>. The all-day collapse buttons show their chevron icons again.</li></ul><p><strong>What to do:</strong> import these from <code>frappe-ui/experimental</code> instead: <code>Calendar</code>, <code>CalendarColorMap</code>, <code>CalendarActiveEvent</code>, and the types <code>CalendarActions</code>, <code>CalendarCellClickData</code>, <code>CalendarConfig</code>, <code>CalendarEvent</code>, <code>CalendarMode</code>, <code>CalendarPublicProps</code>, <code>CalendarTimeFormat</code>, <code>GroupedCalendarEvents</code>.</p><h2 id="deprecation-log" tabindex="-1">Deprecation log <a class="header-anchor" href="#deprecation-log" aria-label="Permalink to “Deprecation log”">​</a></h2><p>Each row names an old API, what replaces it, and what happens to code that still uses it. &quot;Silent&quot; means old code still runs and nothing fails. &quot;Loud&quot; means the import, build or call fails.</p><table tabindex="0"><thead><tr><th>API</th><th>Replacement</th><th>Notes</th></tr></thead><tbody><tr><td><code>Divider.action.handler</code></td><td><code>Divider.action.onClick</code></td><td><strong>Removed</strong>, silent: the key is dropped and the click does nothing</td></tr><tr><td><code>Password.value</code> prop</td><td><code>v-model</code> / <code>modelValue</code></td><td><strong>Removed in 1.0.0</strong> (ADR-0008)</td></tr><tr><td><code>Rating.rating_from</code> prop</td><td><code>max</code></td><td><strong>Removed</strong>, silent: prop ignored</td></tr><tr><td><code>Rating.readonly</code> prop</td><td><code>disabled</code></td><td><strong>Removed</strong>, silent: prop ignored</td></tr><tr><td><code>Switch.change</code> emit</td><td><code>update:modelValue</code> / <code>v-model</code></td><td><strong>Removed</strong>, silent: listener never fires</td></tr><tr><td><code>Switch.labelClasses</code> prop</td><td><code>data-*</code> styling hooks</td><td><strong>Removed</strong>, silent: prop ignored</td></tr><tr><td><code>Checkbox.padding</code> prop</td><td><code>padded</code></td><td><strong>Removed</strong>, silent: prop ignored</td></tr><tr><td><code>Dropdown</code> <code>{ group, items }</code></td><td><code>{ group, options }</code></td><td><strong>Removed</strong>, silent: renders empty, warning in development only</td></tr><tr><td><code>Dropdown.placement</code> prop</td><td><code>align</code></td><td><strong>Removed</strong>, silent: falls back to <code>align=&quot;start&quot;</code></td></tr><tr><td><code>Dropdown</code>/<code>ContextMenu</code> <code>component:</code> rows</td><td><code>slots: { item: fn }</code></td><td><strong>Removed</strong>, silent: renders a row with only the label, warning in development only</td></tr><tr><td><code>DropdownExposed</code> type</td><td><code>v-model:open</code> / <code>close</code> slot prop</td><td><strong>Removed</strong>, loud: it described an exposed member that never existed</td></tr><tr><td>Select <code>#item-*</code> slot prop <code>option</code></td><td><code>item</code></td><td><strong>Removed</strong>, silent: <code>{ option }</code> destructures to <code>undefined</code></td></tr><tr><td><code>Input.vue</code></td><td><code>TextInput</code></td><td><strong>Removed in 1.0.0</strong> (ADR-0008)</td></tr><tr><td><code>Autocomplete</code></td><td><code>Combobox</code> or <code>MultiSelect</code></td><td><strong>Removed</strong>: import fails</td></tr><tr><td><code>GridLayout</code></td><td>depend on <code>grid-layout-plus</code> directly</td><td><strong>Removed</strong>, loud: import fails</td></tr><tr><td><code>FormControl type=&#39;autocomplete&#39;</code></td><td><code>type=&quot;combobox&quot;</code>, or <code>Combobox</code> on its own</td><td><strong>Removed</strong>, silent: <code>console.error</code> in development only</td></tr><tr><td>DatePicker family <code>placement</code></td><td><code>side</code> + <code>align</code> + <code>offset</code></td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td>DatePicker family <code>autoClose</code></td><td><code>keepOpen</code> (inverse)</td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td>DatePicker family <code>allowCustom</code></td><td><code>typeable: false</code></td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td>DatePicker family <code>readonly</code></td><td><code>typeable: false</code></td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td>DatePicker family <code>inputClass</code></td><td><code>class</code> on the component element</td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td>DatePicker family <code>value</code> prop</td><td><code>v-model</code> / <code>modelValue</code></td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td>DatePicker family <code>#target</code> slot</td><td><code>#trigger</code></td><td><strong>Removed</strong>, silent: slot content stops rendering</td></tr><tr><td><code>TimePicker.scrollMode</code></td><td>none (always centered)</td><td><strong>Removed</strong>, silent: unused extra attribute</td></tr><tr><td><code>DateTimePicker.minDateTime</code></td><td><code>min</code></td><td><strong>Removed</strong>, silent: limit no longer enforced</td></tr><tr><td><code>DateTimePicker.maxDateTime</code></td><td><code>max</code></td><td><strong>Removed</strong>, silent: limit no longer enforced</td></tr><tr><td><code>TimePicker.minTime</code></td><td><code>min</code></td><td><strong>Removed</strong>, silent: limit no longer enforced</td></tr><tr><td><code>TimePicker.maxTime</code></td><td><code>max</code></td><td><strong>Removed</strong>, silent: limit no longer enforced</td></tr><tr><td><code>TimePicker.selectAll()</code> / <code>.blurInput()</code></td><td>none: unused, no callers</td><td><strong>Removed</strong>, loud: template-ref member gone</td></tr><tr><td><code>useDatePicker</code> composable</td><td>use the picker components directly</td><td><strong>Removed</strong>, loud: import fails</td></tr><tr><td><code>getDate</code> / <code>getDatesAfter</code> / etc.</td><td>use the picker components directly</td><td><strong>Removed</strong>, loud: import fails</td></tr><tr><td><code>MonthPicker</code></td><td><code>Select</code></td><td><strong>Removed</strong>, loud: import fails</td></tr><tr><td><code>FeatherIcon</code></td><td><code>lucide-*</code> strings (or a <code>Component</code>)</td><td><strong>Removed</strong>: import fails; props with feather names render nothing and warn once in development</td></tr><tr><td><code>Card</code></td><td>layout markup</td><td><strong>Removed in 1.0.0</strong> (ADR-0008), import fails</td></tr><tr><td><code>ListItem</code></td><td>layout markup</td><td><strong>Removed in 1.0.0</strong> (ADR-0008), import fails</td></tr><tr><td><code>Toast</code> (SFC)</td><td>imperative <code>toast(...)</code> API</td><td><strong>Removed in 1.0.0</strong> (ADR-0008), import fails</td></tr><tr><td>Dialog legacy <code>options</code> blob</td><td>flat top-level props</td><td><strong>Removed</strong>, silent: unused attribute</td></tr><tr><td>Dialog <code>disableOutsideClickToClose</code></td><td><code>dismissible</code> (inverted)</td><td><strong>Removed</strong>, silent: unused attribute</td></tr><tr><td>Dialog <code>#body*</code> slots</td><td><code>#default</code> / <code>#title</code> / <code>#actions</code></td><td><strong>Removed</strong>, silent: renders nothing</td></tr><tr><td>Dialog <code>icon.appearance</code></td><td><code>icon.theme</code></td><td><strong>Removed</strong>, silent: icon loses its tone</td></tr><tr><td>Dialog action <code>onClick</code> callable context</td><td><code>{ close }</code> object</td><td><strong>Removed</strong>: throws on call</td></tr><tr><td>Dialog template-ref <code>close()</code></td><td><code>v-model:open</code> / <code>close</code> slot prop</td><td><strong>Removed</strong>: throws on call</td></tr><tr><td><code>ConfirmDialog</code> component</td><td><code>dialog.confirm()</code> / <code>dialog.danger()</code></td><td><strong>Removed</strong>: import fails</td></tr><tr><td><code>confirmDialog()</code></td><td><code>dialog.confirm()</code></td><td><strong>Removed</strong>: import fails</td></tr><tr><td><code>FileUploader.uploadArgs</code></td><td>flat props (<code>private</code>, <code>folder</code>, <code>doctype</code>, <code>docname</code>, <code>fieldname</code>, <code>uploadEndpoint</code>, <code>optimize</code>)</td><td><strong>Removed</strong>, silent: unused attribute</td></tr><tr><td><code>FileUploader</code> template-ref <code>inputRef</code></td><td><code>openFileSelector</code> slot prop</td><td><strong>Removed</strong>: throws on call</td></tr><tr><td><code>FileUploader</code> slot prop <code>error</code></td><td>always <code>string | null</code>, was <code>unknown</code></td><td><strong>Changed</strong>, silent: <code>.message</code> access renders nothing</td></tr><tr><td><code>useFileUpload</code> / <code>FileUploadHandler</code> unset privacy</td><td>explicit <code>private</code></td><td><strong>Default changed</strong>, silent: now resolves to private</td></tr><tr><td><code>fileToBase64</code>, <code>formatBytes</code>, <code>getMaxFileSize</code>, <code>fileSizeLimitMessage</code></td><td>none (internal only)</td><td><strong>Removed</strong>: import fails</td></tr><tr><td><code>frappe-ui/charts</code> <code>ColorScheme</code> type</td><td>root <code>ResolvedColorScheme</code> (import it from <code>frappe-ui</code>)</td><td><strong>Removed</strong>, loud: type import fails</td></tr><tr><td><code>Badge theme=&quot;orange&quot;</code></td><td><code>theme=&quot;amber&quot;</code></td><td><strong>Removed in 1.0.0</strong> (ADR-0008). Loud in TS (compile error); silent in JS (renders gray, warning in development only)</td></tr></tbody></table>`,743)])])}const g=o(s,[["render",r]]);export{u as __pageData,g as default};
